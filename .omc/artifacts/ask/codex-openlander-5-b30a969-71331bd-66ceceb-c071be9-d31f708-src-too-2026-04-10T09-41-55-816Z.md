# codex advisor artifact

- Provider: codex
- Exit code: 0
- Created at: 2026-04-10T09:41:55.816Z

## Original task

OpenLander 블루그린 배포 파이프라인의 버그픽스 5개 커밋(b30a969, 71331bd, 66ceceb, c071be9, d31f708)을 리뷰해줘.

변경 파일: src/tools/defs/deploy.ts, src/db/repos/project.repo.ts, src/db/schema.drizzle.ts, src/pipeline/rollback.ts, src/pipeline/blueGreenRedeploy.ts, src/pipeline/executePlan.ts, 테스트 파일들

Bug 1+3: get_deploy_status wait=true에 5초 DB poll 추가 + lock 있고 job 없으면 queued 반환
Bug 5: releaseDeployLock에 sessionId 파라미터 추가 — 세션 불일치 시 false + 경고 로그. pipeline 3곳에서 sessionId 전달, event handler 4곳은 unconditional
Bug 6: environments 테이블에 container_port 컬럼 + 마이그레이션 + backfill. rollback/blueGreenRedeploy에서 environment에도 containerPort 저장

집중 포인트:

1. 레이스 컨디션: DB poll 5초 인터벌과 lock acquire/release 사이 타이밍 이슈
2. 세션 스코프 lock: sessionId 불일치 시 lock이 영구 잠금되는 시나리오 없는지
3. 마이그레이션 안전성: container_port 추가 시 기존 데이터 무결성
4. rollback 경로: containerPort가 project와 environment 양쪽에 정확히 저장되는지
5. 테스트: mock 수정이 실제 동작과 일치하는지
6. SQL injection, 타입 안전성, 에러 처리

한국어로 답변해줘.

## Final prompt

OpenLander 블루그린 배포 파이프라인의 버그픽스 5개 커밋(b30a969, 71331bd, 66ceceb, c071be9, d31f708)을 리뷰해줘.

변경 파일: src/tools/defs/deploy.ts, src/db/repos/project.repo.ts, src/db/schema.drizzle.ts, src/pipeline/rollback.ts, src/pipeline/blueGreenRedeploy.ts, src/pipeline/executePlan.ts, 테스트 파일들

Bug 1+3: get_deploy_status wait=true에 5초 DB poll 추가 + lock 있고 job 없으면 queued 반환
Bug 5: releaseDeployLock에 sessionId 파라미터 추가 — 세션 불일치 시 false + 경고 로그. pipeline 3곳에서 sessionId 전달, event handler 4곳은 unconditional
Bug 6: environments 테이블에 container_port 컬럼 + 마이그레이션 + backfill. rollback/blueGreenRedeploy에서 environment에도 containerPort 저장

집중 포인트:

1. 레이스 컨디션: DB poll 5초 인터벌과 lock acquire/release 사이 타이밍 이슈
2. 세션 스코프 lock: sessionId 불일치 시 lock이 영구 잠금되는 시나리오 없는지
3. 마이그레이션 안전성: container_port 추가 시 기존 데이터 무결성
4. rollback 경로: containerPort가 project와 environment 양쪽에 정확히 저장되는지
5. 테스트: mock 수정이 실제 동작과 일치하는지
6. SQL injection, 타입 안전성, 에러 처리

한국어로 답변해줘.

## Raw output

```text
1. 높음: `wait=true`가 “이번 배포”가 아니라 “가장 최근 성공 로그”만 보고 `done`으로 오판할 수 있습니다. [src/tools/defs/deploy.ts:512](/home/lee/OpenLander/src/tools/defs/deploy.ts#L512) 의 5초 poll은 `lastLog.status === 'success'` 이고 현재 `JobManager`에 job이 없으면 바로 완료로 해석합니다. 그런데 호출 시점 이전의 오래된 성공 로그와 현재 idle 상태를 구분할 기준이 없습니다. 그래서 idle 프로젝트에 `get_deploy_status(wait=true)`를 호출해도 5초 뒤 `done`이 반환될 수 있고, 락 획득 전/후 레이스로 새 배포 시작 직전의 공백 구간에서도 이전 성공 로그를 주워 false positive가 납니다. 최소한 호출 시작 시각, lock session, 혹은 관찰 대상 job 시작 시각을 기준으로 “이 호출 이후 생성된 로그만” 봐야 합니다.

2. 중간: `environments.container_port` backfill은 “컬럼이 없는 DB”에서만 실행되어, `66ceceb`를 한 번 거친 DB는 `d31f708` 이후에도 영구히 `NULL`이 남을 수 있습니다. [src/db/migration.ts:103](/home/lee/OpenLander/src/db/migration.ts#L103) 은 `container_port` 컬럼이 없을 때만 `ALTER TABLE`과 `UPDATE ... SET container_port = (SELECT ...)`를 같이 수행합니다. 즉 `66ceceb` 시점에 이미 컬럼만 추가된 인스턴스는, 이후 `d31f708`로 올려도 조건문에 걸리지 않아 backfill이 재실행되지 않습니다. 데이터 무결성 측면에서 backfill은 컬럼 존재 여부와 분리해 idempotent하게 항상 실행하거나, 최소한 `WHERE container_port IS NULL` 조건으로 별도 실행해야 안전합니다.

3. 낮음: 새 테스트 하나가 실제 동작을 검증하지 못합니다. [test/deploy-lock.test.ts:341](/home/lee/OpenLander/test/deploy-lock.test.ts#L341) 의 “queued to done” 테스트는 첫 호출이 [src/tools/defs/deploy.ts:396](/home/lee/OpenLander/src/tools/defs/deploy.ts#L396) 에서 즉시 `queued`로 반환된 뒤, 상태를 바꾸고 두 번째 호출로 `done`을 확인합니다. 즉 이벤트 구독 경로([src/tools/defs/deploy.ts:491](/home/lee/OpenLander/src/tools/defs/deploy.ts#L491))나 5초 DB poll 경로를 전혀 타지 않아서, 설명과 달리 “하나의 wait 호출이 queued에서 done으로 전이되는지”는 검증하지 못합니다. 이 테스트는 회귀 방지력이 약합니다.

세션 스코프 lock 자체는 이번 변경 범위 안에서는 “영구 잠금” 시나리오를 재현하지 못했습니다. `acquire`/`release`가 같은 `sessionId`를 쓰고, mismatch 시에도 lock을 지우지 않을 뿐이며, 만료 청소도 있습니다. 다만 변경 밖의 코드인 [src/web/api/routes.ts:190](/home/lee/OpenLander/src/web/api/routes.ts#L190) 부근은 여전히 unconditional release라서, 영구 잠금 대신 “오래된 완료 이벤트가 새 세션 lock을 너무 일찍 풀어버리는” 반대 방향의 리스크는 남아 있습니다.

확인 결과 관련 테스트 6개와 `typecheck`는 모두 통과했습니다. 변경 파일들에서는 SQL injection 징후는 보이지 않았고, 쿼리는 Drizzle 조건식이나 상수 SQL만 사용합니다.


Reading additional input from stdin...
OpenAI Codex v0.118.0 (research preview)
--------
workdir: /home/lee/OpenLander
model: gpt-5.4
provider: openai
approval: never
sandbox: danger-full-access
reasoning effort: none
reasoning summaries: none
session id: 019d76c1-9c43-72a2-9b94-675c9f71b12b
--------
user
OpenLander 블루그린 배포 파이프라인의 버그픽스 5개 커밋(b30a969, 71331bd, 66ceceb, c071be9, d31f708)을 리뷰해줘.

변경 파일: src/tools/defs/deploy.ts, src/db/repos/project.repo.ts, src/db/schema.drizzle.ts, src/pipeline/rollback.ts, src/pipeline/blueGreenRedeploy.ts, src/pipeline/executePlan.ts, 테스트 파일들

Bug 1+3: get_deploy_status wait=true에 5초 DB poll 추가 + lock 있고 job 없으면 queued 반환
Bug 5: releaseDeployLock에 sessionId 파라미터 추가 — 세션 불일치 시 false + 경고 로그. pipeline 3곳에서 sessionId 전달, event handler 4곳은 unconditional
Bug 6: environments 테이블에 container_port 컬럼 + 마이그레이션 + backfill. rollback/blueGreenRedeploy에서 environment에도 containerPort 저장

집중 포인트:
1. 레이스 컨디션: DB poll 5초 인터벌과 lock acquire/release 사이 타이밍 이슈
2. 세션 스코프 lock: sessionId 불일치 시 lock이 영구 잠금되는 시나리오 없는지
3. 마이그레이션 안전성: container_port 추가 시 기존 데이터 무결성
4. rollback 경로: containerPort가 project와 environment 양쪽에 정확히 저장되는지
5. 테스트: mock 수정이 실제 동작과 일치하는지
6. SQL injection, 타입 안전성, 에러 처리

한국어로 답변해줘.
codex
요청한 5개 커밋의 변경 범위를 기준으로, 실제 diff와 현재 파일 상태를 대조해서 레이스 컨디션·세션 스코프 lock·마이그레이션·rollback 저장 경로·테스트 정합성을 중심으로 검토하겠습니다.
exec
/bin/bash -lc 'git status --short' in /home/lee/OpenLander
 succeeded in 0ms:
 M .omc/state/hud-stdin-cache.json
?? logs/mcp-logger-20260410-181155.log
?? logs/mcp-logger-20260410-181249.log
?? logs/mcp-logger-20260410-183029.log
?? logs/mcp-logger-20260410-183602.log

exec
/bin/bash -lc "rg -n \"get_deploy_status|releaseDeployLock|container_port|containerPort|sessionId|queued|wait=true|wait = true|wait\\?\" src test web/test e2e" in /home/lee/OpenLander
 succeeded in 0ms:
e2e/quality-gate/mcp.spec.ts:113:        name: 'get_deploy_status',
src/pipeline/build-deploy-config.ts:56:    containerPort: project.container_port ?? undefined,
src/pipeline/auto-recovery.ts:51:    sessionId?: string,
src/pipeline/auto-recovery.ts:415:        const sessionId = nanoid(12);
src/pipeline/auto-recovery.ts:578:          sessionId,
src/pipeline/recover.ts:148:    const containerPort = getServiceContainerPort(service);
src/pipeline/recover.ts:175:        [`${String(containerPort)}/tcp`]: {},
src/pipeline/recover.ts:187:          [`${String(containerPort)}/tcp`]: [{ HostPort: String(service.port) }],
src/pipeline/recover.ts:275:    const containerPort = project.container_port ?? port;
src/pipeline/recover.ts:288:    const traefikLabels = buildTraefikLabels(project.name, containerPort, undefined, 'production');
src/pipeline/recover.ts:298:      containerPort,
test/deploy-plan-integration.test.ts:168:        next_steps: ['Poll get_deploy_status to monitor build progress'],
src/pipeline/preview.ts:111:      const containerPort = (await this.docker.getImageExposedPort(imageTag)) ?? port;
src/pipeline/preview.ts:112:      const traefikLabels = buildTraefikLabels(previewName, containerPort);
src/pipeline/preview.ts:118:        containerPort,
src/pipeline/traefik.ts:422:  containerPort: number,
src/pipeline/traefik.ts:435:    [`traefik.http.services.${routerName}.loadbalancer.server.port`]: String(containerPort),
src/pipeline/compose.ts:625:      { containerId: string; port: number; containerPort: number }
src/pipeline/compose.ts:735:            const containerPort = parsedPort?.containerPort ?? hostPort;
src/pipeline/compose.ts:737:            const traefikLabels = buildTraefikLabels(routeName, containerPort, undefined, envType);
src/pipeline/compose.ts:748:                  containerPort,
src/pipeline/compose.ts:785:              containerPort,
src/pipeline/compose.ts:795:            buildLog += `[compose run ${service.name}] ${containerId.slice(0, 12)} ${String(hostPort)}:${String(containerPort)}\n`;
src/pipeline/compose.ts:899:            ports: [`${String(deployment.port)}:${String(deployment.containerPort)}`],
src/pipeline/compose.ts:1209:  ): { hostPort: number | null; containerPort: number } | null {
src/pipeline/compose.ts:1372:): { hostPort: number | null; containerPort: number } | null {
src/pipeline/compose.ts:1394:    const containerPort = Number(numericTokens[0]);
src/pipeline/compose.ts:1395:    if (!Number.isFinite(containerPort)) {
src/pipeline/compose.ts:1398:    return { hostPort: null, containerPort };
src/pipeline/compose.ts:1401:  const containerPort = Number(numericTokens[numericTokens.length - 1]);
src/pipeline/compose.ts:1403:  if (!Number.isFinite(containerPort) || !Number.isFinite(hostPort)) {
src/pipeline/compose.ts:1407:  return { hostPort, containerPort };
src/pipeline/cloudflare.ts:173:    containerPort: number,
src/pipeline/cloudflare.ts:176:    const labels = buildTraefikLabels(projectName, containerPort, domains[0]);
src/pipeline/docker.ts:40:  containerPort?: number;
src/pipeline/docker.ts:54:  containerPort?: number;
src/pipeline/docker.ts:431:    const cPort = options.containerPort ?? options.port;
src/pipeline/docker.ts:483:    const cPort = opts.containerPort ?? opts.port;
src/pipeline/service-manager.ts:449:    const containerPort = port;
src/pipeline/service-manager.ts:491:        [`${String(containerPort)}/tcp`]: {},
src/pipeline/service-manager.ts:503:          [`${String(containerPort)}/tcp`]: [{ HostPort: String(hostPort) }],
src/pipeline/deploy-plan/engine.ts:44:  containerPort?: number;
src/pipeline/deploy-plan/engine.ts:62:  containerPort?: number;
src/pipeline/deploy-plan/engine.ts:328:      containerPort: opts.containerPort,
src/pipeline/deploy-plan/engine.ts:337:      execution.containerPort === undefined
src/pipeline/deploy-plan/engine.ts:790:        this.db.releaseDeployLock(lockProjectId, lockSessionId ?? `plan-${planId}`);
src/pipeline/deploy-plan/engine.ts:891:          ...(execution.containerPort !== undefined
src/pipeline/deploy-plan/engine.ts:892:            ? { containerPort: execution.containerPort }
src/pipeline/deploy-core.ts:110:  containerPort?: number;
src/pipeline/deploy-core.ts:413:              containerPort: config.containerPort,
src/pipeline/deploy-core.ts:442:            containerPort: config.containerPort,
src/pipeline/deploy-core.ts:559:              containerPort: config.containerPort,
src/pipeline/deploy-core.ts:573:              containerPort: config.containerPort,
src/pipeline/deploy-core.ts:582:        containerPort: config.containerPort,
src/pipeline/deploy-core.ts:772:        if (!deployConfig.containerPort) {
src/pipeline/deploy-core.ts:775:            deployConfig.containerPort = exposedPort;
src/pipeline/deploy-core.ts:1392:      this.db.releaseDeployLock(projectId, lockSession);
src/pipeline/deploy-core.ts:1514:      const containerPort = (await this.docker.getImageExposedPort(imageTag)) ?? newPort;
src/pipeline/deploy-core.ts:1523:        containerPort,
src/pipeline/deploy-core.ts:1569:        containerPort,
src/pipeline/deploy-core.ts:1578:          containerPort,
src/pipeline/deploy-core.ts:1803:      this.db.releaseDeployLock(projectId, lockSession);
src/pipeline/job-manager.ts:1:export type JobPhase = 'queued' | 'cloning' | 'building' | 'starting' | 'done' | 'failed';
src/pipeline/job-manager.ts:32:      phase: 'queued',
src/pipeline/deploy-queue.ts:64:  /** Get the position in queue (0 = next to run, -1 = not queued). */
src/pipeline/deploy-queue.ts:90:        log.debug({ remaining: this.queue.length }, 'Releasing lock to next queued job');
src/pipeline/deploy/run-step.ts:18:  containerPort?: number;
src/pipeline/deploy/run-step.ts:45:      const configuredContainerPort = config.containerPort;
src/pipeline/deploy/run-step.ts:46:      const containerPort =
src/pipeline/deploy/run-step.ts:54:        containerPort,
src/pipeline/deploy/run-step.ts:64:          containerPort,
src/pipeline/deploy/monorepo-orchestrator.ts:163:      containerPort: childContainerPort,
src/pipeline/deploy/rollback.ts:103:      const containerPort = (await this.docker.getImageExposedPort(rollbackImageTag)) ?? port;
src/pipeline/deploy/rollback.ts:110:        containerPort,
src/pipeline/deploy/rollback.ts:112:        traefikLabels: buildTraefikLabels(project.name, containerPort, undefined, envType),
src/pipeline/deploy/rollback.ts:119:        containerPort,
src/pipeline/deploy/rollback.ts:132:          containerPort,
src/pipeline/deploy/orchestrator.ts:466:  const containerPort =
src/pipeline/deploy/orchestrator.ts:467:    config.containerPort ??
src/pipeline/deploy/orchestrator.ts:486:    containerPort,
src/pipeline/deploy/orchestrator.ts:592:      ...(containerPort != null ? { containerPort } : {}),
test/docker.test.ts:249:      containerPort: 3000,
test/docker.test.ts:284:      containerPort: 3000,
test/docker.test.ts:343:      containerPort: 3000,
test/docker.test.ts:379:      containerPort: 3000,
test/docker.test.ts:399:      containerPort: 3000,
test/deploy-queue.test.ts:48:  it('multiple queued jobs are processed in FIFO order', async () => {
src/channels/base.ts:302:    const sessionId = `${msg.channelType}-${msg.channelId}-${msg.sender}`;
src/channels/base.ts:423:      await this.ctx.agent.chatStream(msg.content, onStreamEvent, sessionId);
src/channels/base.ts:499:          response = `🔄 Restart for "${projectName}" queued — use the web dashboard to confirm`;
src/types/agent-events.ts:23:  | { type: 'session'; sessionId: string; actionRunId?: string }
test/pipeline/image-deploy-e2e.test.ts:27:    containerPort: parsed.port,
test/pipeline/image-deploy-e2e.test.ts:131:        containerPort: config.containerPort,
test/pipeline/image-deploy-e2e.test.ts:150:        containerPort: 8080,
test/pipeline/image-deploy-e2e.test.ts:170:    expect(assembledConfig.containerPort).toBe(8080);
test/job-manager.test.ts:13:  it('tracks a new job with queued phase', () => {
test/job-manager.test.ts:17:    expect(status!.phase).toBe('queued');
src/ipc/client.ts:137:  async chat(message: string, sessionId: string): Promise<AgentResponse & { sessionId: string }> {
src/ipc/client.ts:138:    return this.post<AgentResponse & { sessionId: string }>('/api/chat', {
src/ipc/client.ts:140:      session_id: sessionId,
src/ipc/client.ts:150:    sessionId: string,
src/ipc/client.ts:154:      const body = JSON.stringify({ message, session_id: sessionId });
test/app.test.ts:28:    container_port: 3000,
test/db.test.ts:437:  describe('Environments container_port', () => {
test/db.test.ts:442:    it('environments table has container_port column after migration', () => {
test/db.test.ts:453:      expect(columns).toContain('container_port');
test/db.test.ts:456:    it('updateEnvironment persists containerPort', () => {
test/db.test.ts:461:      db.updateEnvironment(env!.id, { containerPort: 8080 });
test/db.test.ts:465:      expect(updated!.container_port).toBe(8080);
test/mcp/tool-registry-snapshot.test.ts:46:  'get_deploy_status',
src/tools/defs/schemas.ts:175:    .describe('Max wait time in seconds (default 300, only used with wait=true)'),
src/tools/defs/schemas.ts:607:      .describe('Max seconds to wait for completion when wait=true (default: 300)'),
src/tools/defs/schemas.ts:612:        'Create a temporary public URL via TryCloudflare after deploy succeeds (default: false). Requires wait=true.',
src/tools/defs/schemas.ts:618:        'Map a custom domain after deploy succeeds (requires Cloudflare config). Example: api.myapp.com. Requires wait=true.',
test/pipeline/image-deploy.test.ts:33:      containerPort: 8080,
test/pipeline/image-deploy.test.ts:41:    expect(config.containerPort).toBe(8080);
test/pipeline/image-deploy.test.ts:45:  it('preserves image_url, image_cmd, and container_port from DB columns', () => {
test/pipeline/image-deploy.test.ts:53:      containerPort: 80,
test/pipeline/image-deploy.test.ts:60:    expect(config.containerPort).toBe(80);
test/pipeline/image-deploy.test.ts:147:    expect(config.containerPort).toBeUndefined();
test/llm/decision.test.ts:39:  it('get_deploy_status → ALLOW', () => {
test/llm/decision.test.ts:40:    expect(engine.classify('get_deploy_status')).toBe('ALLOW');
src/tools/defs/deploy-plan.ts:28:      next_steps: ['Wait 30 seconds and try again', 'Check deploy status with get_deploy_status'],
src/tools/defs/deploy-plan.ts:35:  sessionId: string,
src/tools/defs/deploy-plan.ts:38:  const locked = context.appCtx.db.acquireDeployLock(projectId, sessionId);
src/tools/defs/deploy-plan.ts:68:        containerPort: (args['port'] as number | undefined) ?? undefined,
src/tools/defs/deploy-plan.ts:157:      'Execute a deployment plan asynchronously. Returns immediately with project_id and status. Use get_deploy_status to poll progress. Plan must be in "ready" status. Provisions services, injects env vars, and starts deployment.',
src/tools/defs/deploy-plan.ts:219:            next_steps: ['Poll get_deploy_status to monitor build progress'],
src/tools/defs/deploy-plan.ts:241:      'One-call deploy: analyzes repo, creates plan, executes, and optionally waits for completion. Combines create_deploy_plan + execute_deploy_plan + get_deploy_status into a single call. Returns final deployment result with URL when done, including internal_host, docker_host, elapsed, and on failure auto_diagnosis/build_log_tail; timeout may be returned when wait times out. If the plan needs missing env vars, returns status "needs_input" with the missing list — provide them and call again. Power users can still use the 3-step flow for finer control.',
src/tools/defs/deploy-plan.ts:243:      'One-call deploy: repo analysis → build → deploy → result. Returns immediately with status. Poll get_deploy_status to track progress. Returns URL on success, error + diagnosis guidance on failure. Use the 3-step flow (create/execute/status) for finer control.',
src/tools/defs/deploy-plan.ts:262:        containerPort: (args['port'] as number | undefined) ?? undefined,
src/tools/defs/deploy-plan.ts:327:        const nextSteps = ['Poll get_deploy_status to monitor build progress'];
src/tools/defs/deploy-plan.ts:330:            'expose/domain require wait=true. After deploy completes, call expose_public or map_domain separately.',
src/tools/defs/deploy-plan.ts:351:            next_steps: ['Poll get_deploy_status to monitor build progress'],
src/tools/defs/deploy-plan.ts:481:              next_steps: ['Poll get_deploy_status to check current progress'],
src/tools/defs/deploy.ts:25:      next_steps: ['Wait 30 seconds and try again', 'Check deploy status with get_deploy_status'],
src/tools/defs/deploy.ts:32:  sessionId: string,
src/tools/defs/deploy.ts:35:  const locked = context.appCtx.db.acquireDeployLock(projectId, sessionId);
src/tools/defs/deploy.ts:105:                'Call get_deploy_status to confirm rollback completed successfully.',
src/tools/defs/deploy.ts:158:            'Call get_deploy_status to poll blue-green deployment progress. Do NOT use wait=true — it blocks the agent.',
src/tools/defs/deploy.ts:199:    name: 'get_deploy_status',
src/tools/defs/deploy.ts:202:      'Get real-time deployment status for one or all projects currently being built. Shows phase (queued/cloning/building/starting/done/failed), timing, and progress details. jobs[] may include urls, internal_host, docker_host, completed_at, health (done), build_step/build_step_total/build_step_desc (building), auto_diagnosis (failed), build_log_tail, and timeout. Use when user asks "is it done yet?" or "what is building?" during a deploy. Returns { active, jobs[] }. If no deploys are in progress, returns { active: 0, jobs: [] }. With wait=true: blocks until completion. Without project_name, waits for ALL active deploys to finish.',
src/tools/defs/deploy.ts:286:        job.phase === 'queued'
src/tools/defs/deploy.ts:290:                  'Deploy is in progress. Poll get_deploy_status periodically to check completion. Do NOT use wait=true — it blocks the agent from responding to the user.',
src/tools/defs/deploy.ts:362:              phase: 'queued',
src/tools/defs/deploy.ts:384:                phase: 'queued',
test/pipeline/build-deploy-config.test.ts:92:      containerPort: undefined,
src/tools/defs/project-ops.ts:28:      next_steps: ['Wait 30 seconds and try again', 'Check deploy status with get_deploy_status'],
src/tools/defs/project-ops.ts:35:  sessionId: string,
src/tools/defs/project-ops.ts:38:  const locked = context.appCtx.db.acquireDeployLock(projectId, sessionId);
src/tools/defs/project-ops.ts:174:      'Restart a running project by stopping and redeploying it with the same configuration. Use when user reports the app is hung, unresponsive, or needs a fresh start after config changes. Returns { status, project, message }. Redeploy runs in background — poll get_deploy_status. Errors: PROJECT_NOT_FOUND.',
src/tools/defs/project-ops.ts:202:          ? 'Restart initiated (no_cache). Full rebuild may take 3-5+ minutes. Poll get_deploy_status to track progress.'
src/tools/defs/project-ops.ts:203:          : 'Redeployment started. Poll get_deploy_status to track progress.',
src/tools/defs/project-ops.ts:277:          ? 'Redeployment started (no_cache). Full rebuild may take 3-5+ minutes. Poll get_deploy_status to track progress.'
src/tools/defs/project-ops.ts:278:          : 'Redeployment started. Poll get_deploy_status to track progress.',
test/deploy-environment.test.ts:547:        containerPort: 8080,
test/deploy-lock.test.ts:48:  const tool = deployToolDefs.find((entry) => entry.name === 'get_deploy_status');
test/deploy-lock.test.ts:102:      db.releaseDeployLock('p1');
test/deploy-lock.test.ts:124:    it('releaseDeployLock with correct session succeeds', () => {
test/deploy-lock.test.ts:132:      const releaseDeployLock = Reflect.get(db, 'releaseDeployLock') as (
test/deploy-lock.test.ts:134:        sessionId?: string,
test/deploy-lock.test.ts:137:      expect(releaseDeployLock.call(db, 'p1', 'session-abc')).toBe(true);
test/deploy-lock.test.ts:141:    it('releaseDeployLock with wrong session returns false and keeps lock', () => {
test/deploy-lock.test.ts:149:      const releaseDeployLock = Reflect.get(db, 'releaseDeployLock') as (
test/deploy-lock.test.ts:151:        sessionId?: string,
test/deploy-lock.test.ts:154:      expect(releaseDeployLock.call(db, 'p1', 'session-xyz')).toBe(false);
test/deploy-lock.test.ts:160:    it('releaseDeployLock without session releases unconditionally', () => {
test/deploy-lock.test.ts:169:      expect(db.releaseDeployLock('p1')).toBe(true);
test/deploy-lock.test.ts:283:  describe('get_deploy_status wait=true hardening', () => {
test/deploy-lock.test.ts:284:    it('wait=true resolves from deploy_logs when job not in JobManager', async () => {
test/deploy-lock.test.ts:317:    it('wait=true shows locked/queued when lock held but no active job', async () => {
test/deploy-lock.test.ts:337:        jobs: [expect.objectContaining({ name: 'locked-status-app', phase: 'queued' })],
test/deploy-lock.test.ts:341:    it('wait=true transitions through queued to done when deploy event fires', async () => {
test/deploy-lock.test.ts:353:      const queuedResult = (await tool.execute(
test/deploy-lock.test.ts:358:      expect(queuedResult).toMatchObject({
test/deploy-lock.test.ts:362:        jobs: [expect.objectContaining({ name: 'evented-status-app', phase: 'queued' })],
test/deploy-lock.test.ts:367:      db.releaseDeployLock('p1');
src/mcp/prompts.ts:53:4. **Monitor** — \`get_deploy_status\` to poll build progress. \`get_build_log\` for raw output if it fails.
src/mcp/composite-tools.ts:32:  'get_deploy_status',
src/mcp/server.ts:72:IMPORTANT: Docker may run on a remote host, not the MCP client machine. Do NOT use docker CLI, docker compose, or curl localhost to interact with containers. Use OpenLander tools (get_logs, get_deploy_status, restart_project, etc.) instead. Tool responses include docker_host ("local" or "remote") when relevant — check this to determine if local commands would work.
src/mcp/server.ts:79:- execute_deploy_plan — Execute a deployment plan. Returns immediately; poll with get_deploy_status.
src/mcp/server.ts:83:- get_deploy_status — Poll build progress. Shows phase (queued/cloning/building/starting/done/failed) and elapsed time. Call periodically to track progress. Avoid wait=true as it blocks the agent.
src/mcp/server.ts:152:5. Monitor: get_deploy_status until done or failed.
src/mcp/server.ts:187:5. get_deploy_status({ project_name: "..." }) — poll until done
src/mcp/server.ts:225:3. get_deploy_status({ project_name: "my-nginx" }) — poll until done
src/mcp/server.ts:280:- Deploys are non-blocking — execute_deploy_plan returns immediately. ALWAYS poll get_deploy_status afterward.
src/mcp/server.ts:281:- list_projects and get_deploy_status return a urls array with LAN and VPN access URLs (type: "lan" or "vpn"). Use these to tell the user how to access from other devices on the network.
src/mcp/server.ts:296:Key actions: deploy, create_deploy_plan, execute_deploy_plan, get_deploy_status, rollback_project, get_build_log
src/mcp/server.ts:321:2. openlander_deploy({ action: "get_deploy_status", params: { project_name: "..." } })  ← poll until done
src/mcp/server.ts:433:    const sessionId = c.req.header('mcp-session-id');
src/mcp/server.ts:435:    if (sessionId) {
src/mcp/server.ts:436:      const session = sessions.get(sessionId);
src/mcp/server.ts:448:      sessionIdGenerator: () => crypto.randomUUID(),
src/mcp/server.ts:461:        log.info({ sessionId: sid }, 'MCP HTTP session created');
src/mcp/server.ts:474:              log.info({ sessionId: sid }, 'MCP HTTP session TTL expired, removed from map');
src/mcp/server.ts:479:          log.info({ sessionId: sid }, 'MCP HTTP session closed, TTL cleanup scheduled');
src/mcp/server.ts:513:    sseSessions.set(transport.sessionId, {
src/mcp/server.ts:520:      sseSessions.delete(transport.sessionId);
src/mcp/server.ts:521:      log.info({ sessionId: transport.sessionId }, 'MCP SSE session closed');
src/mcp/server.ts:527:    log.info({ sessionId: transport.sessionId }, 'MCP SSE session created');
src/mcp/server.ts:549:    const sessionId = c.req.query('sessionId');
src/mcp/server.ts:550:    if (!sessionId) {
src/mcp/server.ts:554:          error: { code: -32000, message: 'Missing sessionId query parameter' },
src/mcp/server.ts:561:    const session = sseSessions.get(sessionId);
test/pipeline/deploy/rollback.test.ts:208:  it('persists containerPort to project and environment on rollback', async () => {
test/pipeline/deploy/rollback.test.ts:237:    expect(project?.container_port).toBe(8080);
test/pipeline/deploy/rollback.test.ts:240:    expect(environment?.container_port).toBe(8080);
src/web/api/deploy-stream-routes.ts:78:          containerPort: body.port,
src/web/api/deploy-stream-routes.ts:151:          containerPort: body.port,
src/web/api/deploy-stream-routes.ts:212:        containerPort: body.port,
src/web/api/routes.ts:191:    ctx.db.releaseDeployLock(p.projectId);
src/web/api/routes.ts:194:    ctx.db.releaseDeployLock(p.projectId);
src/web/api/routes.ts:197:    ctx.db.releaseDeployLock(p.projectId);
src/web/api/routes.ts:200:    ctx.db.releaseDeployLock(p.projectId);
src/web/api/routes.ts:440:      const internalPort = project.container_port ?? project.assigned_port;
test/pipeline/deploy/run-step.test.ts:58:        containerPort: 12001,
test/pipeline/deploy/run-step.test.ts:71:  it('uses custom containerPort when provided', async () => {
test/pipeline/deploy/run-step.test.ts:82:      containerPort: 8080,
test/pipeline/deploy/run-step.test.ts:87:        containerPort: 8080,
src/web/api/project-routes.ts:100:    containerPort: project.container_port ?? null,
src/web/api/project-routes.ts:345:        containerPort?: unknown;
src/web/api/project-routes.ts:348:        container_port?: unknown;
src/web/api/project-routes.ts:354:          containerPort?: unknown;
src/web/api/project-routes.ts:357:          container_port?: unknown;
src/web/api/project-routes.ts:363:    const containerPortRaw = body.containerPort ?? body.container_port;
src/web/api/project-routes.ts:384:    const containerPort =
src/web/api/project-routes.ts:385:      containerPortRaw === undefined || containerPortRaw === null
src/web/api/project-routes.ts:387:        : typeof containerPortRaw === 'number' && Number.isInteger(containerPortRaw)
src/web/api/project-routes.ts:388:          ? containerPortRaw
src/web/api/project-routes.ts:389:          : typeof containerPortRaw === 'string'
src/web/api/project-routes.ts:390:            ? Number.parseInt(containerPortRaw, 10)
src/web/api/project-routes.ts:393:    if (containerPort !== undefined && !Number.isFinite(containerPort)) {
src/web/api/project-routes.ts:395:        { error: 'INVALID_FIELD', message: 'containerPort must be a valid integer' },
src/web/api/project-routes.ts:400:    if (containerPort !== undefined && (containerPort < 1 || containerPort > 65535)) {
src/web/api/project-routes.ts:402:        { error: 'INVALID_FIELD', message: 'containerPort must be between 1 and 65535' },
src/web/api/project-routes.ts:410:      containerPort,
src/web/api/chat-routes.ts:26:    let sessionId = typeof body.session_id === 'string' ? body.session_id.trim() : '';
src/web/api/chat-routes.ts:34:    if (!sessionId) {
src/web/api/chat-routes.ts:35:      sessionId = projectId ? `project:${projectId}` : `web-agent-${Date.now().toString(36)}`;
src/web/api/chat-routes.ts:40:    if (activeStreams.get(sessionId)) {
src/web/api/chat-routes.ts:44:    const agent = ctx.agentPool.getOrCreate(sessionId);
src/web/api/chat-routes.ts:45:    activeStreams.set(sessionId, true);
src/web/api/chat-routes.ts:62:            sessionId,
src/web/api/chat-routes.ts:69:          activeStreams.delete(sessionId);
src/web/api/chat-routes.ts:70:          ctx.agentPool?.release(sessionId);
src/web/api/chat-routes.ts:75:        activeStreams.delete(sessionId);
src/web/api/chat-routes.ts:76:        ctx.agentPool?.release(sessionId);
src/web/api/deploy-failure-handler.ts:34:  containerPort?: number;
src/web/api/deploy-failure-handler.ts:61:    containerPort: deps.containerPort,
src/web/api/ai-usage-routes.ts:119:    sessionId: log.session_id,
src/web/api/domain-routes.ts:136:  const sessionId = `domain-analysis-${projectId}-${Date.now().toString(36)}`;
src/web/api/domain-routes.ts:149:        'Use existing tools first (scan_project, list_env_vars, get_deploy_status).',
src/web/api/domain-routes.ts:160:      sessionId,
src/llm/decision.ts:17:  'get_deploy_status',
src/llm/prompts.ts:171:| Deploy a repo                 | create_deploy_plan, execute_deploy_plan | Create plan first, then execute. Check get_deploy_status. |
src/llm/prompts.ts:192:| Check deploy progress          | get_deploy_status    | ALWAYS call after create_deploy_plan/execute_deploy_plan.   |
src/llm/prompts.ts:217:5. On confirmation -> create_deploy_plan, then execute_deploy_plan, then get_deploy_status
src/llm/prompts.ts:224:5. On confirmation -> orchestrate_deploy, then get_deploy_status
src/llm/prompts.ts:250:3. Call get_deploy_status to check progress
src/llm/prompts.ts:260:3. Call get_deploy_status → check if build is done
src/llm/prompts.ts:306:2. Call execute_deploy_plan → wait for completion via get_deploy_status
src/llm/prompts.ts:317:2. Use get_deploy_status to monitor progress
src/llm/prompts.ts:453:   - Do NOT rely on get_deploy_status for build logs — it only shows active jobs
src/llm/prompts.ts:470:     - Inspect current failure evidence first: get_deploy_status for latest state, debug_build_error for build context, get_logs for runtime crashes
src/llm/prompts.ts:479:7. Available tools for recovery: get_deploy_status, debug_build_error, ask_user_question, set_env_vars, create_deploy_plan, execute_deploy_plan, restart_project, get_logs, get_system_stats
test/llm/agent-pool.test.ts:137:        sessionId: 'old-session',
test/llm/agent-pool.test.ts:164:    expect(lock?.sessionId).toBe('session-A');
test/llm/agent-pool.test.ts:174:        sessionId: 'old-session',
test/llm/agent-pool.test.ts:212:      sessionId: 'session-A',
src/llm/transparency.ts:121:    sessionId?: string;
src/llm/transparency.ts:143:    session_id: params.sessionId ?? null,
test/tool-registry.test.ts:35:  'get_deploy_status',
src/llm/agent.ts:87:  async chat(userMessage: string, sessionId?: string): Promise<AgentResponse> {
src/llm/agent.ts:89:      const resolvedSessionId = this.resolveSessionId(sessionId);
src/llm/agent.ts:132:        sessionId: resolvedSessionId,
src/llm/agent.ts:152:  async switchSession(sessionId: string): Promise<void> {
src/llm/agent.ts:153:    if (sessionId === this.currentSessionId) {
src/llm/agent.ts:158:    this.currentSessionId = sessionId;
src/llm/agent.ts:175:    sessionId?: string,
src/llm/agent.ts:180:      const resolvedSessionId = this.resolveSessionId(sessionId);
src/llm/agent.ts:192:          projectId: sessionId ?? 'web_agent',
src/llm/agent.ts:194:          triggerSessionId: sessionId,
src/llm/agent.ts:198:      await onEvent({ type: 'session', sessionId: resolvedSessionId, actionRunId });
src/llm/agent.ts:345:          sessionId: resolvedSessionId,
src/llm/agent.ts:376:        sessionId: resolvedSessionId,
src/llm/agent.ts:479:  private resolveSessionId(sessionId?: string): string {
src/llm/agent.ts:480:    return sessionId ?? DEFAULT_CHANNEL_SESSION_ID;
src/llm/agent-pool.ts:34:  sessionId: string;
src/llm/agent-pool.ts:45:  private projectLocks = new Map<string, { sessionId: string; startedAt: number }>();
src/llm/agent-pool.ts:56:  getOrCreate(sessionId: string): Agent {
src/llm/agent-pool.ts:58:    const existing = this.pool.get(sessionId);
src/llm/agent-pool.ts:62:      this.clearIdleTimer(sessionId);
src/llm/agent-pool.ts:74:    this.pool.set(sessionId, {
src/llm/agent-pool.ts:76:      sessionId,
src/llm/agent-pool.ts:84:  release(sessionId: string): void {
src/llm/agent-pool.ts:85:    const entry = this.pool.get(sessionId);
src/llm/agent-pool.ts:92:    this.startIdleTimer(sessionId);
src/llm/agent-pool.ts:96:    for (const sessionId of this.idleTimers.keys()) {
src/llm/agent-pool.ts:97:      this.clearIdleTimer(sessionId);
src/llm/agent-pool.ts:199:    this.clearIdleTimer(oldestIdleEntry.sessionId);
src/llm/agent-pool.ts:200:    this.pool.delete(oldestIdleEntry.sessionId);
src/llm/agent-pool.ts:203:  private startIdleTimer(sessionId: string): void {
src/llm/agent-pool.ts:204:    this.clearIdleTimer(sessionId);
src/llm/agent-pool.ts:206:      const entry = this.pool.get(sessionId);
src/llm/agent-pool.ts:211:      this.pool.delete(sessionId);
src/llm/agent-pool.ts:212:      this.idleTimers.delete(sessionId);
src/llm/agent-pool.ts:214:    this.idleTimers.set(sessionId, timer);
src/llm/agent-pool.ts:217:  private clearIdleTimer(sessionId: string): void {
src/llm/agent-pool.ts:218:    const timer = this.idleTimers.get(sessionId);
src/llm/agent-pool.ts:221:      this.idleTimers.delete(sessionId);
src/llm/agent-pool.ts:230:  acquireProjectLock(projectId: string, sessionId: string): boolean {
src/llm/agent-pool.ts:239:    this.projectLocks.set(projectId, { sessionId, startedAt: Date.now() });
src/llm/agent-pool.ts:245:   * Only releases if the sessionId matches the current lock holder.
src/llm/agent-pool.ts:247:  releaseProjectLock(projectId: string, sessionId: string): void {
src/llm/agent-pool.ts:249:    if (existing && existing.sessionId === sessionId) {
src/llm/agent-pool.ts:258:  getProjectLock(projectId: string): { sessionId: string; startedAt: number } | null {
src/llm/agent-pool.ts:274:    sessionId: string,
src/llm/agent-pool.ts:277:  ): Promise<T | { error: 'project_busy'; sessionId: string; message: string }> {
src/llm/agent-pool.ts:278:    if (!this.acquireProjectLock(projectId, sessionId)) {
src/llm/agent-pool.ts:282:        sessionId: lock?.sessionId ?? 'unknown',
src/llm/agent-pool.ts:289:      this.releaseProjectLock(projectId, sessionId);
src/db/types.ts:25:  container_port: number | null;
src/db/types.ts:49:  container_port: number | null;
src/db/schema.drizzle.ts:41:    container_port: integer('container_port'),
src/db/schema.drizzle.ts:85:    container_port: integer('container_port'),
src/db/migration.ts:72:  if (!colNames.has('container_port')) {
src/db/migration.ts:73:    sqlite.exec('ALTER TABLE projects ADD COLUMN container_port INTEGER DEFAULT NULL');
src/db/migration.ts:91:    container_port INTEGER,
src/db/migration.ts:103:  if (!environmentColNames.has('container_port')) {
src/db/migration.ts:104:    sqlite.exec('ALTER TABLE environments ADD COLUMN container_port INTEGER');
src/db/migration.ts:105:    // Backfill container_port from projects table for existing environments
src/db/migration.ts:107:      'UPDATE environments SET container_port = (SELECT container_port FROM projects WHERE id = environments.project_id) WHERE container_port IS NULL',
src/db/index.ts:136:  acquireDeployLock(projectId: string, sessionId: string) { return this.projectRepo.acquireDeployLock(projectId, sessionId); }
src/db/index.ts:137:  releaseDeployLock(projectId: string, sessionId?: string) {
src/db/index.ts:138:    return this.projectRepo.releaseDeployLock(projectId, sessionId);
src/db/schema.ts:27:   container_port INTEGER DEFAULT NULL,
test/blue-green-port-conflict.test.ts:204:  it('persists containerPort to environment after blue-green promotion', async () => {
test/blue-green-port-conflict.test.ts:222:    expect(environment?.container_port).toBe(3000);
test/blue-green-port-conflict.test.ts:225:    expect(project?.container_port).toBe(3000);
src/db/repos/environment.repo.ts:72:      containerPort: number | null;
src/db/repos/environment.repo.ts:98:    if (updates.containerPort !== undefined) {
src/db/repos/environment.repo.ts:99:      setValues.container_port = updates.containerPort;
src/db/repos/project.repo.ts:28:    containerPort?: number;
src/db/repos/project.repo.ts:45:          container_port: project.containerPort ?? null,
src/db/repos/project.repo.ts:110:      containerPort: number | null;
src/db/repos/project.repo.ts:166:    if (updates.containerPort !== undefined) {
src/db/repos/project.repo.ts:167:      setValues.container_port = updates.containerPort;
src/db/repos/project.repo.ts:299:  acquireDeployLock(projectId: string, sessionId: string): boolean {
src/db/repos/project.repo.ts:304:        deploy_lock_session: sessionId,
src/db/repos/project.repo.ts:311:          or(isNull(projects.deploy_lock_session), eq(projects.deploy_lock_session, sessionId)),
src/db/repos/project.repo.ts:321:  releaseDeployLock(projectId: string, sessionId?: string): boolean {
src/db/repos/project.repo.ts:322:    if (sessionId !== undefined) {
src/db/repos/project.repo.ts:330:        .where(and(eq(projects.id, projectId), eq(projects.deploy_lock_session, sessionId)))
src/db/repos/project.repo.ts:339:          { projectId, sessionId },
src/db/repos/project.repo.ts:340:          '[DeployLock] releaseDeployLock session mismatch or lock missing',
test/start-deploy.test.ts:104:      expect(job!.phase).toBe('queued');
test/start-deploy.test.ts:220:      expect(job!.phase).toBe('queued');
test/tools/registry-critical-tools.test.ts:119:  it('get_deploy_status formats single project status for agent and mcp targets', async () => {
test/tools/registry-critical-tools.test.ts:121:    const tool = getTool(ctx, 'get_deploy_status');
test/tools/deploy-lock-mcp-tools.test.ts:52:      next_steps: ['Wait 30 seconds and try again', 'Check deploy status with get_deploy_status'],
test/tools/project-ops-restart.test.ts:70:        'Restart initiated (no_cache). Full rebuild may take 3-5+ minutes. Poll get_deploy_status to track progress.',

exec
/bin/bash -lc 'git show --stat --oneline --no-renames b30a969 71331bd 66ceceb c071be9 d31f708' in /home/lee/OpenLander
 succeeded in 0ms:
b30a969 fix(tools): add DB poll and lock visibility to wait=true status path
 src/tools/defs/deploy.ts |  86 +++++++++++++++++++++++++++++++--
 test/deploy-lock.test.ts | 120 +++++++++++++++++++++++++++++++++++++++++++++++
 2 files changed, 201 insertions(+), 5 deletions(-)
71331bd fix(db): session-scoped releaseDeployLock with caller updates
 src/db/index.ts                    |  4 ++-
 src/db/repos/project.repo.ts       | 33 +++++++++++++++++-
 src/pipeline/deploy-core.ts        |  4 +--
 src/pipeline/deploy-plan/engine.ts |  2 +-
 test/deploy-lock.test.ts           | 69 ++++++++++++++++++++++++++++++++++++++
 5 files changed, 107 insertions(+), 5 deletions(-)
66ceceb fix(db): add container_port to environments table with migration
 src/db/migration.ts              | 10 ++++++++++
 src/db/repos/environment.repo.ts |  4 ++++
 src/db/schema.drizzle.ts         |  1 +
 src/db/types.ts                  |  1 +
 test/db.test.ts                  | 33 +++++++++++++++++++++++++++++++++
 5 files changed, 49 insertions(+)
c071be9 fix(db): add container_port to environments with rollback persistence
 src/pipeline/deploy-core.ts           |  1 +
 src/pipeline/deploy/rollback.ts       |  2 ++
 test/blue-green-port-conflict.test.ts | 24 ++++++++++++++++++++++++
 test/pipeline/deploy/rollback.test.ts | 35 +++++++++++++++++++++++++++++++++++
 4 files changed, 62 insertions(+)
d31f708 test(tools): fix mock regressions from deploy-lock and status hardening
 .omc/state/hud-state.json                  |  2 +-
 .omc/state/hud-stdin-cache.json            | 32 +++++++--------
 logs/mcp-logger-20260410-144303.log        | 63 ++++++++++++++++++++++++++++++
 logs/mcp-logger-20260410-160406.log        | 63 ++++++++++++++++++++++++++++++
 logs/mcp-logger-20260410-161030.log        | 63 ++++++++++++++++++++++++++++++
 src/db/migration.ts                        |  4 ++
 test/tools/deploy-lock-mcp-tools.test.ts   |  2 +-
 test/tools/registry-critical-tools.test.ts |  1 +
 8 files changed, 212 insertions(+), 18 deletions(-)

codex
변경 지점은 확인했습니다. 이제 각 버그 축별로 현재 구현과 테스트를 읽어서, 커밋 의도와 실제 동작이 어긋나는 부분이 있는지 확인하겠습니다.
exec
/bin/bash -lc 'git show b30a969 -- src/tools/defs/deploy.ts test/deploy-lock.test.ts' in /home/lee/OpenLander
 succeeded in 0ms:
commit b30a96915af5882e42ec7f027a37e8ccdfe3c938
Author: lehdqlsl <lehdqlsl@naver.com>
Date:   Fri Apr 10 16:11:18 2026 +0900

    fix(tools): add DB poll and lock visibility to wait=true status path

    Ultraworked with [Sisyphus](https://github.com/code-yeongyu/oh-my-openagent)

    Co-authored-by: Sisyphus <clio-agent@sisyphuslabs.ai>

diff --git a/src/tools/defs/deploy.ts b/src/tools/defs/deploy.ts
index 772703c..f4ab6c1 100644
--- a/src/tools/defs/deploy.ts
+++ b/src/tools/defs/deploy.ts
@@ -348,6 +348,24 @@ export const deployToolDefs: ToolDef[] = [
           };
         };

+        const buildLockedQueuedResult = (
+          currentLockInfo: { session: string; lockedAt: string },
+          timedOut = false,
+        ) => ({
+          active: 1,
+          locked: true,
+          lock_session: currentLockInfo.session,
+          jobs: [
+            formatJob({
+              projectId: project.id,
+              projectName: project.name,
+              phase: 'queued',
+              startedAt: new Date(currentLockInfo.lockedAt),
+            }),
+          ],
+          ...(timedOut ? { timeout: true } : {}),
+        });
+
         let status = appCtx.jobManager.getStatus(project.id);
         const lockInfo = appCtx.db.getDeployLockInfo(project.id);

@@ -375,9 +393,41 @@ export const deployToolDefs: ToolDef[] = [
           return buildProjectResult(status);
         }

+        if (lockInfo && !status) {
+          return buildLockedQueuedResult(lockInfo);
+        }
+
         return await new Promise((resolve) => {
           let settled = false;

+          const cleanup = (): void => {
+            clearTimeout(timer);
+            clearInterval(dbPollInterval);
+            unsubSuccess();
+            unsubFailed();
+          };
+
+          const resolveFromDeployLog = (lastLogCreatedAt: string): void => {
+            if (settled) return;
+            settled = true;
+            cleanup();
+
+            const dbProject = appCtx.db.getProjectByName(projectName);
+            resolve({
+              active: 0,
+              jobs: [
+                formatJob({
+                  projectId: project.id,
+                  projectName: project.name,
+                  phase: 'done',
+                  startedAt: new Date(lastLogCreatedAt),
+                  completedAt: new Date(lastLogCreatedAt),
+                }),
+              ],
+              ...(dbProject ? { health: dbProject.status } : {}),
+            });
+          };
+
           const matchesProject = (payload: {
             projectId: string;
             parentProjectId?: string;
@@ -386,9 +436,7 @@ export const deployToolDefs: ToolDef[] = [
           const resolveWithCurrent = (timedOut: boolean): void => {
             if (settled) return;
             settled = true;
-            clearTimeout(timer);
-            unsubSuccess();
-            unsubFailed();
+            cleanup();

             const current = appCtx.jobManager.getStatus(project.id);
             if (current) {
@@ -400,7 +448,6 @@ export const deployToolDefs: ToolDef[] = [
                 lastLog.status === 'success' &&
                 (current.phase === 'failed' || current.phase === 'done')
               ) {
-                const dbProject = appCtx.db.getProjectByName(projectName);
                 resolve({
                   active: 0,
                   jobs: [
@@ -412,7 +459,9 @@ export const deployToolDefs: ToolDef[] = [
                       completedAt: new Date(lastLog.created_at),
                     }),
                   ],
-                  ...(dbProject ? { health: dbProject.status } : {}),
+                  ...(appCtx.db.getProjectByName(projectName)
+                    ? { health: appCtx.db.getProjectByName(projectName)?.status }
+                    : {}),
                   ...(timedOut ? { timeout: true } : {}),
                 });
                 return;
@@ -424,6 +473,12 @@ export const deployToolDefs: ToolDef[] = [
               return;
             }

+            const currentLockInfo = appCtx.db.getDeployLockInfo(project.id);
+            if (currentLockInfo) {
+              resolve(buildLockedQueuedResult(currentLockInfo, timedOut));
+              return;
+            }
+
             const dbProject = appCtx.db.getProjectByName(projectName);
             resolve({
               project: projectName,
@@ -448,6 +503,27 @@ export const deployToolDefs: ToolDef[] = [
             Math.max(1, timeoutSec) * 1000,
           );

+          const dbPollInterval = setInterval(() => {
+            if (settled) {
+              clearInterval(dbPollInterval);
+              return;
+            }
+
+            const lastLog = appCtx.db.getLastDeployLog(project.id);
+            if (!lastLog || lastLog.status !== 'success') {
+              return;
+            }
+
+            const currentJob = appCtx.jobManager.getStatus(project.id);
+            const logIsNewer =
+              !currentJob ||
+              new Date(lastLog.created_at).getTime() > currentJob.startedAt.getTime();
+
+            if (logIsNewer) {
+              resolveFromDeployLog(lastLog.created_at);
+            }
+          }, 5000);
+
           status = appCtx.jobManager.getStatus(project.id);
           if (status && (status.phase === 'done' || status.phase === 'failed')) {
             resolveWithCurrent(false);
diff --git a/test/deploy-lock.test.ts b/test/deploy-lock.test.ts
index 616548e..f38412d 100644
--- a/test/deploy-lock.test.ts
+++ b/test/deploy-lock.test.ts
@@ -3,12 +3,16 @@ import { join } from 'node:path';
 import { mkdtempSync, rmSync } from 'node:fs';
 import { tmpdir } from 'node:os';

+import type { AppContext } from '../src/app.js';
 import { DeployPipeline } from '../src/pipeline/deploy.js';
+import { JobManager } from '../src/pipeline/job-manager.js';
 import { Database } from '../src/db/index.js';
 import { DeployLockedError } from '../src/errors.js';
+import { eventBus } from '../src/events/index.js';
 import type { OpenLanderConfig } from '../src/config/index.js';
 import type { Docker } from '../src/pipeline/docker.js';
 import { clearPortScanCache } from '../src/pipeline/port.js';
+import { deployToolDefs } from '../src/tools/defs/deploy.js';

 type EnvLike = {
   getMergedForDeploy: (projectId: string, environmentId?: string) => Record<string, string>;
@@ -33,6 +37,19 @@ function createMockDocker(): Docker {
   } as unknown as Docker;
 }

+function createStatusToolContext(db: Database, jobManager: JobManager): AppContext {
+  return {
+    db,
+    jobManager,
+  } as unknown as AppContext;
+}
+
+function getDeployStatusTool() {
+  const tool = deployToolDefs.find((entry) => entry.name === 'get_deploy_status');
+  expect(tool).toBeDefined();
+  return tool!;
+}
+
 describe('BUG-010: Deploy lock prevents concurrent deploys', () => {
   let tmpDir: string;
   let db: Database;
@@ -193,4 +210,107 @@ describe('BUG-010: Deploy lock prevents concurrent deploys', () => {
       expect(result.error).toContain('Project not found');
     });
   });
+
+  describe('get_deploy_status wait=true hardening', () => {
+    it('wait=true resolves from deploy_logs when job not in JobManager', async () => {
+      vi.useFakeTimers();
+
+      db.createProject({
+        id: 'p1',
+        name: 'status-app',
+        repoUrl: 'https://github.com/test/status-app',
+      });
+      db.createDeployLog({
+        id: 'log-1',
+        projectId: 'p1',
+        status: 'success',
+        trigger: 'api',
+      });
+
+      const ctx = createStatusToolContext(db, new JobManager());
+      const tool = getDeployStatusTool();
+
+      const resultPromise = tool.execute(
+        { project_name: 'status-app', wait: true, timeout: 6 },
+        { appCtx: ctx, target: 'agent' },
+      ) as Promise<Record<string, unknown>>;
+
+      await vi.advanceTimersByTimeAsync(5000);
+      const result = await resultPromise;
+
+      expect(result).toMatchObject({
+        active: 0,
+        jobs: [expect.objectContaining({ name: 'status-app', phase: 'done' })],
+      });
+      expect(result).not.toHaveProperty('timeout');
+    });
+
+    it('wait=true shows locked/queued when lock held but no active job', async () => {
+      db.createProject({
+        id: 'p1',
+        name: 'locked-status-app',
+        repoUrl: 'https://github.com/test/locked-status-app',
+      });
+      db.acquireDeployLock('p1', 'test-session');
+
+      const ctx = createStatusToolContext(db, new JobManager());
+      const tool = getDeployStatusTool();
+
+      const result = (await tool.execute(
+        { project_name: 'locked-status-app', wait: true, timeout: 30 },
+        { appCtx: ctx, target: 'agent' },
+      )) as Record<string, unknown>;
+
+      expect(result).toMatchObject({
+        active: 1,
+        locked: true,
+        lock_session: 'test-session',
+        jobs: [expect.objectContaining({ name: 'locked-status-app', phase: 'queued' })],
+      });
+    });
+
+    it('wait=true transitions through queued to done when deploy event fires', async () => {
+      db.createProject({
+        id: 'p1',
+        name: 'evented-status-app',
+        repoUrl: 'https://github.com/test/evented-status-app',
+      });
+      db.acquireDeployLock('p1', 'test-session');
+
+      const jobManager = new JobManager();
+      const ctx = createStatusToolContext(db, jobManager);
+      const tool = getDeployStatusTool();
+
+      const queuedResult = (await tool.execute(
+        { project_name: 'evented-status-app', wait: true, timeout: 30 },
+        { appCtx: ctx, target: 'agent' },
+      )) as Record<string, unknown>;
+
+      expect(queuedResult).toMatchObject({
+        active: 1,
+        locked: true,
+        lock_session: 'test-session',
+        jobs: [expect.objectContaining({ name: 'evented-status-app', phase: 'queued' })],
+      });
+
+      jobManager.trackJob('p1', 'evented-status-app');
+      jobManager.updatePhase('p1', 'done');
+      db.releaseDeployLock('p1');
+      await eventBus.emit('deploy:success', {
+        projectId: 'p1',
+        url: 'https://example.test',
+        totalDurationMs: 1000,
+      });
+
+      const doneResult = (await tool.execute(
+        { project_name: 'evented-status-app', wait: true, timeout: 30 },
+        { appCtx: ctx, target: 'agent' },
+      )) as Record<string, unknown>;
+
+      expect(doneResult).toMatchObject({
+        active: 0,
+        jobs: [expect.objectContaining({ name: 'evented-status-app', phase: 'done' })],
+      });
+    });
+  });
 });

exec
/bin/bash -lc 'git show 71331bd -- src/db/repos/project.repo.ts src/db/index.ts src/pipeline/deploy-core.ts src/pipeline/deploy-plan/engine.ts test/deploy-lock.test.ts' in /home/lee/OpenLander
 succeeded in 0ms:
commit 71331bdcb2d24b2b12c35801bcb5edb62febf59d
Author: lehdqlsl <lehdqlsl@naver.com>
Date:   Fri Apr 10 16:21:26 2026 +0900

    fix(db): session-scoped releaseDeployLock with caller updates

diff --git a/src/db/index.ts b/src/db/index.ts
index 927b32c..eac2690 100644
--- a/src/db/index.ts
+++ b/src/db/index.ts
@@ -134,7 +134,9 @@ export class Database implements AuthDatabase {
   getPreviewProjects(parentProjectId: string) { return this.projectRepo.getPreviewProjects(parentProjectId); }
   isParentProject(id: string) { return this.projectRepo.isParentProject(id); }
   acquireDeployLock(projectId: string, sessionId: string) { return this.projectRepo.acquireDeployLock(projectId, sessionId); }
-  releaseDeployLock(projectId: string) { this.projectRepo.releaseDeployLock(projectId); }
+  releaseDeployLock(projectId: string, sessionId?: string) {
+    return this.projectRepo.releaseDeployLock(projectId, sessionId);
+  }
   getDeployLockInfo(projectId: string) { return this.projectRepo.getDeployLockInfo(projectId); }
   cleanExpiredDeployLocks(timeoutMinutes = 10) { return this.projectRepo.cleanExpiredDeployLocks(timeoutMinutes); }
   createEnvironment(environment: Parameters<EnvironmentRepo['createEnvironment']>[0]) { return this.environmentRepo.createEnvironment(environment); }
diff --git a/src/db/repos/project.repo.ts b/src/db/repos/project.repo.ts
index a55f83a..0fb2020 100644
--- a/src/db/repos/project.repo.ts
+++ b/src/db/repos/project.repo.ts
@@ -1,9 +1,12 @@
 import { and, asc, count, desc, eq, isNotNull, isNull, or, sql } from 'drizzle-orm';
 import { OpenLanderError, ProjectAlreadyExistsError, ProjectNotFoundError } from '../../errors.js';
+import { createModuleLogger } from '../../lib/logger.js';
 import type { DrizzleClient, SqliteDatabase } from '../drizzle.js';
 import { projects } from '../schema.drizzle.js';
 import type { PendingFixRow, ProjectRow } from '../types.js';

+const log = createModuleLogger('project-repo');
+
 export class ProjectRepo {
   constructor(
     private readonly db: DrizzleClient,
@@ -315,12 +318,40 @@ export class ProjectRepo {
     return (row?.changes ?? 0) > 0;
   }

-  releaseDeployLock(projectId: string): void {
+  releaseDeployLock(projectId: string, sessionId?: string): boolean {
+    if (sessionId !== undefined) {
+      this.db
+        .update(projects)
+        .set({
+          deploy_lock_session: null,
+          deploy_lock_at: null,
+          updated_at: sql`CURRENT_TIMESTAMP`,
+        })
+        .where(and(eq(projects.id, projectId), eq(projects.deploy_lock_session, sessionId)))
+        .run();
+
+      const row = this.sqlite.prepare('SELECT changes() as changes').get() as {
+        changes: number;
+      } | null;
+
+      if ((row?.changes ?? 0) === 0) {
+        log.warn(
+          { projectId, sessionId },
+          '[DeployLock] releaseDeployLock session mismatch or lock missing',
+        );
+        return false;
+      }
+
+      return true;
+    }
+
     this.db
       .update(projects)
       .set({ deploy_lock_session: null, deploy_lock_at: null, updated_at: sql`CURRENT_TIMESTAMP` })
       .where(eq(projects.id, projectId))
       .run();
+
+    return true;
   }

   getDeployLockInfo(projectId: string): { session: string; lockedAt: string } | null {
diff --git a/src/pipeline/deploy-core.ts b/src/pipeline/deploy-core.ts
index d2ba4f9..7c406b4 100644
--- a/src/pipeline/deploy-core.ts
+++ b/src/pipeline/deploy-core.ts
@@ -1389,7 +1389,7 @@ export class DeployPipeline {

       return await this.deploy(config);
     } finally {
-      this.db.releaseDeployLock(projectId);
+      this.db.releaseDeployLock(projectId, lockSession);
     }
   }

@@ -1799,7 +1799,7 @@ export class DeployPipeline {
     try {
       return await this.rollbackExecutor.rollbackToImage(projectId, environmentId);
     } finally {
-      this.db.releaseDeployLock(projectId);
+      this.db.releaseDeployLock(projectId, lockSession);
     }
   }

diff --git a/src/pipeline/deploy-plan/engine.ts b/src/pipeline/deploy-plan/engine.ts
index fc84d22..f39d078 100644
--- a/src/pipeline/deploy-plan/engine.ts
+++ b/src/pipeline/deploy-plan/engine.ts
@@ -787,7 +787,7 @@ export class PlanEngine {
       }

       try {
-        this.db.releaseDeployLock(lockProjectId);
+        this.db.releaseDeployLock(lockProjectId, lockSessionId ?? `plan-${planId}`);
       } catch (error) {
         log.warn({ planId, projectId: lockProjectId, error }, 'Failed to release deploy lock');
       } finally {
diff --git a/test/deploy-lock.test.ts b/test/deploy-lock.test.ts
index f38412d..5b24b4f 100644
--- a/test/deploy-lock.test.ts
+++ b/test/deploy-lock.test.ts
@@ -120,6 +120,75 @@ describe('BUG-010: Deploy lock prevents concurrent deploys', () => {
       expect(db.acquireDeployLock('p1', 'session-1')).toBe(true);
       expect(db.acquireDeployLock('p2', 'session-2')).toBe(true);
     });
+
+    it('releaseDeployLock with correct session succeeds', () => {
+      db.createProject({
+        id: 'p1',
+        name: 'session-release-app',
+        repoUrl: 'https://github.com/test/session-release-app',
+      });
+
+      expect(db.acquireDeployLock('p1', 'session-abc')).toBe(true);
+      const releaseDeployLock = Reflect.get(db, 'releaseDeployLock') as (
+        projectId: string,
+        sessionId?: string,
+      ) => boolean;
+
+      expect(releaseDeployLock.call(db, 'p1', 'session-abc')).toBe(true);
+      expect(db.getDeployLockInfo('p1')).toBeNull();
+    });
+
+    it('releaseDeployLock with wrong session returns false and keeps lock', () => {
+      db.createProject({
+        id: 'p1',
+        name: 'wrong-session-release-app',
+        repoUrl: 'https://github.com/test/wrong-session-release-app',
+      });
+
+      expect(db.acquireDeployLock('p1', 'session-abc')).toBe(true);
+      const releaseDeployLock = Reflect.get(db, 'releaseDeployLock') as (
+        projectId: string,
+        sessionId?: string,
+      ) => boolean;
+
+      expect(releaseDeployLock.call(db, 'p1', 'session-xyz')).toBe(false);
+      expect(db.getDeployLockInfo('p1')).toEqual(
+        expect.objectContaining({ session: 'session-abc' }),
+      );
+    });
+
+    it('releaseDeployLock without session releases unconditionally', () => {
+      db.createProject({
+        id: 'p1',
+        name: 'unconditional-release-app',
+        repoUrl: 'https://github.com/test/unconditional-release-app',
+      });
+
+      expect(db.acquireDeployLock('p1', 'session-abc')).toBe(true);
+
+      expect(db.releaseDeployLock('p1')).toBe(true);
+      expect(db.getDeployLockInfo('p1')).toBeNull();
+    });
+
+    it('cleanExpiredDeployLocks bypasses session check', () => {
+      db.createProject({
+        id: 'p1',
+        name: 'expired-lock-app',
+        repoUrl: 'https://github.com/test/expired-lock-app',
+      });
+
+      expect(db.acquireDeployLock('p1', 'session-abc')).toBe(true);
+
+      const sqlite = Reflect.get(db, 'sqlite') as {
+        prepare: (sql: string) => { run: (...params: Array<string | number>) => unknown };
+      };
+      sqlite
+        .prepare("UPDATE projects SET deploy_lock_at = datetime('now', '-31 minutes') WHERE id = ?")
+        .run('p1');
+
+      expect(db.cleanExpiredDeployLocks()).toBeGreaterThan(0);
+      expect(db.getDeployLockInfo('p1')).toBeNull();
+    });
   });

   describe('DeployLockedError', () => {

exec
/bin/bash -lc 'git show 66ceceb -- src/db/migration.ts src/db/repos/environment.repo.ts src/db/schema.drizzle.ts src/db/types.ts test/db.test.ts' in /home/lee/OpenLander
 succeeded in 0ms:
commit 66ceceb4f6bb0c2291c9a3788b7ec43a801b4d1c
Author: lehdqlsl <lehdqlsl@naver.com>
Date:   Fri Apr 10 16:26:50 2026 +0900

    fix(db): add container_port to environments table with migration

diff --git a/src/db/migration.ts b/src/db/migration.ts
index 00de620..ca1dc0c 100644
--- a/src/db/migration.ts
+++ b/src/db/migration.ts
@@ -88,12 +88,22 @@ export function runMigrations(sqlite: SqliteDatabase): void {
     image_tag TEXT,
     previous_image_tag TEXT,
     public_url TEXT,
+    container_port INTEGER,
     created_at TEXT DEFAULT CURRENT_TIMESTAMP,
     updated_at TEXT DEFAULT CURRENT_TIMESTAMP,
     UNIQUE(project_id, type)
   )`);
   sqlite.exec('CREATE INDEX IF NOT EXISTS idx_environments_project ON environments(project_id)');

+  const environmentColumns = sqlite.prepare("PRAGMA table_info('environments')").all() as Array<{
+    name: string;
+  }>;
+  const environmentColNames = new Set(environmentColumns.map((c) => c.name));
+
+  if (!environmentColNames.has('container_port')) {
+    sqlite.exec('ALTER TABLE environments ADD COLUMN container_port INTEGER');
+  }
+
   const envVarColumns = sqlite.prepare("PRAGMA table_info('env_vars')").all() as Array<{
     name: string;
   }>;
diff --git a/src/db/repos/environment.repo.ts b/src/db/repos/environment.repo.ts
index c822b2d..4627790 100644
--- a/src/db/repos/environment.repo.ts
+++ b/src/db/repos/environment.repo.ts
@@ -69,6 +69,7 @@ export class EnvironmentRepo {
       imageTag: string | null;
       previousImageTag: string | null;
       publicUrl: string | null;
+      containerPort: number | null;
     }>,
   ): void {
     const setValues: Partial<typeof environments.$inferInsert> = {};
@@ -94,6 +95,9 @@ export class EnvironmentRepo {
     if (updates.publicUrl !== undefined) {
       setValues.public_url = updates.publicUrl;
     }
+    if (updates.containerPort !== undefined) {
+      setValues.container_port = updates.containerPort;
+    }

     if (Object.keys(setValues).length === 0) return;

diff --git a/src/db/schema.drizzle.ts b/src/db/schema.drizzle.ts
index 36e5d9d..cc4e112 100644
--- a/src/db/schema.drizzle.ts
+++ b/src/db/schema.drizzle.ts
@@ -82,6 +82,7 @@ export const environments = sqliteTable(
     image_tag: text('image_tag'),
     previous_image_tag: text('previous_image_tag'),
     public_url: text('public_url'),
+    container_port: integer('container_port'),
     created_at: text('created_at').default(sql`CURRENT_TIMESTAMP`),
     updated_at: text('updated_at').default(sql`CURRENT_TIMESTAMP`),
   },
diff --git a/src/db/types.ts b/src/db/types.ts
index e5064f8..d59f14c 100644
--- a/src/db/types.ts
+++ b/src/db/types.ts
@@ -46,6 +46,7 @@ export interface EnvironmentRow {
   image_tag: string | null;
   previous_image_tag: string | null;
   public_url: string | null;
+  container_port: number | null;
   created_at: string;
   updated_at: string;
 }
diff --git a/test/db.test.ts b/test/db.test.ts
index b20c5fe..158fd3b 100644
--- a/test/db.test.ts
+++ b/test/db.test.ts
@@ -9,6 +9,7 @@ import { ProjectAlreadyExistsError } from '../src/errors.js';

 type LegacySqlite = {
   exec: (sql: string) => unknown;
+  prepare: (sql: string) => { all: () => unknown };
   close: () => void;
 };

@@ -432,4 +433,36 @@ describe('Database', () => {
       ).not.toThrow();
     });
   });
+
+  describe('Environments container_port', () => {
+    beforeEach(() => {
+      db.createProject({ id: 'p1', name: 'my-app', repoUrl: 'https://github.com/test/a' });
+    });
+
+    it('environments table has container_port column after migration', () => {
+      const dbPath = join(tmpDir, 'test.db');
+      const sqlite = createLegacySqlite(dbPath);
+
+      const columns = (
+        sqlite.prepare('PRAGMA table_info(environments)').all() as Array<{
+          name: string;
+        }>
+      ).map((c) => c.name);
+
+      sqlite.close();
+      expect(columns).toContain('container_port');
+    });
+
+    it('updateEnvironment persists containerPort', () => {
+      const envs = db.getEnvironmentsByProject('p1');
+      const env = envs.find((e) => e.type === 'production');
+      expect(env).toBeDefined();
+
+      db.updateEnvironment(env!.id, { containerPort: 8080 });
+
+      const updated = db.getEnvironment(env!.id);
+      expect(updated).toBeDefined();
+      expect(updated!.container_port).toBe(8080);
+    });
+  });
 });

exec
/bin/bash -lc 'git show c071be9 -- src/pipeline/deploy-core.ts src/pipeline/deploy/rollback.ts test/blue-green-port-conflict.test.ts test/pipeline/deploy/rollback.test.ts' in /home/lee/OpenLander
 succeeded in 0ms:
commit c071be9b64d3d2a4e1fdf59bcac3659367e21520
Author: lehdqlsl <lehdqlsl@naver.com>
Date:   Fri Apr 10 16:33:59 2026 +0900

    fix(db): add container_port to environments with rollback persistence

diff --git a/src/pipeline/deploy-core.ts b/src/pipeline/deploy-core.ts
index 7c406b4..a4ad1de 100644
--- a/src/pipeline/deploy-core.ts
+++ b/src/pipeline/deploy-core.ts
@@ -1575,6 +1575,7 @@ export class DeployPipeline {
           status: 'running',
           containerId: greenContainerId,
           assignedPort: newPort,
+          containerPort,
           imageTag,
           previousImageTag: prodEnv.image_tag,
         });
diff --git a/src/pipeline/deploy/rollback.ts b/src/pipeline/deploy/rollback.ts
index ab6c1be..ad447d1 100644
--- a/src/pipeline/deploy/rollback.ts
+++ b/src/pipeline/deploy/rollback.ts
@@ -116,6 +116,7 @@ export class RollbackExecutor {
       this.db.updateProject(projectId, {
         status: 'running',
         assignedPort: port,
+        containerPort,
         containerId,
         imageTag: rollbackImageTag,
         previousImageTag: currentImageTag,
@@ -128,6 +129,7 @@ export class RollbackExecutor {
           imageTag: rollbackImageTag,
           previousImageTag: currentImageTag,
           assignedPort: port,
+          containerPort,
         });
       }

diff --git a/test/blue-green-port-conflict.test.ts b/test/blue-green-port-conflict.test.ts
index 3141e4c..3519022 100644
--- a/test/blue-green-port-conflict.test.ts
+++ b/test/blue-green-port-conflict.test.ts
@@ -201,6 +201,30 @@ describe('BUG-003: blue-green promotion avoids port conflicts', () => {
     expect(dockerControls.promotedRenameMock).toHaveBeenCalledTimes(1);
   });

+  it('persists containerPort to environment after blue-green promotion', async () => {
+    db.updateEnvironment('p1-production', {
+      status: 'running',
+      containerId: 'container-blue',
+      imageTag: 'openlander/demo-app:old',
+      assignedPort: 10010,
+    });
+
+    vi.spyOn(
+      pipeline as unknown as { healthCheck: () => Promise<boolean> },
+      'healthCheck',
+    ).mockResolvedValue(true);
+
+    const result = await pipeline.redeploy('p1', { strategy: 'blue-green' });
+
+    expect(result.success).toBe(true);
+
+    const environment = db.getEnvironment('p1-production');
+    expect(environment?.container_port).toBe(3000);
+
+    const project = db.getProject('p1');
+    expect(project?.container_port).toBe(3000);
+  });
+
   it('tries to restart blue when promoted container fails and blue is not running', async () => {
     const mockDocker = createMockDocker({ blueRunning: false });
     docker = mockDocker.docker;
diff --git a/test/pipeline/deploy/rollback.test.ts b/test/pipeline/deploy/rollback.test.ts
index d6322f5..01e4e4a 100644
--- a/test/pipeline/deploy/rollback.test.ts
+++ b/test/pipeline/deploy/rollback.test.ts
@@ -205,6 +205,41 @@ describe('RollbackExecutor', () => {
     expect(docker.runContainer as ReturnType<typeof vi.fn>).not.toHaveBeenCalled();
   });

+  it('persists containerPort to project and environment on rollback', async () => {
+    db.createProject({
+      id: 'p-port',
+      name: 'port-app',
+      repoUrl: 'https://github.com/openlander/port-app',
+      branch: 'main',
+    });
+    db.updateProject('p-port', {
+      status: 'running',
+      containerId: 'container-port-old',
+      imageTag: 'openlander/port-app:v2',
+      previousImageTag: 'openlander/port-app:v1',
+      assignedPort: 10050,
+    });
+    db.updateEnvironment('p-port-production', {
+      status: 'running',
+      containerId: 'container-port-old',
+      imageTag: 'openlander/port-app:v2',
+      previousImageTag: 'openlander/port-app:v1',
+      assignedPort: 10050,
+    });
+
+    (docker.getImageExposedPort as ReturnType<typeof vi.fn>).mockResolvedValueOnce(8080);
+
+    const result = await rollbackExecutor.rollbackToImage('p-port', 'p-port-production');
+
+    expect(result.success).toBe(true);
+
+    const project = db.getProject('p-port');
+    expect(project?.container_port).toBe(8080);
+
+    const environment = db.getEnvironment('p-port-production');
+    expect(environment?.container_port).toBe(8080);
+  });
+
   it('returns an error and marks status when container start fails', async () => {
     db.createProject({
       id: 'p4',

exec
/bin/bash -lc "sed -n '330,560p' src/tools/defs/deploy.ts" in /home/lee/OpenLander
 succeeded in 0ms:
        }

        const buildProjectResult = (status?: {
          projectId: string;
          projectName: string;
          phase: string;
          startedAt: Date;
          completedAt?: Date;
          errorSummary?: string;
          buildLogTail?: string;
          buildStep?: number;
          buildStepTotal?: number;
          buildStepDesc?: string;
        }) => {
          const isActive = status && status.phase !== 'done' && status.phase !== 'failed';
          return {
            active: isActive ? 1 : 0,
            jobs: status ? [formatJob(status)] : [],
          };
        };

        const buildLockedQueuedResult = (
          currentLockInfo: { session: string; lockedAt: string },
          timedOut = false,
        ) => ({
          active: 1,
          locked: true,
          lock_session: currentLockInfo.session,
          jobs: [
            formatJob({
              projectId: project.id,
              projectName: project.name,
              phase: 'queued',
              startedAt: new Date(currentLockInfo.lockedAt),
            }),
          ],
          ...(timedOut ? { timeout: true } : {}),
        });

        let status = appCtx.jobManager.getStatus(project.id);
        const lockInfo = appCtx.db.getDeployLockInfo(project.id);

        if (!wait) {
          const result = buildProjectResult(status) as Record<string, unknown>;
          if (lockInfo) {
            result['locked'] = true;
            result['lock_session'] = lockInfo.session;
          }
          if (!status && lockInfo) {
            result['active'] = 1;
            result['jobs'] = [
              formatJob({
                projectId: project.id,
                projectName: project.name,
                phase: 'queued',
                startedAt: new Date(lockInfo.lockedAt),
              }),
            ];
          }
          return result;
        }

        if (status && (status.phase === 'done' || status.phase === 'failed')) {
          return buildProjectResult(status);
        }

        if (lockInfo && !status) {
          return buildLockedQueuedResult(lockInfo);
        }

        return await new Promise((resolve) => {
          let settled = false;

          const cleanup = (): void => {
            clearTimeout(timer);
            clearInterval(dbPollInterval);
            unsubSuccess();
            unsubFailed();
          };

          const resolveFromDeployLog = (lastLogCreatedAt: string): void => {
            if (settled) return;
            settled = true;
            cleanup();

            const dbProject = appCtx.db.getProjectByName(projectName);
            resolve({
              active: 0,
              jobs: [
                formatJob({
                  projectId: project.id,
                  projectName: project.name,
                  phase: 'done',
                  startedAt: new Date(lastLogCreatedAt),
                  completedAt: new Date(lastLogCreatedAt),
                }),
              ],
              ...(dbProject ? { health: dbProject.status } : {}),
            });
          };

          const matchesProject = (payload: {
            projectId: string;
            parentProjectId?: string;
          }): boolean => payload.projectId === project.id || payload.parentProjectId === project.id;

          const resolveWithCurrent = (timedOut: boolean): void => {
            if (settled) return;
            settled = true;
            cleanup();

            const current = appCtx.jobManager.getStatus(project.id);
            if (current) {
              const lastLog = appCtx.db.getLastDeployLog(project.id);
              const logIsNewer =
                lastLog && new Date(lastLog.created_at).getTime() > current.startedAt.getTime();
              if (
                logIsNewer &&
                lastLog.status === 'success' &&
                (current.phase === 'failed' || current.phase === 'done')
              ) {
                resolve({
                  active: 0,
                  jobs: [
                    formatJob({
                      projectId: project.id,
                      projectName: project.name,
                      phase: 'done',
                      startedAt: new Date(lastLog.created_at),
                      completedAt: new Date(lastLog.created_at),
                    }),
                  ],
                  ...(appCtx.db.getProjectByName(projectName)
                    ? { health: appCtx.db.getProjectByName(projectName)?.status }
                    : {}),
                  ...(timedOut ? { timeout: true } : {}),
                });
                return;
              }

              const payload = buildProjectResult(current) as Record<string, unknown>;
              if (timedOut) payload['timeout'] = true;
              resolve(payload);
              return;
            }

            const currentLockInfo = appCtx.db.getDeployLockInfo(project.id);
            if (currentLockInfo) {
              resolve(buildLockedQueuedResult(currentLockInfo, timedOut));
              return;
            }

            const dbProject = appCtx.db.getProjectByName(projectName);
            resolve({
              project: projectName,
              status: dbProject?.status ?? 'unknown',
              phase: 'none',
              ...(timedOut ? { timeout: true } : {}),
            });
          };

          const unsubSuccess = eventBus.on('deploy:success', (payload) => {
            if (matchesProject(payload)) resolveWithCurrent(false);
          });

          const unsubFailed = eventBus.on('deploy:failed', (payload) => {
            if (matchesProject(payload)) resolveWithCurrent(false);
          });

          const timer = setTimeout(
            () => {
              resolveWithCurrent(true);
            },
            Math.max(1, timeoutSec) * 1000,
          );

          const dbPollInterval = setInterval(() => {
            if (settled) {
              clearInterval(dbPollInterval);
              return;
            }

            const lastLog = appCtx.db.getLastDeployLog(project.id);
            if (!lastLog || lastLog.status !== 'success') {
              return;
            }

            const currentJob = appCtx.jobManager.getStatus(project.id);
            const logIsNewer =
              !currentJob ||
              new Date(lastLog.created_at).getTime() > currentJob.startedAt.getTime();

            if (logIsNewer) {
              resolveFromDeployLog(lastLog.created_at);
            }
          }, 5000);

          status = appCtx.jobManager.getStatus(project.id);
          if (status && (status.phase === 'done' || status.phase === 'failed')) {
            resolveWithCurrent(false);
          }
        });
      }

      const buildAllResult = () => {
        const allJobs = appCtx.jobManager.getStatuses();
        const recentJobs = allJobs.filter(
          (j) =>
            (j.phase !== 'done' && j.phase !== 'failed') ||
            (j.completedAt && Date.now() - j.completedAt.getTime() < 5 * 60 * 1000),
        );
        const activeCount = recentJobs.filter(
          (j) => j.phase !== 'done' && j.phase !== 'failed',
        ).length;
        return { active: activeCount, jobs: recentJobs.map(formatJob) };
      };

      if (!wait) {
        return buildAllResult();
      }

      const trackedIds = new Set(
        appCtx.jobManager
          .getStatuses()
          .filter((j) => j.phase !== 'done' && j.phase !== 'failed')
          .map((j) => j.projectId),
      );

      if (trackedIds.size === 0) {
        return buildAllResult();
      }

exec
/bin/bash -lc "sed -n '280,360p' src/db/repos/project.repo.ts" in /home/lee/OpenLander
 succeeded in 0ms:

  getPreviewProjects(parentProjectId: string): ProjectRow[] {
    return this.db
      .select()
      .from(projects)
      .where(and(eq(projects.parent_project_id, parentProjectId), eq(projects.is_preview, 1)))
      .orderBy(desc(projects.updated_at))
      .all() as ProjectRow[];
  }

  isParentProject(id: string): boolean {
    const row = this.db
      .select({ cnt: count() })
      .from(projects)
      .where(eq(projects.parent_project_id, id))
      .get();
    return (row?.cnt ?? 0) > 0;
  }

  acquireDeployLock(projectId: string, sessionId: string): boolean {
    this.cleanExpiredDeployLocks();
    this.db
      .update(projects)
      .set({
        deploy_lock_session: sessionId,
        deploy_lock_at: sql`CURRENT_TIMESTAMP`,
        updated_at: sql`CURRENT_TIMESTAMP`,
      })
      .where(
        and(
          eq(projects.id, projectId),
          or(isNull(projects.deploy_lock_session), eq(projects.deploy_lock_session, sessionId)),
        ),
      )
      .run();
    const row = this.sqlite.prepare('SELECT changes() as changes').get() as {
      changes: number;
    } | null;
    return (row?.changes ?? 0) > 0;
  }

  releaseDeployLock(projectId: string, sessionId?: string): boolean {
    if (sessionId !== undefined) {
      this.db
        .update(projects)
        .set({
          deploy_lock_session: null,
          deploy_lock_at: null,
          updated_at: sql`CURRENT_TIMESTAMP`,
        })
        .where(and(eq(projects.id, projectId), eq(projects.deploy_lock_session, sessionId)))
        .run();

      const row = this.sqlite.prepare('SELECT changes() as changes').get() as {
        changes: number;
      } | null;

      if ((row?.changes ?? 0) === 0) {
        log.warn(
          { projectId, sessionId },
          '[DeployLock] releaseDeployLock session mismatch or lock missing',
        );
        return false;
      }

      return true;
    }

    this.db
      .update(projects)
      .set({ deploy_lock_session: null, deploy_lock_at: null, updated_at: sql`CURRENT_TIMESTAMP` })
      .where(eq(projects.id, projectId))
      .run();

    return true;
  }

  getDeployLockInfo(projectId: string): { session: string; lockedAt: string } | null {
    const project = this.getProject(projectId);
    if (!project?.deploy_lock_session || !project.deploy_lock_at) return null;
    return { session: project.deploy_lock_session, lockedAt: project.deploy_lock_at };

exec
/bin/bash -lc "sed -n '1,160p' src/db/migration.ts" in /home/lee/OpenLander
 succeeded in 0ms:
import type { SqliteDatabase } from './drizzle.js';
import { SCHEMA } from './schema.js';

/**
 * Initialize the database by creating tables and running migrations.
 */
export function initializeDatabase(sqlite: SqliteDatabase): void {
  sqlite.exec(SCHEMA);
  runMigrations(sqlite);
}

/**
 * Run all schema migrations.
 */
export function runMigrations(sqlite: SqliteDatabase): void {
  sqlite.exec('DROP TABLE IF EXISTS chat_history');

  const columns = sqlite.prepare("PRAGMA table_info('projects')").all() as Array<{
    name: string;
  }>;
  const colNames = new Set(columns.map((c) => c.name));

  if (!colNames.has('parent_project_id')) {
    sqlite.exec(
      'ALTER TABLE projects ADD COLUMN parent_project_id TEXT REFERENCES projects(id) ON DELETE CASCADE',
    );
  }
  if (!colNames.has('dockerfile_path')) {
    sqlite.exec("ALTER TABLE projects ADD COLUMN dockerfile_path TEXT DEFAULT 'Dockerfile'");
  }
  if (!colNames.has('docker_target')) {
    sqlite.exec('ALTER TABLE projects ADD COLUMN docker_target TEXT DEFAULT NULL');
  }
  if (!colNames.has('build_context')) {
    sqlite.exec('ALTER TABLE projects ADD COLUMN build_context TEXT DEFAULT NULL');
  }
  if (!colNames.has('build_method')) {
    sqlite.exec(
      "ALTER TABLE projects ADD COLUMN build_method TEXT DEFAULT NULL CHECK(build_method IN ('dockerfile', 'compose'))",
    );
  }
  if (!colNames.has('pending_fix')) {
    sqlite.exec('ALTER TABLE projects ADD COLUMN pending_fix TEXT DEFAULT NULL');
  }
  if (!colNames.has('deploy_lock_session')) {
    sqlite.exec('ALTER TABLE projects ADD COLUMN deploy_lock_session TEXT DEFAULT NULL');
  }
  if (!colNames.has('deploy_lock_at')) {
    sqlite.exec('ALTER TABLE projects ADD COLUMN deploy_lock_at DATETIME DEFAULT NULL');
  }
  if (!colNames.has('access_code')) {
    sqlite.exec('ALTER TABLE projects ADD COLUMN access_code TEXT');
  }
  if (!colNames.has('access_code_iv')) {
    sqlite.exec('ALTER TABLE projects ADD COLUMN access_code_iv TEXT');
  }
  if (!colNames.has('is_preview')) {
    sqlite.exec('ALTER TABLE projects ADD COLUMN is_preview INTEGER DEFAULT 0');
  }
  if (!colNames.has('pr_number')) {
    sqlite.exec('ALTER TABLE projects ADD COLUMN pr_number INTEGER');
  }
  if (!colNames.has('source')) {
    sqlite.exec("ALTER TABLE projects ADD COLUMN source TEXT NOT NULL DEFAULT 'git'");
  }
  if (!colNames.has('image_url')) {
    sqlite.exec('ALTER TABLE projects ADD COLUMN image_url TEXT DEFAULT NULL');
  }
  if (!colNames.has('image_cmd')) {
    sqlite.exec('ALTER TABLE projects ADD COLUMN image_cmd TEXT DEFAULT NULL');
  }
  if (!colNames.has('container_port')) {
    sqlite.exec('ALTER TABLE projects ADD COLUMN container_port INTEGER DEFAULT NULL');
  }
  if (!colNames.has('archived_at')) {
    sqlite.exec('ALTER TABLE projects ADD COLUMN archived_at TEXT');
  }
  sqlite.exec('CREATE INDEX IF NOT EXISTS idx_projects_parent ON projects(parent_project_id)');

  sqlite.exec(`CREATE TABLE IF NOT EXISTS environments (
    id TEXT PRIMARY KEY,
    project_id TEXT NOT NULL REFERENCES projects(id) ON DELETE CASCADE,
    type TEXT NOT NULL CHECK(type IN ('production', 'development')),
    branch TEXT NOT NULL DEFAULT 'main',
    status TEXT DEFAULT 'idle' CHECK(status IN ('running', 'stopped', 'building', 'error', 'idle')),
    assigned_port INTEGER UNIQUE,
    container_id TEXT,
    image_tag TEXT,
    previous_image_tag TEXT,
    public_url TEXT,
    container_port INTEGER,
    created_at TEXT DEFAULT CURRENT_TIMESTAMP,
    updated_at TEXT DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(project_id, type)
  )`);
  sqlite.exec('CREATE INDEX IF NOT EXISTS idx_environments_project ON environments(project_id)');

  const environmentColumns = sqlite.prepare("PRAGMA table_info('environments')").all() as Array<{
    name: string;
  }>;
  const environmentColNames = new Set(environmentColumns.map((c) => c.name));

  if (!environmentColNames.has('container_port')) {
    sqlite.exec('ALTER TABLE environments ADD COLUMN container_port INTEGER');
    // Backfill container_port from projects table for existing environments
    sqlite.exec(
      'UPDATE environments SET container_port = (SELECT container_port FROM projects WHERE id = environments.project_id) WHERE container_port IS NULL',
    );
  }

  const envVarColumns = sqlite.prepare("PRAGMA table_info('env_vars')").all() as Array<{
    name: string;
  }>;
  const envVarColumnNames = new Set(envVarColumns.map((c) => c.name));
  const envVarTable = sqlite
    .prepare("SELECT sql FROM sqlite_master WHERE type = 'table' AND name = 'env_vars'")
    .get() as { sql: string | null } | undefined;
  const hasLegacyProjectKeyUnique =
    typeof envVarTable?.sql === 'string' && envVarTable.sql.includes('UNIQUE(project_id, key)');

  if (hasLegacyProjectKeyUnique) {
    const environmentIdSelect = envVarColumnNames.has('environment_id')
      ? 'environment_id'
      : 'NULL AS environment_id';

    sqlite.exec(`CREATE TABLE env_vars_migrated (
      id TEXT PRIMARY KEY,
      project_id TEXT NOT NULL REFERENCES projects(id) ON DELETE CASCADE,
      environment_id TEXT REFERENCES environments(id) ON DELETE CASCADE,
      key TEXT NOT NULL,
      value TEXT NOT NULL,
      created_at TEXT DEFAULT CURRENT_TIMESTAMP
    )`);
    sqlite.exec(`INSERT INTO env_vars_migrated (
      id,
      project_id,
      environment_id,
      key,
      value,
      created_at
    ) SELECT
      id,
      project_id,
      ${environmentIdSelect},
      key,
      value,
      created_at
    FROM env_vars`);
    sqlite.exec('DROP TABLE env_vars');
    sqlite.exec('ALTER TABLE env_vars_migrated RENAME TO env_vars');
  } else if (!envVarColumnNames.has('environment_id')) {
    sqlite.exec(
      'ALTER TABLE env_vars ADD COLUMN environment_id TEXT REFERENCES environments(id) ON DELETE CASCADE',
    );
  }

  sqlite.exec('DROP INDEX IF EXISTS idx_env_vars_project');
  sqlite.exec('DROP INDEX IF EXISTS idx_env_vars_environment');
  sqlite.exec(
    'CREATE UNIQUE INDEX IF NOT EXISTS env_vars_project_key_global_unique ON env_vars(project_id, key) WHERE environment_id IS NULL',

exec
/bin/bash -lc "sed -n '80,170p' src/pipeline/deploy/rollback.ts" in /home/lee/OpenLander
 succeeded in 0ms:
        getImage: (imageTag: string) => {
          inspect: () => Promise<unknown>;
        };
      };
    };
    if (typeof dockerWithClient.getClient === 'function') {
      const dockerClient = dockerWithClient.getClient();
      try {
        await dockerClient.getImage(rollbackImageTag).inspect();
      } catch {
        return {
          success: false,
          projectId,
          projectName: project.name,
          error: 'No previous image available for rollback — the image may have been pruned',
        };
      }
    }

    try {
      await this.cleanupRunningContainer(target.target);

      const { port, containerName } = await this.resolveContainerRuntime(target.target);
      const containerPort = (await this.docker.getImageExposedPort(rollbackImageTag)) ?? port;

      const envType: OpenLanderEnv = 'production';
      const containerId = await this.docker.runContainer({
        imageTag: rollbackImageTag,
        name: containerName,
        port,
        containerPort,
        envVars: this.db.getEnvVars(projectId, productionEnvironment?.id),
        traefikLabels: buildTraefikLabels(project.name, containerPort, undefined, envType),
        network: getPolicy(envType).networkName,
      });

      this.db.updateProject(projectId, {
        status: 'running',
        assignedPort: port,
        containerPort,
        containerId,
        imageTag: rollbackImageTag,
        previousImageTag: currentImageTag,
      });

      if (productionEnvironment) {
        this.db.updateEnvironment(productionEnvironment.id, {
          status: 'running',
          containerId,
          imageTag: rollbackImageTag,
          previousImageTag: currentImageTag,
          assignedPort: port,
          containerPort,
        });
      }

      await eventBus.emit('deploy:rollback', {
        projectId,
        fromImage: currentImageTag,
        toImage: rollbackImageTag,
      });

      const totalDuration = Date.now() - startTime;
      this.db.createDeployLog({
        id: nanoid(12),
        projectId,
        environmentId: productionEnvironment?.id,
        status: 'success',
        trigger: 'api',
        commitMessage: undefined,
        buildLog: `[rollback] ${currentImageTag} → ${rollbackImageTag}\n`,
        durationMs: totalDuration,
      });

      return {
        success: true,
        projectId,
        projectName: project.name,
        previousImageTag: currentImageTag,
        rollbackImageTag,
        containerId,
        url: getProjectUrl(project.name),
        port,
        buildDurationMs: totalDuration,
      };
    } catch (error) {
      const errorMsg = error instanceof Error ? error.message : String(error);
      if (isDockerNotFoundError(error)) {
        return {
          success: false,
          projectId,

exec
/bin/bash -lc "sed -n '1500,1605p' src/pipeline/deploy-core.ts" in /home/lee/OpenLander
 succeeded in 0ms:
        },
      );

      const buildDuration = Date.now() - startTime;
      buildLog += `[build] Done (${String(Math.round(buildDuration / 1000))}s)\n`;

      await eventBus.emit('deploy:build', {
        projectId,
        imageTag,
        durationMs: buildDuration,
      });

      this.jobManager?.updatePhase(projectId, 'starting');
      newPort = await allocatePort(this.db, this.docker, {}, 'production');
      const containerPort = (await this.docker.getImageExposedPort(imageTag)) ?? newPort;
      const envVars = resolveEnvVars({ projectId, environmentId }, { env: this.env });
      const secretFiles = this.env.getSecretFilesForDeploy(projectId);
      const networkName = getPolicy('production').networkName;

      greenContainerId = await this.docker.runContainer({
        imageTag,
        name: projectContainerName(`${projectName}-green`),
        port: newPort,
        containerPort,
        envVars,
        traefikLabels: { 'traefik.enable': 'false' },
        network: networkName,
        secretFiles,
      });
      shouldCleanupGreen = true;

      await eventBus.emit('deploy:run', {
        projectId,
        containerId: greenContainerId,
        port: newPort,
        url: getProjectUrl(projectName),
      });

      buildLog += `[health] Checking http://localhost:${String(newPort)}${healthCheckPath}\n`;
      const healthy = await this.healthCheck(
        newPort,
        healthCheckPath,
        healthCheckRetries,
        healthCheckIntervalMs,
      );

      if (!healthy) {
        throw new Error(
          `Health check failed for ${projectName} on port ${String(newPort)} path ${healthCheckPath}`,
        );
      }
      buildLog += '[health] Passed\n';

      // Promote green container: stop old blue, rename green to canonical name.
      // This avoids creating a new container (which caused port conflicts)
      // and achieves zero-downtime promotion.
      await this.docker.stopContainer(blueContainerId);
      await this.docker.safeRemoveContainer(blueContainerId);

      // Add Traefik labels to the green container so it receives traffic
      const canonicalName = projectContainerName(projectName);
      const greenContainer = this.docker.getClient().getContainer(greenContainerId);
      await greenContainer.rename({ name: canonicalName });
      shouldCleanupGreen = false;

      this.db.updateProject(projectId, {
        status: 'running',
        containerId: greenContainerId,
        assignedPort: newPort,
        containerPort,
        imageTag,
        previousImageTag: project.image_tag,
      });
      if (prodEnv) {
        this.db.updateEnvironment(prodEnv.id, {
          status: 'running',
          containerId: greenContainerId,
          assignedPort: newPort,
          containerPort,
          imageTag,
          previousImageTag: prodEnv.image_tag,
        });
      }

      const durationMs = Date.now() - startTime;
      const projectUrl = getProjectUrl(projectName);

      this.db.createDeployLog({
        id: nanoid(12),
        projectId,
        environmentId,
        status: 'success',
        trigger: 'api',
        commitSha,
        buildLog,
        durationMs,
      });

      this.jobManager?.updatePhase(projectId, 'done');
      await eventBus.emit('deploy:success', {
        projectId,
        url: projectUrl,
        totalDurationMs: durationMs,
      });

      return {

exec
/bin/bash -lc "sed -n '760,820p' src/pipeline/deploy-plan/engine.ts" in /home/lee/OpenLander
 succeeded in 0ms:
    const freshPlan = JSON.parse(freshRow.plan_json) as DeployPlan;
    if (freshPlan.status === 'needs_input') {
      const missingKeys = freshPlan.missing.join(', ') || 'unknown';
      throw new Error(
        `Plan requires missing environment variables: ${missingKeys}. ` +
          `Call update_deploy_plan to provide them, then execute again.`,
      );
    }
    if (freshPlan.status !== 'ready') {
      throw new Error(`Plan status is "${freshPlan.status}" — only "ready" plans can be executed.`);
    }

    const PROJECT_NAME_REGEX = /^[a-z0-9][a-z0-9-]*$/;
    if (!PROJECT_NAME_REGEX.test(freshPlan.app.name)) {
      throw new Error(
        `Invalid project name "${freshPlan.app.name}": must contain only lowercase letters, numbers, and hyphens, starting with a letter or number`,
      );
    }

    const plan = freshPlan;

    const existingProject = this.db.getProjectByName(plan.app.name);
    let lockProjectId: string | null = null;
    let deployLockReleased = false;
    const safeReleaseDeployLock = () => {
      if (!lockProjectId || deployLockReleased) {
        return;
      }

      try {
        this.db.releaseDeployLock(lockProjectId, lockSessionId ?? `plan-${planId}`);
      } catch (error) {
        log.warn({ planId, projectId: lockProjectId, error }, 'Failed to release deploy lock');
      } finally {
        deployLockReleased = true;
      }
    };

    if (existingProject) {
      const lockSession = lockSessionId ?? `plan-${planId}`;
      const locked = this.db.acquireDeployLock(existingProject.id, lockSession);
      if (!locked) {
        const lockInfo = this.db.getDeployLockInfo(existingProject.id);
        throw new DeployLockedError(existingProject.id, lockInfo?.session ?? 'unknown');
      }
      lockProjectId = existingProject.id;
    }

    const executingPlan = PlanStateMachine.transition(plan, 'executing');
    this.db.updateDeployPlan(planId, {
      status: 'executing',
      planJson: JSON.stringify(this.preparePlanForStorage(executingPlan)),
    });

    try {
      const mergedEnv = resolveEnvVars(
        {
          projectId: plan.project_id ?? plan.app.name,
          autoEnvVars: plan.env.auto,
          inlineEnvVars: plan.env.provided,
        },

exec
/bin/bash -lc "sed -n '1,220p' test/tools/deploy-lock-mcp-tools.test.ts" in /home/lee/OpenLander
 succeeded in 0ms:
import { describe, expect, it, vi } from 'vitest';

import type { AppContext } from '../../src/app.js';
import { createSharedToolRegistry } from './shared-tool-registry.js';

function createLockedContext(): AppContext {
  const project = { id: 'proj-1', name: 'locked-app' };
  return {
    db: {
      getProjectByName: vi.fn((name: string) => (name === 'locked-app' ? project : undefined)),
      acquireDeployLock: vi.fn(() => false),
      getDeployLockInfo: vi.fn(() => ({
        session: 'other-session',
        lockedAt: new Date().toISOString(),
      })),
      getDeployPlan: vi.fn((planId: string) => ({
        id: planId,
        plan_json: JSON.stringify({
          plan_id: planId,
          project_id: 'proj-1',
          app: { name: 'locked-app' },
        }),
      })),
    },
    planEngine: {
      createPlan: vi.fn(async () => ({
        plan_id: 'plan-1',
        status: 'ready',
        app: { name: 'locked-app' },
        project_id: 'proj-1',
        missing: [],
        warnings: [],
      })),
      executePlan: vi.fn(),
    },
    pipeline: {
      redeploy: vi.fn(),
      rollback: vi.fn(),
    },
    deployQueue: {
      acquire: vi.fn().mockResolvedValue(() => {}),
    },
  } as unknown as AppContext;
}

function expectDeployLockedResult(result: unknown) {
  expect(result).toMatchObject({
    success: false,
    error: 'DEPLOY_LOCKED',
    _agent_guidance: {
      message: 'Another deploy is in progress for this project.',
      next_steps: ['Wait 30 seconds and try again', 'Check deploy status with get_deploy_status'],
    },
  });
}

function getTool(ctx: AppContext, name: string) {
  const tool = createSharedToolRegistry(ctx, { target: 'mcp', names: [name] }).find(
    (entry) => entry.name === name,
  );
  expect(tool).toBeDefined();
  return tool!;
}

describe('BUG-002 MCP deploy tool lock guard', () => {
  it('deploy returns DEPLOY_LOCKED response when project lock is held', async () => {
    const ctx = createLockedContext();
    const tool = getTool(ctx, 'deploy');

    const result = await tool.execute(
      { repo_url: 'https://github.com/test/app' },
      { target: 'mcp' },
    );

    expectDeployLockedResult(result);
    expect(ctx.planEngine.executePlan).not.toHaveBeenCalled();
  });

  it('execute_deploy_plan returns DEPLOY_LOCKED response when project lock is held', async () => {
    const ctx = createLockedContext();
    const tool = getTool(ctx, 'execute_deploy_plan');

    const result = await tool.execute({ plan_id: 'plan-1' }, { target: 'mcp' });

    expectDeployLockedResult(result);
    expect(ctx.planEngine.executePlan).not.toHaveBeenCalled();
  });

  it('redeploy_project returns DEPLOY_LOCKED response when project lock is held', async () => {
    const ctx = createLockedContext();
    const tool = getTool(ctx, 'redeploy_project');

    const result = await tool.execute({ project_name: 'locked-app' }, { target: 'mcp' });

    expectDeployLockedResult(result);
    expect(ctx.pipeline.redeploy).not.toHaveBeenCalled();
  });

  it('rollback_project returns DEPLOY_LOCKED response when project lock is held', async () => {
    const ctx = createLockedContext();
    const tool = getTool(ctx, 'rollback_project');

    const result = await tool.execute({ project_name: 'locked-app' }, { target: 'mcp' });

    expectDeployLockedResult(result);
    expect(ctx.pipeline.rollback).not.toHaveBeenCalled();
  });

  it('deploy_blue_green returns DEPLOY_LOCKED response when project lock is held', async () => {
    const ctx = createLockedContext();
    const tool = getTool(ctx, 'deploy_blue_green');

    const result = await tool.execute({ project_name: 'locked-app' }, { target: 'mcp' });

    expectDeployLockedResult(result);
    expect(ctx.pipeline.redeploy).not.toHaveBeenCalled();
  });
});

exec
/bin/bash -lc 'git show d31f708 -- test/tools/deploy-lock-mcp-tools.test.ts test/tools/registry-critical-tools.test.ts src/db/migration.ts' in /home/lee/OpenLander
 succeeded in 0ms:
commit d31f7082da4fde9179d049bca7f2872b64d85870
Author: lehdqlsl <lehdqlsl@naver.com>
Date:   Fri Apr 10 16:42:39 2026 +0900

    test(tools): fix mock regressions from deploy-lock and status hardening

    - deploy-lock-mcp-tools.test.ts: Fix deployQueue.acquire mock to return release function
    - registry-critical-tools.test.ts: Add getDeployLockInfo mock to db context
    - migration.ts: Add backfill SQL for environments.container_port from projects table

    Fixes 2 test failures, leaving only 3 pre-existing ops-incident-routes failures.

diff --git a/src/db/migration.ts b/src/db/migration.ts
index ca1dc0c..00e08c3 100644
--- a/src/db/migration.ts
+++ b/src/db/migration.ts
@@ -102,6 +102,10 @@ export function runMigrations(sqlite: SqliteDatabase): void {

   if (!environmentColNames.has('container_port')) {
     sqlite.exec('ALTER TABLE environments ADD COLUMN container_port INTEGER');
+    // Backfill container_port from projects table for existing environments
+    sqlite.exec(
+      'UPDATE environments SET container_port = (SELECT container_port FROM projects WHERE id = environments.project_id) WHERE container_port IS NULL',
+    );
   }

   const envVarColumns = sqlite.prepare("PRAGMA table_info('env_vars')").all() as Array<{
diff --git a/test/tools/deploy-lock-mcp-tools.test.ts b/test/tools/deploy-lock-mcp-tools.test.ts
index a2e69e1..f446ddc 100644
--- a/test/tools/deploy-lock-mcp-tools.test.ts
+++ b/test/tools/deploy-lock-mcp-tools.test.ts
@@ -38,7 +38,7 @@ function createLockedContext(): AppContext {
       rollback: vi.fn(),
     },
     deployQueue: {
-      acquire: vi.fn(),
+      acquire: vi.fn().mockResolvedValue(() => {}),
     },
   } as unknown as AppContext;
 }
diff --git a/test/tools/registry-critical-tools.test.ts b/test/tools/registry-critical-tools.test.ts
index 1d181e7..2d88090 100644
--- a/test/tools/registry-critical-tools.test.ts
+++ b/test/tools/registry-critical-tools.test.ts
@@ -54,6 +54,7 @@ function createMockContext() {
     db: {
       getProjectByName: vi.fn().mockReturnValue(project),
       getLastDeployLog: vi.fn(),
+      getDeployLockInfo: vi.fn().mockReturnValue(null),
     },
     composePipeline,
     jobManager,

exec
/bin/bash -lc "sed -n '100,150p' test/tools/registry-critical-tools.test.ts" in /home/lee/OpenLander
 succeeded in 0ms:
    });
    mockLoadConfig.mockReturnValue({ gitProviders: { github: { token: '' } } });
  });

  afterEach(() => {
    vi.restoreAllMocks();
  });

  it('get_logs applies target-specific default line counts', async () => {
    const { ctx, pipeline } = createMockContext();
    const tool = getTool(ctx, 'get_logs');

    await tool.execute({ project_name: 'critical-app' }, { target: 'agent' });
    await tool.execute({ project_name: 'critical-app' }, { target: 'mcp' });

    expect(pipeline.getLogs).toHaveBeenNthCalledWith(1, 'p1', 20);
    expect(pipeline.getLogs).toHaveBeenNthCalledWith(2, 'p1', 50);
  });

  it('get_deploy_status formats single project status for agent and mcp targets', async () => {
    const { ctx, jobManager } = createMockContext();
    const tool = getTool(ctx, 'get_deploy_status');
    const now = new Date(Date.now() - 3000);

    jobManager.getStatus
      .mockReturnValueOnce({
        projectName: 'critical-app',
        phase: 'building',
        startedAt: now,
        errorSummary: null,
      })
      .mockReturnValueOnce({
        projectName: 'critical-app',
        phase: 'done',
        startedAt: now,
        errorSummary: 'none',
      });

    const agentResult = await tool.execute({ project_name: 'critical-app' }, { target: 'agent' });
    const mcpResult = await tool.execute({ project_name: 'critical-app' }, { target: 'mcp' });

    expect(agentResult).toEqual({
      active: 1,
      jobs: [
        expect.objectContaining({
          name: 'critical-app',
          phase: 'building',
          elapsed: expect.any(String),
        }),
      ],
    });

exec
/bin/bash -lc "sed -n '1,260p' test/deploy-lock.test.ts" in /home/lee/OpenLander
 succeeded in 0ms:
import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest';
import { join } from 'node:path';
import { mkdtempSync, rmSync } from 'node:fs';
import { tmpdir } from 'node:os';

import type { AppContext } from '../src/app.js';
import { DeployPipeline } from '../src/pipeline/deploy.js';
import { JobManager } from '../src/pipeline/job-manager.js';
import { Database } from '../src/db/index.js';
import { DeployLockedError } from '../src/errors.js';
import { eventBus } from '../src/events/index.js';
import type { OpenLanderConfig } from '../src/config/index.js';
import type { Docker } from '../src/pipeline/docker.js';
import { clearPortScanCache } from '../src/pipeline/port.js';
import { deployToolDefs } from '../src/tools/defs/deploy.js';

type EnvLike = {
  getMergedForDeploy: (projectId: string, environmentId?: string) => Record<string, string>;
  getSecretFilesForDeploy: (
    projectId: string,
  ) => Array<{ filename: string; content: string; mountPath: string }>;
};

function createMockDocker(): Docker {
  return {
    stopContainer: vi.fn().mockResolvedValue(undefined),
    removeContainer: vi.fn().mockResolvedValue(undefined),
    safeRemoveContainer: vi.fn().mockResolvedValue(undefined),
    runContainer: vi.fn().mockResolvedValue('container-new-123456'),
    startContainer: vi.fn().mockResolvedValue(undefined),
    getImageExposedPort: vi.fn().mockResolvedValue(3000),
    listContainers: vi.fn().mockResolvedValue([]),
    listAllContainers: vi.fn().mockResolvedValue([]),
    inspectContainer: vi.fn().mockResolvedValue(null),
    getLogs: vi.fn().mockResolvedValue(''),
    cleanupSecretFiles: vi.fn(),
  } as unknown as Docker;
}

function createStatusToolContext(db: Database, jobManager: JobManager): AppContext {
  return {
    db,
    jobManager,
  } as unknown as AppContext;
}

function getDeployStatusTool() {
  const tool = deployToolDefs.find((entry) => entry.name === 'get_deploy_status');
  expect(tool).toBeDefined();
  return tool!;
}

describe('BUG-010: Deploy lock prevents concurrent deploys', () => {
  let tmpDir: string;
  let db: Database;
  let docker: Docker;
  let env: EnvLike;
  let pipeline: DeployPipeline;
  const testConfig = { ai: { secretScan: { enabled: false } } } as OpenLanderConfig;

  beforeEach(() => {
    tmpDir = mkdtempSync(join(tmpdir(), 'openlander-deploy-lock-'));
    db = new Database(join(tmpDir, 'test.db'));
    docker = createMockDocker();
    env = {
      getMergedForDeploy: vi.fn().mockReturnValue({ NODE_ENV: 'test' }),
      getSecretFilesForDeploy: vi.fn().mockReturnValue([]),
    };
    pipeline = new DeployPipeline(docker, db, env as never, testConfig);
  });

  afterEach(() => {
    clearPortScanCache();
    db.close();
    rmSync(tmpDir, { recursive: true, force: true });
    vi.restoreAllMocks();
  });

  describe('DB-level lock', () => {
    it('rejects concurrent lock on same project', () => {
      db.createProject({
        id: 'p1',
        name: 'test-app',
        repoUrl: 'https://github.com/test/app',
      });

      const first = db.acquireDeployLock('p1', 'session-1');
      expect(first).toBe(true);

      const second = db.acquireDeployLock('p1', 'session-2');
      expect(second).toBe(false);
    });

    it('allows re-acquisition after release', () => {
      db.createProject({
        id: 'p1',
        name: 'test-app',
        repoUrl: 'https://github.com/test/app',
      });

      db.acquireDeployLock('p1', 'session-1');
      db.releaseDeployLock('p1');

      const result = db.acquireDeployLock('p1', 'session-2');
      expect(result).toBe(true);
    });

    it('allows concurrent locks on different projects', () => {
      db.createProject({
        id: 'p1',
        name: 'app-one',
        repoUrl: 'https://github.com/test/app-one',
      });
      db.createProject({
        id: 'p2',
        name: 'app-two',
        repoUrl: 'https://github.com/test/app-two',
      });

      expect(db.acquireDeployLock('p1', 'session-1')).toBe(true);
      expect(db.acquireDeployLock('p2', 'session-2')).toBe(true);
    });

    it('releaseDeployLock with correct session succeeds', () => {
      db.createProject({
        id: 'p1',
        name: 'session-release-app',
        repoUrl: 'https://github.com/test/session-release-app',
      });

      expect(db.acquireDeployLock('p1', 'session-abc')).toBe(true);
      const releaseDeployLock = Reflect.get(db, 'releaseDeployLock') as (
        projectId: string,
        sessionId?: string,
      ) => boolean;

      expect(releaseDeployLock.call(db, 'p1', 'session-abc')).toBe(true);
      expect(db.getDeployLockInfo('p1')).toBeNull();
    });

    it('releaseDeployLock with wrong session returns false and keeps lock', () => {
      db.createProject({
        id: 'p1',
        name: 'wrong-session-release-app',
        repoUrl: 'https://github.com/test/wrong-session-release-app',
      });

      expect(db.acquireDeployLock('p1', 'session-abc')).toBe(true);
      const releaseDeployLock = Reflect.get(db, 'releaseDeployLock') as (
        projectId: string,
        sessionId?: string,
      ) => boolean;

      expect(releaseDeployLock.call(db, 'p1', 'session-xyz')).toBe(false);
      expect(db.getDeployLockInfo('p1')).toEqual(
        expect.objectContaining({ session: 'session-abc' }),
      );
    });

    it('releaseDeployLock without session releases unconditionally', () => {
      db.createProject({
        id: 'p1',
        name: 'unconditional-release-app',
        repoUrl: 'https://github.com/test/unconditional-release-app',
      });

      expect(db.acquireDeployLock('p1', 'session-abc')).toBe(true);

      expect(db.releaseDeployLock('p1')).toBe(true);
      expect(db.getDeployLockInfo('p1')).toBeNull();
    });

    it('cleanExpiredDeployLocks bypasses session check', () => {
      db.createProject({
        id: 'p1',
        name: 'expired-lock-app',
        repoUrl: 'https://github.com/test/expired-lock-app',
      });

      expect(db.acquireDeployLock('p1', 'session-abc')).toBe(true);

      const sqlite = Reflect.get(db, 'sqlite') as {
        prepare: (sql: string) => { run: (...params: Array<string | number>) => unknown };
      };
      sqlite
        .prepare("UPDATE projects SET deploy_lock_at = datetime('now', '-31 minutes') WHERE id = ?")
        .run('p1');

      expect(db.cleanExpiredDeployLocks()).toBeGreaterThan(0);
      expect(db.getDeployLockInfo('p1')).toBeNull();
    });
  });

  describe('DeployLockedError', () => {
    it('has correct code and statusCode', () => {
      const err = new DeployLockedError('p1', 'session-1');
      expect(err.code).toBe('DEPLOY_LOCKED');
      expect(err.statusCode).toBe(409);
      expect(err.message).toContain('p1');
      expect(err.name).toBe('DeployLockedError');
    });

    it('serializes with toJSON', () => {
      const err = new DeployLockedError('p1', 'session-1');
      const json = err.toJSON();
      expect(json.error).toBe('DEPLOY_LOCKED');
      expect(json.details).toEqual({ projectId: 'p1', lockedBySession: 'session-1' });
    });
  });

  describe('Pipeline redeploy lock integration', () => {
    it('redeploy throws DeployLockedError when project is locked', async () => {
      db.createProject({
        id: 'p1',
        name: 'locked-app',
        repoUrl: 'https://github.com/test/locked-app',
        branch: 'main',
      });
      db.updateProject('p1', {
        status: 'running',
        containerId: 'container-1',
        assignedPort: 10010,
      });

      db.acquireDeployLock('p1', 'other-session');

      await expect(pipeline.redeploy('p1')).rejects.toThrow(DeployLockedError);
    });

    it('redeploy releases lock after successful completion', async () => {
      db.createProject({
        id: 'p1',
        name: 'release-app',
        repoUrl: 'https://github.com/test/release-app',
        branch: 'main',
      });
      db.updateProject('p1', {
        status: 'running',
        containerId: 'container-1',
        assignedPort: 10010,
      });

      vi.spyOn(pipeline, 'deploy').mockResolvedValue({
        success: true,
        projectId: 'p1',
        projectName: 'release-app',
      });

      await pipeline.redeploy('p1');

      const lockInfo = db.getDeployLockInfo('p1');
      expect(lockInfo).toBeNull();
    });

    it('redeploy releases lock even after failure', async () => {
      db.createProject({
        id: 'p1',
        name: 'fail-app',
        repoUrl: 'https://github.com/test/fail-app',
        branch: 'main',

exec
/bin/bash -lc "sed -n '260,520p' test/deploy-lock.test.ts" in /home/lee/OpenLander
 succeeded in 0ms:
        branch: 'main',
      });
      db.updateProject('p1', {
        status: 'running',
        containerId: 'container-1',
        assignedPort: 10010,
      });

      vi.spyOn(pipeline, 'deploy').mockRejectedValue(new Error('build failed'));

      await pipeline.redeploy('p1').catch(() => undefined);

      const lockInfo = db.getDeployLockInfo('p1');
      expect(lockInfo).toBeNull();
    });

    it('missing project skips lock and returns not-found', async () => {
      const result = await pipeline.redeploy('nonexistent');
      expect(result.success).toBe(false);
      expect(result.error).toContain('Project not found');
    });
  });

  describe('get_deploy_status wait=true hardening', () => {
    it('wait=true resolves from deploy_logs when job not in JobManager', async () => {
      vi.useFakeTimers();

      db.createProject({
        id: 'p1',
        name: 'status-app',
        repoUrl: 'https://github.com/test/status-app',
      });
      db.createDeployLog({
        id: 'log-1',
        projectId: 'p1',
        status: 'success',
        trigger: 'api',
      });

      const ctx = createStatusToolContext(db, new JobManager());
      const tool = getDeployStatusTool();

      const resultPromise = tool.execute(
        { project_name: 'status-app', wait: true, timeout: 6 },
        { appCtx: ctx, target: 'agent' },
      ) as Promise<Record<string, unknown>>;

      await vi.advanceTimersByTimeAsync(5000);
      const result = await resultPromise;

      expect(result).toMatchObject({
        active: 0,
        jobs: [expect.objectContaining({ name: 'status-app', phase: 'done' })],
      });
      expect(result).not.toHaveProperty('timeout');
    });

    it('wait=true shows locked/queued when lock held but no active job', async () => {
      db.createProject({
        id: 'p1',
        name: 'locked-status-app',
        repoUrl: 'https://github.com/test/locked-status-app',
      });
      db.acquireDeployLock('p1', 'test-session');

      const ctx = createStatusToolContext(db, new JobManager());
      const tool = getDeployStatusTool();

      const result = (await tool.execute(
        { project_name: 'locked-status-app', wait: true, timeout: 30 },
        { appCtx: ctx, target: 'agent' },
      )) as Record<string, unknown>;

      expect(result).toMatchObject({
        active: 1,
        locked: true,
        lock_session: 'test-session',
        jobs: [expect.objectContaining({ name: 'locked-status-app', phase: 'queued' })],
      });
    });

    it('wait=true transitions through queued to done when deploy event fires', async () => {
      db.createProject({
        id: 'p1',
        name: 'evented-status-app',
        repoUrl: 'https://github.com/test/evented-status-app',
      });
      db.acquireDeployLock('p1', 'test-session');

      const jobManager = new JobManager();
      const ctx = createStatusToolContext(db, jobManager);
      const tool = getDeployStatusTool();

      const queuedResult = (await tool.execute(
        { project_name: 'evented-status-app', wait: true, timeout: 30 },
        { appCtx: ctx, target: 'agent' },
      )) as Record<string, unknown>;

      expect(queuedResult).toMatchObject({
        active: 1,
        locked: true,
        lock_session: 'test-session',
        jobs: [expect.objectContaining({ name: 'evented-status-app', phase: 'queued' })],
      });

      jobManager.trackJob('p1', 'evented-status-app');
      jobManager.updatePhase('p1', 'done');
      db.releaseDeployLock('p1');
      await eventBus.emit('deploy:success', {
        projectId: 'p1',
        url: 'https://example.test',
        totalDurationMs: 1000,
      });

      const doneResult = (await tool.execute(
        { project_name: 'evented-status-app', wait: true, timeout: 30 },
        { appCtx: ctx, target: 'agent' },
      )) as Record<string, unknown>;

      expect(doneResult).toMatchObject({
        active: 0,
        jobs: [expect.objectContaining({ name: 'evented-status-app', phase: 'done' })],
      });
    });
  });
});

exec
/bin/bash -lc "rg -n \"releaseDeployLock\\(\" src test | sed -n '1,200p'" in /home/lee/OpenLander
 succeeded in 0ms:
test/deploy-lock.test.ts:102:      db.releaseDeployLock('p1');
test/deploy-lock.test.ts:169:      expect(db.releaseDeployLock('p1')).toBe(true);
test/deploy-lock.test.ts:367:      db.releaseDeployLock('p1');
src/pipeline/deploy-plan/engine.ts:790:        this.db.releaseDeployLock(lockProjectId, lockSessionId ?? `plan-${planId}`);
src/pipeline/deploy-core.ts:1392:      this.db.releaseDeployLock(projectId, lockSession);
src/pipeline/deploy-core.ts:1803:      this.db.releaseDeployLock(projectId, lockSession);
src/web/api/routes.ts:191:    ctx.db.releaseDeployLock(p.projectId);
src/web/api/routes.ts:194:    ctx.db.releaseDeployLock(p.projectId);
src/web/api/routes.ts:197:    ctx.db.releaseDeployLock(p.projectId);
src/web/api/routes.ts:200:    ctx.db.releaseDeployLock(p.projectId);
src/db/index.ts:137:  releaseDeployLock(projectId: string, sessionId?: string) {
src/db/index.ts:138:    return this.projectRepo.releaseDeployLock(projectId, sessionId);
src/db/repos/project.repo.ts:321:  releaseDeployLock(projectId: string, sessionId?: string): boolean {

ERROR: Reconnecting... 2/5
exec
/bin/bash -lc "sed -n '170,210p' src/web/api/routes.ts" in /home/lee/OpenLander
 succeeded in 0ms:
    'compose:up',
    'compose:failed',
    'monitor:inactive',
    'health:degraded',
    'recovery:start',
    'recovery:success',
    'recovery:failed',
    'recovery:exhausted',
    'recovery:approval-needed',
    'recovery:approval-resolved',
    'recovery:blocked',
    'recovery:stopped',
    'recovery:started',
    'ai:invoked',
    'ai:completed',
    'alert:new',
    'alert:resolved',
  ];

  // Auto-release deploy locks on completion/failure
  eventBus.on('deploy:success', (p) => {
    ctx.db.releaseDeployLock(p.projectId);
  });
  eventBus.on('deploy:failed', (p) => {
    ctx.db.releaseDeployLock(p.projectId);
  });
  eventBus.on('compose:up', (p) => {
    ctx.db.releaseDeployLock(p.projectId);
  });
  eventBus.on('compose:failed', (p) => {
    ctx.db.releaseDeployLock(p.projectId);
  });

  // --- Helper: resolve project names for activity log rows ---

  function resolveActivityRows(rows: ActivityLogRow[]): ActivityEvent[] {
    const projectNameCache = new Map<string, string>();
    return rows.map((row) => {
      let name = projectNameCache.get(row.project_id);
      if (name === undefined) {
        const project = ctx.db.getProject(row.project_id);

codex
lock 릴리스 호출 지점까지 확인했습니다. 다음은 `environments.container_port`가 생성·업데이트·rollback/blue-green 경로에서 어떻게 흘러가는지 연결해서 보겠습니다.
exec
/bin/bash -lc "sed -n '1,160p' src/db/repos/environment.repo.ts" in /home/lee/OpenLander
 succeeded in 0ms:
import { asc, eq, sql } from 'drizzle-orm';
import type { DrizzleClient, SqliteDatabase } from '../drizzle.js';
import { environments } from '../schema.drizzle.js';
import type { EnvironmentRow } from '../types.js';

export class EnvironmentRepo {
  constructor(
    private readonly db: DrizzleClient,
    private readonly sqlite: SqliteDatabase,
  ) {
    void this.sqlite;
  }

  createEnvironment(environment: {
    id: string;
    projectId: string;
    type: EnvironmentRow['type'];
    branch: string;
    status?: EnvironmentRow['status'];
    assignedPort?: number | null;
    containerId?: string | null;
    imageTag?: string | null;
    previousImageTag?: string | null;
    publicUrl?: string | null;
  }): EnvironmentRow {
    this.db
      .insert(environments)
      .values({
        id: environment.id,
        project_id: environment.projectId,
        type: environment.type,
        branch: environment.branch,
        status: environment.status ?? 'idle',
        assigned_port: environment.assignedPort ?? null,
        container_id: environment.containerId ?? null,
        image_tag: environment.imageTag ?? null,
        previous_image_tag: environment.previousImageTag ?? null,
        public_url: environment.publicUrl ?? null,
      })
      .run();

    const created = this.getEnvironment(environment.id);
    if (!created) throw new Error(`Failed to create environment ${environment.id}`);
    return created;
  }

  getEnvironment(id: string): EnvironmentRow | undefined {
    return this.db.select().from(environments).where(eq(environments.id, id)).get() as
      | EnvironmentRow
      | undefined;
  }

  getEnvironmentsByProject(projectId: string): EnvironmentRow[] {
    return this.db
      .select()
      .from(environments)
      .where(eq(environments.project_id, projectId))
      .orderBy(asc(environments.created_at))
      .all() as EnvironmentRow[];
  }

  updateEnvironment(
    id: string,
    updates: Partial<{
      branch: string;
      status: EnvironmentRow['status'];
      assignedPort: number | null;
      containerId: string | null;
      imageTag: string | null;
      previousImageTag: string | null;
      publicUrl: string | null;
      containerPort: number | null;
    }>,
  ): void {
    const setValues: Partial<typeof environments.$inferInsert> = {};

    if (updates.branch !== undefined) {
      setValues.branch = updates.branch;
    }
    if (updates.status !== undefined) {
      setValues.status = updates.status;
    }
    if (updates.assignedPort !== undefined) {
      setValues.assigned_port = updates.assignedPort;
    }
    if (updates.containerId !== undefined) {
      setValues.container_id = updates.containerId;
    }
    if (updates.imageTag !== undefined) {
      setValues.image_tag = updates.imageTag;
    }
    if (updates.previousImageTag !== undefined) {
      setValues.previous_image_tag = updates.previousImageTag;
    }
    if (updates.publicUrl !== undefined) {
      setValues.public_url = updates.publicUrl;
    }
    if (updates.containerPort !== undefined) {
      setValues.container_port = updates.containerPort;
    }

    if (Object.keys(setValues).length === 0) return;

    this.db
      .update(environments)
      .set({ ...setValues, updated_at: sql`CURRENT_TIMESTAMP` })
      .where(eq(environments.id, id))
      .run();
  }

  deleteEnvironment(id: string): void {
    this.db.delete(environments).where(eq(environments.id, id)).run();
  }
}

exec
/bin/bash -lc "sed -n '1,140p' src/db/schema.drizzle.ts" in /home/lee/OpenLander
 succeeded in 0ms:
import { sql } from 'drizzle-orm';
import {
  check,
  index,
  integer,
  real,
  sqliteTable,
  text,
  uniqueIndex,
  type AnySQLiteColumn,
} from 'drizzle-orm/sqlite-core';

export const projects = sqliteTable(
  'projects',
  {
    id: text('id').primaryKey(),
    name: text('name').notNull().unique(),
    repo_url: text('repo_url'),
    branch: text('branch').default('main'),
    status: text('status', {
      enum: ['running', 'stopped', 'building', 'error', 'recovering'],
    }).default('stopped'),
    visibility: text('visibility', {
      enum: ['internal', 'quick-share', 'shared', 'production'],
    }).default('internal'),
    assigned_port: integer('assigned_port').unique(),
    container_id: text('container_id'),
    image_tag: text('image_tag'),
    previous_image_tag: text('previous_image_tag'),
    public_url: text('public_url'),
    parent_project_id: text('parent_project_id').references((): AnySQLiteColumn => projects.id, {
      onDelete: 'cascade',
    }),
    dockerfile_path: text('dockerfile_path').default('Dockerfile'),
    docker_target: text('docker_target'),
    build_context: text('build_context'),
    build_method: text('build_method', { enum: ['dockerfile', 'compose'] }),
    source: text('source').notNull().default('git'),
    image_url: text('image_url'),
    image_cmd: text('image_cmd'),
    container_port: integer('container_port'),
    pending_fix: text('pending_fix'),
    created_at: text('created_at').default(sql`CURRENT_TIMESTAMP`),
    updated_at: text('updated_at').default(sql`CURRENT_TIMESTAMP`),
    archived_at: text('archived_at'),
    deploy_lock_session: text('deploy_lock_session'),
    deploy_lock_at: text('deploy_lock_at'),
    access_code: text('access_code'),
    access_code_iv: text('access_code_iv'),
    is_preview: integer('is_preview').default(0),
    pr_number: integer('pr_number'),
  },
  (table) => [
    check(
      'projects_status_check',
      sql`${table.status} IN ('running', 'stopped', 'building', 'error', 'recovering')`,
    ),
    check(
      'projects_visibility_check',
      sql`${table.visibility} IN ('internal', 'quick-share', 'shared', 'production')`,
    ),
    check('projects_build_method_check', sql`${table.build_method} IN ('dockerfile', 'compose')`),
    check('projects_is_preview_check', sql`${table.is_preview} IN (0, 1)`),
    index('idx_projects_parent').on(table.parent_project_id),
  ],
);

export const environments = sqliteTable(
  'environments',
  {
    id: text('id').primaryKey(),
    project_id: text('project_id')
      .notNull()
      .references(() => projects.id, { onDelete: 'cascade' }),
    type: text('type', { enum: ['production', 'development'] }).notNull(),
    branch: text('branch').notNull().default('main'),
    status: text('status', { enum: ['running', 'stopped', 'building', 'error', 'idle'] }).default(
      'idle',
    ),
    assigned_port: integer('assigned_port').unique(),
    container_id: text('container_id'),
    image_tag: text('image_tag'),
    previous_image_tag: text('previous_image_tag'),
    public_url: text('public_url'),
    container_port: integer('container_port'),
    created_at: text('created_at').default(sql`CURRENT_TIMESTAMP`),
    updated_at: text('updated_at').default(sql`CURRENT_TIMESTAMP`),
  },
  (table) => [
    check('environments_type_check', sql`${table.type} IN ('production', 'development')`),
    check(
      'environments_status_check',
      sql`${table.status} IN ('running', 'stopped', 'building', 'error', 'idle')`,
    ),
    uniqueIndex('environments_project_type_unique').on(table.project_id, table.type),
    index('idx_environments_project').on(table.project_id),
  ],
);

export const envVars = sqliteTable(
  'env_vars',
  {
    id: text('id').primaryKey(),
    project_id: text('project_id')
      .notNull()
      .references(() => projects.id, { onDelete: 'cascade' }),
    environment_id: text('environment_id').references(() => environments.id, {
      onDelete: 'cascade',
    }),
    key: text('key').notNull(),
    value: text('value').notNull(),
    created_at: text('created_at').default(sql`CURRENT_TIMESTAMP`),
  },
  (table) => [
    uniqueIndex('env_vars_project_key_unique').on(table.project_id, table.key),
    index('idx_env_vars_project').on(table.project_id),
    index('idx_env_vars_environment').on(table.environment_id),
  ],
);

export const deployLogs = sqliteTable(
  'deploy_logs',
  {
    id: text('id').primaryKey(),
    project_id: text('project_id')
      .notNull()
      .references(() => projects.id, { onDelete: 'cascade' }),
    environment_id: text('environment_id').references(() => environments.id, {
      onDelete: 'cascade',
    }),
    status: text('status', { enum: ['success', 'failed', 'cancelled'] }),
    trigger: text('trigger_source', { enum: ['chat', 'webhook', 'api'] }),
    trigger_detail: text('trigger_detail'),
    commit_sha: text('commit_sha'),
    commit_message: text('commit_message'),
    build_log: text('build_log'),
    runtime_log: text('runtime_log'),
    duration_ms: integer('duration_ms'),
    created_at: text('created_at').default(sql`CURRENT_TIMESTAMP`),
  },

exec
/bin/bash -lc "sed -n '1,180p' src/db/types.ts" in /home/lee/OpenLander
 succeeded in 0ms:
// --- Row types (match DB schema) ---

export type EnvironmentType = 'production' | 'development';

export interface ProjectRow {
  id: string;
  name: string;
  repo_url: string | null;
  branch: string;
  status: 'running' | 'stopped' | 'building' | 'error';
  visibility: 'internal' | 'quick-share' | 'shared' | 'production';
  assigned_port: number | null;
  container_id: string | null;
  image_tag: string | null;
  previous_image_tag: string | null;
  public_url: string | null;
  parent_project_id: string | null;
  dockerfile_path: string;
  docker_target: string | null;
  build_context: string | null;
  build_method: 'dockerfile' | 'compose' | null;
  source: 'git' | 'image';
  image_url: string | null;
  image_cmd: string | null;
  container_port: number | null;
  pending_fix: string | null;
  created_at: string;
  updated_at: string;
  archived_at: string | null;
  deploy_lock_session: string | null;
  deploy_lock_at: string | null;
  access_code: string | null;
  access_code_iv: string | null;
  is_preview: 0 | 1;
  pr_number: number | null;
}

export interface EnvironmentRow {
  id: string;
  project_id: string;
  type: EnvironmentType;
  branch: string;
  status: 'running' | 'stopped' | 'building' | 'error' | 'idle';
  assigned_port: number | null;
  container_id: string | null;
  image_tag: string | null;
  previous_image_tag: string | null;
  public_url: string | null;
  container_port: number | null;
  created_at: string;
  updated_at: string;
}

export interface DeployLogRow {
  id: string;
  project_id: string;
  environment_id: string | null;
  status: 'success' | 'failed' | 'cancelled';
  trigger: 'chat' | 'webhook' | 'api';
  trigger_detail: string | null;
  commit_sha: string | null;
  commit_message: string | null;
  build_log: string | null;
  runtime_log: string | null;
  duration_ms: number | null;
  created_at: string;
}

export interface TimelineEventRow {
  id: string;
  project_id: string;
  deploy_id: string | null;
  type: string;
  message: string;
  detail: string | null;
  severity: string | null;
  percent: number | null;
  tool_name: string | null;
  action_buttons: string | null;
  created_at: string;
}

export interface DomainMappingRow {
  id: string;
  project_id: string;
  domain: string;
  cloudflare_zone_id: string | null;
  cloudflare_dns_record_id: string | null;
  status: 'active' | 'pending' | 'error';
  created_at: string;
}

export interface OAuthTokenRow {
  id: string;
  provider: string;
  access_token: string;
  refresh_token: string | null;
  expires_at: string | null;
  token_type: string;
  auth_method: string | null;
  user_email: string | null;
  iv: string | null;
  created_at: string;
  updated_at: string;
}

export interface WebhookConfigRow {
  id: string;
  project_id: string;
  source: 'github' | 'gitlab' | 'bitbucket';
  secret: string;
  branch_filter: string;
  enabled: 0 | 1;
  created_at: string;
}

export interface ServiceRow {
  id: string;
  name: string;
  type: string;
  image: string;
  status: 'running' | 'stopped' | 'error';
  container_id: string | null;
  container_name: string;
  port: number;
  env_vars: string | null;
  credentials: string | null;
  created_at: string;
  updated_at: string;
}

export interface ServiceConnectionRow {
  id: string;
  project_id: string;
  service_id: string;
  environment_id: string | null;
  auto_injected_env_keys: string | null;
  created_at: string;
}

export interface RuntimeIncidentRow {
  id: string;
  project_id: string;
  environment_id: string | null;
  category: string;
  exit_code: number | null;
  error_snippet: string | null;
  container_image: string | null;
  container_uptime_ms: number | null;
  restart_count: number | null;
  diagnosis: string | null;
  resolved: number;
  resolved_at: string | null;
  created_at: string;
}

export interface DeployConfigRow {
  id: string;
  project_id: string;
  config_json: string;
  config_version: number;
  created_at: string;
  updated_at: string;
}

export interface PendingFixRow {
  filePath: string;
  content?: string;
  patches?: Array<{
    pattern: string;
    replacement: string;
    flags?: string;
  }>;
}

export interface DeployPlanRow {
  id: string;
  project_name: string | null;
  project_id: string | null;
  status: string;

exec
/bin/bash -lc "rg -n \"createEnvironment\\(|updateEnvironment\\(|container_port|containerPort\" src/pipeline src/db test/pipeline test/blue-green-port-conflict.test.ts | sed -n '1,260p'" in /home/lee/OpenLander
 succeeded in 0ms:
test/blue-green-port-conflict.test.ts:204:  it('persists containerPort to environment after blue-green promotion', async () => {
test/blue-green-port-conflict.test.ts:205:    db.updateEnvironment('p1-production', {
test/blue-green-port-conflict.test.ts:222:    expect(environment?.container_port).toBe(3000);
test/blue-green-port-conflict.test.ts:225:    expect(project?.container_port).toBe(3000);
test/pipeline/env-merge-precedence.test.ts:34:  db.createEnvironment({
test/pipeline/image-deploy-e2e.test.ts:27:    containerPort: parsed.port,
test/pipeline/image-deploy-e2e.test.ts:131:        containerPort: config.containerPort,
test/pipeline/image-deploy-e2e.test.ts:150:        containerPort: 8080,
test/pipeline/image-deploy-e2e.test.ts:170:    expect(assembledConfig.containerPort).toBe(8080);
src/db/types.ts:25:  container_port: number | null;
src/db/types.ts:49:  container_port: number | null;
test/pipeline/image-deploy.test.ts:33:      containerPort: 8080,
test/pipeline/image-deploy.test.ts:41:    expect(config.containerPort).toBe(8080);
test/pipeline/image-deploy.test.ts:45:  it('preserves image_url, image_cmd, and container_port from DB columns', () => {
test/pipeline/image-deploy.test.ts:53:      containerPort: 80,
test/pipeline/image-deploy.test.ts:60:    expect(config.containerPort).toBe(80);
test/pipeline/image-deploy.test.ts:147:    expect(config.containerPort).toBeUndefined();
test/pipeline/build-deploy-config.test.ts:92:      containerPort: undefined,
src/db/migration.ts:72:  if (!colNames.has('container_port')) {
src/db/migration.ts:73:    sqlite.exec('ALTER TABLE projects ADD COLUMN container_port INTEGER DEFAULT NULL');
src/db/migration.ts:91:    container_port INTEGER,
src/db/migration.ts:103:  if (!environmentColNames.has('container_port')) {
src/db/migration.ts:104:    sqlite.exec('ALTER TABLE environments ADD COLUMN container_port INTEGER');
src/db/migration.ts:105:    // Backfill container_port from projects table for existing environments
src/db/migration.ts:107:      'UPDATE environments SET container_port = (SELECT container_port FROM projects WHERE id = environments.project_id) WHERE container_port IS NULL',
src/db/index.ts:121:  createProject(project: Parameters<ProjectRepo['createProject']>[0]): ProjectRow { const created = this.projectRepo.createProject(project); this.environmentRepo.createEnvironment({ id: `${project.id}-production`, projectId: created.id, type: 'production', branch: project.branch ?? 'main' }); return created; }
src/db/index.ts:142:  createEnvironment(environment: Parameters<EnvironmentRepo['createEnvironment']>[0]) { return this.environmentRepo.createEnvironment(environment); }
src/db/index.ts:145:  updateEnvironment(id: string, updates: Parameters<EnvironmentRepo['updateEnvironment']>[1]) { this.environmentRepo.updateEnvironment(id, updates); }
test/pipeline/performance-baseline.test.ts:103:    db.createEnvironment({
test/pipeline/performance-baseline.test.ts:159:    db.createEnvironment({
test/pipeline/deploy-config-integration.test.ts:32:  db.createEnvironment({
src/db/schema.ts:27:   container_port INTEGER DEFAULT NULL,
src/db/schema.drizzle.ts:41:    container_port: integer('container_port'),
src/db/schema.drizzle.ts:85:    container_port: integer('container_port'),
src/pipeline/build-deploy-config.ts:56:    containerPort: project.container_port ?? undefined,
src/pipeline/recover.ts:148:    const containerPort = getServiceContainerPort(service);
src/pipeline/recover.ts:175:        [`${String(containerPort)}/tcp`]: {},
src/pipeline/recover.ts:187:          [`${String(containerPort)}/tcp`]: [{ HostPort: String(service.port) }],
src/pipeline/recover.ts:275:    const containerPort = project.container_port ?? port;
src/pipeline/recover.ts:288:    const traefikLabels = buildTraefikLabels(project.name, containerPort, undefined, 'production');
src/pipeline/recover.ts:298:      containerPort,
src/pipeline/deploy-core.ts:110:  containerPort?: number;
src/pipeline/deploy-core.ts:413:              containerPort: config.containerPort,
src/pipeline/deploy-core.ts:442:            containerPort: config.containerPort,
src/pipeline/deploy-core.ts:491:      this.db.updateEnvironment(env.id, { status: 'error' });
src/pipeline/deploy-core.ts:559:              containerPort: config.containerPort,
src/pipeline/deploy-core.ts:573:              containerPort: config.containerPort,
src/pipeline/deploy-core.ts:582:        containerPort: config.containerPort,
src/pipeline/deploy-core.ts:617:      targetEnvironment = this.db.createEnvironment({
src/pipeline/deploy-core.ts:702:    this.db.updateEnvironment(environmentId, {
src/pipeline/deploy-core.ts:772:        if (!deployConfig.containerPort) {
src/pipeline/deploy-core.ts:775:            deployConfig.containerPort = exposedPort;
src/pipeline/deploy-core.ts:895:      this.db.updateEnvironment(environmentId, { status: 'error' });
src/pipeline/deploy-core.ts:1368:        this.db.updateEnvironment(env.id, {
src/pipeline/deploy-core.ts:1469:        this.db.updateEnvironment(prodEnv.id, { status: 'building' });
src/pipeline/deploy-core.ts:1514:      const containerPort = (await this.docker.getImageExposedPort(imageTag)) ?? newPort;
src/pipeline/deploy-core.ts:1523:        containerPort,
src/pipeline/deploy-core.ts:1569:        containerPort,
src/pipeline/deploy-core.ts:1574:        this.db.updateEnvironment(prodEnv.id, {
src/pipeline/deploy-core.ts:1578:          containerPort,
src/pipeline/deploy-core.ts:1647:            this.db.updateEnvironment(environmentId, { status: 'running' });
src/pipeline/deploy-core.ts:1655:            this.db.updateEnvironment(environmentId, { status: 'error' });
src/pipeline/deploy-core.ts:1842:      this.db.updateEnvironment(environmentId, { status: 'stopped' });
src/pipeline/deploy-core.ts:1876:      this.db.updateEnvironment(environmentId, { status: 'running' });
src/pipeline/docker.ts:40:  containerPort?: number;
src/pipeline/docker.ts:54:  containerPort?: number;
src/pipeline/docker.ts:431:    const cPort = options.containerPort ?? options.port;
src/pipeline/docker.ts:483:    const cPort = opts.containerPort ?? opts.port;
src/pipeline/preview.ts:111:      const containerPort = (await this.docker.getImageExposedPort(imageTag)) ?? port;
src/pipeline/preview.ts:112:      const traefikLabels = buildTraefikLabels(previewName, containerPort);
src/pipeline/preview.ts:118:        containerPort,
test/pipeline/deploy/lifecycle.test.ts:162:    db.createEnvironment({
src/pipeline/traefik.ts:422:  containerPort: number,
src/pipeline/traefik.ts:435:    [`traefik.http.services.${routerName}.loadbalancer.server.port`]: String(containerPort),
test/pipeline/deploy/rollback.test.ts:56:    db.createEnvironment({
test/pipeline/deploy/rollback.test.ts:208:  it('persists containerPort to project and environment on rollback', async () => {
test/pipeline/deploy/rollback.test.ts:222:    db.updateEnvironment('p-port-production', {
test/pipeline/deploy/rollback.test.ts:237:    expect(project?.container_port).toBe(8080);
test/pipeline/deploy/rollback.test.ts:240:    expect(environment?.container_port).toBe(8080);
src/pipeline/service-manager.ts:449:    const containerPort = port;
src/pipeline/service-manager.ts:491:        [`${String(containerPort)}/tcp`]: {},
src/pipeline/service-manager.ts:503:          [`${String(containerPort)}/tcp`]: [{ HostPort: String(hostPort) }],
src/db/repos/environment.repo.ts:14:  createEnvironment(environment: {
src/db/repos/environment.repo.ts:62:  updateEnvironment(
src/db/repos/environment.repo.ts:72:      containerPort: number | null;
src/db/repos/environment.repo.ts:98:    if (updates.containerPort !== undefined) {
src/db/repos/environment.repo.ts:99:      setValues.container_port = updates.containerPort;
test/pipeline/deploy/run-step.test.ts:58:        containerPort: 12001,
test/pipeline/deploy/run-step.test.ts:71:  it('uses custom containerPort when provided', async () => {
test/pipeline/deploy/run-step.test.ts:82:      containerPort: 8080,
test/pipeline/deploy/run-step.test.ts:87:        containerPort: 8080,
src/pipeline/cloudflare.ts:173:    containerPort: number,
src/pipeline/cloudflare.ts:176:    const labels = buildTraefikLabels(projectName, containerPort, domains[0]);
test/pipeline/resolve-env.test.ts:30:  db.createEnvironment({
src/pipeline/compose.ts:625:      { containerId: string; port: number; containerPort: number }
src/pipeline/compose.ts:735:            const containerPort = parsedPort?.containerPort ?? hostPort;
src/pipeline/compose.ts:737:            const traefikLabels = buildTraefikLabels(routeName, containerPort, undefined, envType);
src/pipeline/compose.ts:748:                  containerPort,
src/pipeline/compose.ts:785:              containerPort,
src/pipeline/compose.ts:795:            buildLog += `[compose run ${service.name}] ${containerId.slice(0, 12)} ${String(hostPort)}:${String(containerPort)}\n`;
src/pipeline/compose.ts:899:            ports: [`${String(deployment.port)}:${String(deployment.containerPort)}`],
src/pipeline/compose.ts:1209:  ): { hostPort: number | null; containerPort: number } | null {
src/pipeline/compose.ts:1372:): { hostPort: number | null; containerPort: number } | null {
src/pipeline/compose.ts:1394:    const containerPort = Number(numericTokens[0]);
src/pipeline/compose.ts:1395:    if (!Number.isFinite(containerPort)) {
src/pipeline/compose.ts:1398:    return { hostPort: null, containerPort };
src/pipeline/compose.ts:1401:  const containerPort = Number(numericTokens[numericTokens.length - 1]);
src/pipeline/compose.ts:1403:  if (!Number.isFinite(containerPort) || !Number.isFinite(hostPort)) {
src/pipeline/compose.ts:1407:  return { hostPort, containerPort };
src/pipeline/deploy-plan/engine.ts:44:  containerPort?: number;
src/pipeline/deploy-plan/engine.ts:62:  containerPort?: number;
src/pipeline/deploy-plan/engine.ts:328:      containerPort: opts.containerPort,
src/pipeline/deploy-plan/engine.ts:337:      execution.containerPort === undefined
src/pipeline/deploy-plan/engine.ts:891:          ...(execution.containerPort !== undefined
src/pipeline/deploy-plan/engine.ts:892:            ? { containerPort: execution.containerPort }
src/pipeline/deploy/run-step.ts:18:  containerPort?: number;
src/pipeline/deploy/run-step.ts:45:      const configuredContainerPort = config.containerPort;
src/pipeline/deploy/run-step.ts:46:      const containerPort =
src/pipeline/deploy/run-step.ts:54:        containerPort,
src/pipeline/deploy/run-step.ts:64:          containerPort,
src/db/repos/project.repo.ts:28:    containerPort?: number;
src/db/repos/project.repo.ts:45:          container_port: project.containerPort ?? null,
src/db/repos/project.repo.ts:110:      containerPort: number | null;
src/db/repos/project.repo.ts:166:    if (updates.containerPort !== undefined) {
src/db/repos/project.repo.ts:167:      setValues.container_port = updates.containerPort;
src/pipeline/deploy/monorepo-orchestrator.ts:163:      containerPort: childContainerPort,
src/pipeline/deploy/lifecycle.ts:53:          this.db.updateEnvironment(env.id, { status: 'error' });
src/pipeline/deploy/lifecycle.ts:62:      this.db.updateEnvironment(env.id, { status: 'running' });
src/pipeline/deploy/lifecycle.ts:101:      this.db.updateEnvironment(env.id, { status: 'stopped' });
src/pipeline/deploy/orchestrator.ts:466:  const containerPort =
src/pipeline/deploy/orchestrator.ts:467:    config.containerPort ??
src/pipeline/deploy/orchestrator.ts:486:    containerPort,
src/pipeline/deploy/orchestrator.ts:519:    deps.db.updateEnvironment(environmentId, {
src/pipeline/deploy/orchestrator.ts:578:  deps.db.updateEnvironment(environmentId, {
src/pipeline/deploy/orchestrator.ts:592:      ...(containerPort != null ? { containerPort } : {}),
src/pipeline/deploy/rollback.ts:103:      const containerPort = (await this.docker.getImageExposedPort(rollbackImageTag)) ?? port;
src/pipeline/deploy/rollback.ts:110:        containerPort,
src/pipeline/deploy/rollback.ts:112:        traefikLabels: buildTraefikLabels(project.name, containerPort, undefined, envType),
src/pipeline/deploy/rollback.ts:119:        containerPort,
src/pipeline/deploy/rollback.ts:126:        this.db.updateEnvironment(productionEnvironment.id, {
src/pipeline/deploy/rollback.ts:132:          containerPort,
src/pipeline/deploy/rollback.ts:179:        this.db.updateEnvironment(productionEnvironment.id, { status: 'error' });

exec
/bin/bash -lc "rg -n \"createDeployLog\\(|getLastDeployLog\\(|created_at\" src/db src/tools/defs test | sed -n '1,240p'" in /home/lee/OpenLander
 succeeded in 0ms:
src/db/types.ts:27:  created_at: string;
src/db/types.ts:50:  created_at: string;
src/db/types.ts:66:  created_at: string;
src/db/types.ts:80:  created_at: string;
src/db/types.ts:90:  created_at: string;
src/db/types.ts:103:  created_at: string;
src/db/types.ts:114:  created_at: string;
src/db/types.ts:128:  created_at: string;
src/db/types.ts:138:  created_at: string;
src/db/types.ts:154:  created_at: string;
src/db/types.ts:162:  created_at: string;
src/db/types.ts:185:  created_at: string;
src/db/types.ts:197:  session_created_at: number | null;
src/db/types.ts:218:  created_at: string;
src/db/types.ts:243:  created_at: string;
src/db/types.ts:254:  created_at: number;
src/db/types.ts:273:  created_at: number;
src/db/types.ts:296:  created_at: string;
test/ops-drift.test.ts:21:    created_at: partial?.created_at ?? '2026-01-01T00:00:00.000Z',
src/db/migration.ts:92:    created_at TEXT DEFAULT CURRENT_TIMESTAMP,
src/db/migration.ts:132:      created_at TEXT DEFAULT CURRENT_TIMESTAMP
src/db/migration.ts:140:      created_at
src/db/migration.ts:147:      created_at
src/db/migration.ts:233:    created_at TEXT DEFAULT CURRENT_TIMESTAMP,
src/db/migration.ts:249:    created_at TEXT DEFAULT CURRENT_TIMESTAMP,
src/db/migration.ts:265:    created_at TEXT DEFAULT CURRENT_TIMESTAMP
src/db/migration.ts:268:    'CREATE INDEX IF NOT EXISTS idx_timeline_project ON timeline_events(project_id, created_at)',
src/db/migration.ts:297:      created_at TEXT DEFAULT CURRENT_TIMESTAMP,
src/db/migration.ts:311:      created_at,
src/db/migration.ts:324:      created_at,
src/db/migration.ts:339:    created_at TEXT DEFAULT CURRENT_TIMESTAMP,
src/db/migration.ts:361:    created_at TEXT DEFAULT CURRENT_TIMESTAMP,
src/db/migration.ts:369:  sqlite.exec('CREATE INDEX IF NOT EXISTS idx_deploy_plans_created_at ON deploy_plans(created_at)');
src/db/migration.ts:376:    created_at TEXT DEFAULT CURRENT_TIMESTAMP,
src/db/migration.ts:387:    created_at TEXT DEFAULT CURRENT_TIMESTAMP,
src/db/migration.ts:401:    session_created_at INTEGER,
src/db/migration.ts:424:    created_at TEXT NOT NULL
src/db/migration.ts:427:  sqlite.exec('CREATE INDEX IF NOT EXISTS idx_ai_usage_log_created_at ON ai_usage_log(created_at)');
src/db/migration.ts:443:    created_at TEXT NOT NULL
src/db/migration.ts:467:      created_at TEXT NOT NULL
src/db/migration.ts:482:      created_at
src/db/migration.ts:496:      created_at
src/db/migration.ts:537:  sqlite.exec('CREATE INDEX IF NOT EXISTS idx_action_runs_created_at ON action_runs(created_at)');
src/db/migration.ts:548:     created_at TEXT NOT NULL DEFAULT ''
src/db/migration.ts:566:    created_at INTEGER NOT NULL,
src/db/migration.ts:580:    created_at INTEGER NOT NULL
src/db/migration.ts:602:      created_at INTEGER NOT NULL
src/db/migration.ts:605:      'INSERT INTO ops_incident_events_migrated SELECT id, incident_id, event_type, description, metadata, created_at FROM ops_incident_events',
src/db/migration.ts:631:    created_at TEXT NOT NULL DEFAULT ''
src/db/migration.ts:655:    created_at TEXT NOT NULL
src/db/migration.ts:657:  sqlite.exec('CREATE INDEX IF NOT EXISTS idx_activity_log_created_at ON activity_log(created_at)');
src/db/migration.ts:662:    'CREATE INDEX IF NOT EXISTS idx_activity_log_project_created ON activity_log(project_id, created_at)',
src/db/migration.ts:665:    'CREATE INDEX IF NOT EXISTS idx_activity_log_type_created ON activity_log(activity_type, created_at)',
src/db/index.ts:180:  createDeployLog(log: Parameters<DeployLogRepo['createDeployLog']>[0]) { this.deployLogRepo.createDeployLog(log); }
src/db/index.ts:182:  getLastDeployLog(projectId: string, environmentId?: string) { return this.deployLogRepo.getLastDeployLog(projectId, environmentId); }
test/alerts.test.ts:39:    created_at: partial?.created_at ?? '2026-01-01T00:00:00.000Z',
src/tools/defs/debug.ts:10:      'Get the raw build log for a project deployment. Returns the full unprocessed build output. Use this instead of debug_build_error when you need to parse the log yourself. Returns { status, build_log, duration_ms, created_at }. Errors: PROJECT_NOT_FOUND, NO_DEPLOY_LOGS.',
src/tools/defs/debug.ts:35:        created_at: log.created_at,
src/tools/defs/debug.ts:61:      const lastDeploy = appCtx.db.getLastDeployLog(project.id);
src/db/schema.ts:29:  created_at TEXT DEFAULT CURRENT_TIMESTAMP,
src/db/schema.ts:50:  created_at TEXT DEFAULT CURRENT_TIMESTAMP,
src/db/schema.ts:61:  created_at TEXT DEFAULT CURRENT_TIMESTAMP,
src/db/schema.ts:75:  created_at TEXT DEFAULT CURRENT_TIMESTAMP
src/db/schema.ts:89:  created_at TEXT DEFAULT CURRENT_TIMESTAMP
src/db/schema.ts:99:  created_at TEXT DEFAULT CURRENT_TIMESTAMP
src/db/schema.ts:113:  created_at TEXT DEFAULT CURRENT_TIMESTAMP,
src/db/schema.ts:124:  created_at TEXT DEFAULT CURRENT_TIMESTAMP,
src/db/schema.ts:134:  created_at TEXT DEFAULT CURRENT_TIMESTAMP,
src/db/schema.ts:149:   created_at TEXT DEFAULT CURRENT_TIMESTAMP,
src/db/schema.ts:159:   created_at TEXT NOT NULL DEFAULT (datetime('now')),
src/db/schema.ts:176:  created_at TEXT NOT NULL DEFAULT (datetime('now'))
src/db/schema.ts:184:  created_at TEXT DEFAULT CURRENT_TIMESTAMP,
src/db/schema.ts:193:CREATE INDEX IF NOT EXISTS idx_timeline_project ON timeline_events(project_id, created_at);
src/db/schema.ts:211:  created_at TEXT DEFAULT CURRENT_TIMESTAMP,
src/db/schema.ts:225:  session_created_at INTEGER,
src/db/schema.ts:237:  created_at TEXT NOT NULL DEFAULT ''
src/db/schema.drizzle.ts:43:    created_at: text('created_at').default(sql`CURRENT_TIMESTAMP`),
src/db/schema.drizzle.ts:86:    created_at: text('created_at').default(sql`CURRENT_TIMESTAMP`),
src/db/schema.drizzle.ts:112:    created_at: text('created_at').default(sql`CURRENT_TIMESTAMP`),
src/db/schema.drizzle.ts:139:    created_at: text('created_at').default(sql`CURRENT_TIMESTAMP`),
src/db/schema.drizzle.ts:164:    created_at: text('created_at').default(sql`CURRENT_TIMESTAMP`),
src/db/schema.drizzle.ts:166:  (table) => [index('idx_timeline_project').on(table.project_id, table.created_at)],
src/db/schema.drizzle.ts:180:    created_at: text('created_at').default(sql`CURRENT_TIMESTAMP`),
src/db/schema.drizzle.ts:200:    created_at: text('created_at').default(sql`CURRENT_TIMESTAMP`),
src/db/schema.drizzle.ts:217:    created_at: text('created_at').default(sql`CURRENT_TIMESTAMP`),
src/db/schema.drizzle.ts:238:    created_at: text('created_at').default(sql`CURRENT_TIMESTAMP`),
src/db/schema.drizzle.ts:257:    created_at: text('created_at').default(sql`CURRENT_TIMESTAMP`),
src/db/schema.drizzle.ts:282:    created_at: text('created_at')
src/db/schema.drizzle.ts:312:    created_at: text('created_at')
src/db/schema.drizzle.ts:330:  created_at: text('created_at').default(sql`CURRENT_TIMESTAMP`),
src/db/schema.drizzle.ts:341:  created_at: text('created_at').default(sql`CURRENT_TIMESTAMP`),
src/db/schema.drizzle.ts:354:    created_at: text('created_at').default(sql`CURRENT_TIMESTAMP`),
src/db/schema.drizzle.ts:374:    created_at: text('created_at').default(sql`CURRENT_TIMESTAMP`),
src/db/schema.drizzle.ts:381:    index('idx_deploy_plans_created_at').on(table.created_at),
src/db/schema.drizzle.ts:393:    session_created_at: integer('session_created_at'),
src/db/schema.drizzle.ts:420:    created_at: text('created_at').notNull().default(''),
src/db/schema.drizzle.ts:424:    index('idx_ai_usage_log_created_at').on(table.created_at),
src/db/schema.drizzle.ts:458:    created_at: text('created_at').notNull().default(''),
src/db/schema.drizzle.ts:463:    index('idx_action_runs_created_at').on(table.created_at),
src/db/schema.drizzle.ts:478:    created_at: text('created_at').notNull().default(''),
src/db/schema.drizzle.ts:498:    created_at: integer('created_at').notNull(),
src/db/schema.drizzle.ts:535:    created_at: integer('created_at').notNull(),
src/db/schema.drizzle.ts:578:    created_at: text('created_at').notNull().default(''),
src/db/schema.drizzle.ts:615:    created_at: text('created_at').notNull(),
src/db/schema.drizzle.ts:618:    index('idx_activity_log_created_at').on(table.created_at),
src/db/schema.drizzle.ts:620:    index('idx_activity_log_project_created').on(table.project_id, table.created_at),
src/db/schema.drizzle.ts:621:    index('idx_activity_log_type_created').on(table.activity_type, table.created_at),
src/tools/defs/deploy.ts:443:              const lastLog = appCtx.db.getLastDeployLog(project.id);
src/tools/defs/deploy.ts:445:                lastLog && new Date(lastLog.created_at).getTime() > current.startedAt.getTime();
src/tools/defs/deploy.ts:458:                      startedAt: new Date(lastLog.created_at),
src/tools/defs/deploy.ts:459:                      completedAt: new Date(lastLog.created_at),
src/tools/defs/deploy.ts:512:            const lastLog = appCtx.db.getLastDeployLog(project.id);
src/tools/defs/deploy.ts:520:              new Date(lastLog.created_at).getTime() > currentJob.startedAt.getTime();
src/tools/defs/deploy.ts:523:              resolveFromDeployLog(lastLog.created_at);
src/tools/defs/deploy.ts:631:          created_at: log.created_at,
src/db/repos/action-run.repo.ts:43:        created_at: now,
src/db/repos/action-run.repo.ts:144:      .orderBy(desc(actionRuns.created_at))
src/db/repos/action-run.repo.ts:162:      .orderBy(desc(actionRuns.created_at))
src/db/repos/action-run.repo.ts:174:      .orderBy(desc(actionRuns.created_at));
src/db/repos/action-run.repo.ts:184:   * Find recent action runs globally, ordered by created_at DESC.
src/db/repos/action-run.repo.ts:190:      .orderBy(desc(actionRuns.created_at))
src/tools/defs/service.ts:144:            createdAt: service.created_at,
src/tools/defs/service.ts:356:        createdAt: service.created_at,
src/db/repos/activity-log.repo.ts:63:    created_at?: string;
src/db/repos/activity-log.repo.ts:66:    const now = entry.created_at ?? new Date().toISOString();
src/db/repos/activity-log.repo.ts:81:        created_at: now,
src/db/repos/activity-log.repo.ts:110:    const conditions = [between(activityLog.created_at, from, to)];
src/db/repos/activity-log.repo.ts:203:    const result = this.db.delete(activityLog).where(lt(activityLog.created_at, isoDate)).run();
src/tools/defs/project-ops.ts:142:            createdAt: project.created_at,
src/db/repos/environment.repo.ts:58:      .orderBy(asc(environments.created_at))
test/service-manager-extended.test.ts:38:    created_at: partial.created_at ?? '2026-01-01T00:00:00.000Z',
test/deploy-plan/internal-url.test.ts:39:        created_at: new Date().toISOString(),
test/deploy-plan/internal-url.test.ts:82:        created_at: new Date().toISOString(),
test/deploy-plan/internal-url.test.ts:161:        created_at: new Date().toISOString(),
test/deploy-plan/internal-url.test.ts:207:        created_at: new Date().toISOString(),
src/db/repos/project-dependency.repo.ts:17:  create(data: Omit<NewProjectDependency, 'id' | 'created_at'>): ProjectDependencyRow {
src/db/repos/project-dependency.repo.ts:29:        created_at: now,
src/db/repos/project-dependency.repo.ts:112:          created_at: new Date().toISOString(),
src/db/repos/ai-usage-log.repo.ts:22:   * Generates UUID and sets created_at timestamp.
src/db/repos/ai-usage-log.repo.ts:24:  create(data: Omit<AiUsageLogRow, 'id' | 'created_at'>): string {
src/db/repos/ai-usage-log.repo.ts:47:        created_at: createdAt,
src/db/repos/ai-usage-log.repo.ts:62:      .orderBy(desc(aiUsageLog.created_at))
src/db/repos/ai-usage-log.repo.ts:76:      .where(and(gte(aiUsageLog.created_at, fromIso), lte(aiUsageLog.created_at, toIso)))
src/db/repos/ai-usage-log.repo.ts:77:      .orderBy(desc(aiUsageLog.created_at))
src/db/repos/ai-usage-log.repo.ts:88:      .orderBy(desc(aiUsageLog.created_at))
src/db/repos/ai-usage-log.repo.ts:177:      conditions.push(gte(aiUsageLog.created_at, opts.from.toISOString()));
src/db/repos/ai-usage-log.repo.ts:181:      conditions.push(lte(aiUsageLog.created_at, opts.to.toISOString()));
src/db/repos/runtime-incident.repo.ts:61:        .orderBy(desc(runtimeIncidents.created_at))
src/db/repos/runtime-incident.repo.ts:74:      .orderBy(desc(runtimeIncidents.created_at))
src/db/repos/runtime-incident.repo.ts:83:      .orderBy(desc(runtimeIncidents.created_at))
src/db/repos/service-connection.repo.ts:65:      .orderBy(desc(serviceConnections.created_at))
src/db/repos/service-connection.repo.ts:74:      .orderBy(desc(serviceConnections.created_at))
test/app.test.ts:30:    created_at: new Date().toISOString(),
src/db/repos/ops-incident.repo.ts:33:        created_at: Date.now(),
src/db/repos/ops-incident.repo.ts:53:      .orderBy(desc(opsIncidents.created_at));
src/db/repos/ops-incident.repo.ts:72:      .orderBy(desc(opsIncidents.created_at))
src/db/repos/ops-incident.repo.ts:81:      .orderBy(desc(opsIncidents.created_at))
src/db/repos/ops-incident.repo.ts:86:    const conditions = [gte(opsIncidents.created_at, from), lte(opsIncidents.created_at, to)];
src/db/repos/ops-incident.repo.ts:99:      .orderBy(desc(opsIncidents.created_at))
src/db/repos/ops-incident.repo.ts:114:      .orderBy(desc(opsIncidents.created_at));
src/db/repos/deploy-plan.repo.ts:111:        .orderBy(desc(deployPlans.created_at))
src/db/repos/deploy-plan.repo.ts:117:      .orderBy(desc(deployPlans.created_at))
src/db/repos/deploy-plan.repo.ts:126:      .orderBy(desc(deployPlans.created_at), desc(sql`rowid`))
src/db/repos/global-secret.repo.ts:22:    created_at: string | null;
test/infra-analyzer.test.ts:22:    created_at: partial.created_at ?? '2026-01-01T00:00:00.000Z',
test/webhook.test.ts:61:        created_at: new Date().toISOString(),
src/db/repos/deploy-log.repo.ts:15:  createDeployLog(log: {
src/db/repos/deploy-log.repo.ts:58:  getLastDeployLog(projectId: string, environmentId?: string): DeployLogRow | undefined {
src/db/repos/ops-incident-event.repo.ts:37:        created_at: Date.now(),
src/db/repos/ops-incident-event.repo.ts:56:      .orderBy(asc(opsIncidentEvents.created_at))
src/db/repos/ops-incident-event.repo.ts:66:      .orderBy(asc(opsIncidentEvents.created_at))
src/db/repos/timeline.repo.ts:39:        created_at: event.createdAt ?? new Date().toISOString(),
src/db/repos/timeline.repo.ts:52:             ORDER BY datetime(created_at) DESC, rowid DESC
src/db/repos/timeline.repo.ts:64:      .orderBy(desc(sql`datetime(${timelineEvents.created_at})`), desc(sql`rowid`))
test/tool-registry.test.ts:67:    created_at: string;
test/tool-registry.test.ts:213:          created_at: '2026-01-01 00:00:00',
test/tool-registry.test.ts:305:      created_at: '2026-01-01 00:00:00',
test/tool-registry.test.ts:354:      created_at: '2026-01-01 00:00:00',
test/tool-registry.test.ts:387:      created_at: '2026-01-01 00:00:00',
test/db.test.ts:219:      db.createDeployLog({
test/db.test.ts:235:      db.createDeployLog({ id: 'dl1', projectId: 'p1', status: 'failed', trigger: 'api' });
test/db.test.ts:236:      db.createDeployLog({ id: 'dl2', projectId: 'p1', status: 'success', trigger: 'chat' });
test/db.test.ts:238:      const last = db.getLastDeployLog('p1');
test/db.test.ts:257:        created_at TEXT DEFAULT CURRENT_TIMESTAMP
test/db.test.ts:268:      db.createDeployLog({
src/db/repos/auth.repo.ts:55:      row.session_created_at === null ||
src/db/repos/auth.repo.ts:62:      createdAt: row.session_created_at,
src/db/repos/auth.repo.ts:74:          session_created_at: createdAt,
src/db/repos/auth.repo.ts:87:          session_created_at: createdAt,
src/db/repos/auth.repo.ts:99:        session_created_at: null,
src/db/repos/domain-mapping.repo.ts:51:        created_at: domainMappings.created_at,
src/db/repos/domain-mapping.repo.ts:56:      .orderBy(desc(domainMappings.created_at))
test/web-routes.test.ts:147:    db.createDeployLog({
test/web-routes.test.ts:954:        created_at: '2026-01-01T00:00:00.000Z',
src/db/repos/deployment-pattern.repo.ts:22:      .orderBy(desc(deploymentPatterns.created_at))
src/db/repos/deployment-pattern.repo.ts:82:        created_at: createdAt,
test/environments.test.ts:133:        created_at TEXT DEFAULT CURRENT_TIMESTAMP,
test/environments.test.ts:142:        created_at TEXT DEFAULT CURRENT_TIMESTAMP,
test/service-manager-backup.test.ts:52:    created_at: partial.created_at ?? '2026-01-01T00:00:00.000Z',
test/auth/auth-service.test.ts:43:      session_created_at: this.session?.createdAt ?? null,
test/helpers/deploy-plan-mocks.ts:73:    created_at: now,
test/fixtures/deploy-plan/simple-plan.json:31:  "created_at": "2025-03-19T10:00:00Z",
test/helpers/web-route-mocks.ts:117:    created_at: new Date().toISOString(),
test/llm/context-assembler.test.ts:40:          created_at: null,
test/llm/context-assembler.test.ts:49:          created_at: null,
test/llm/context-assembler.test.ts:154:          created_at: '2025-01-01T00:00:00Z',
test/llm/context-assembler.test.ts:169:          created_at: '2025-01-01T00:00:00Z',
test/llm/context-assembler.test.ts:184:          created_at: '2025-01-01T00:00:00Z',
test/llm/context-assembler.test.ts:199:          created_at: '2025-01-01T00:00:00Z',
test/llm/context-assembler.test.ts:214:          created_at: '2025-01-01T00:00:00Z',
test/llm/context-assembler.test.ts:229:          created_at: '2025-01-01T00:00:00Z',
test/llm/context-assembler.test.ts:341:              created_at: '2025-01-15T10:00:00Z',
test/llm/context-assembler.test.ts:349:              created_at: '2025-01-14T09:00:00Z',
test/llm/context-assembler.test.ts:370:          created_at: '2025-01-14T09:00:00Z',
test/llm/context-assembler.test.ts:395:          created_at: '2025-01-15T10:00:00Z',
test/llm/context-assembler.test.ts:438:          created_at: '2026-01-01',
test/llm/context-assembler.test.ts:565:        created_at: '2026-01-01',
test/fixtures/deploy-plan/needs-input-plan.json:45:  "created_at": "2025-03-19T13:00:00Z",
test/fixtures/deploy-plan/complex-plan.json:56:  "created_at": "2025-03-19T12:00:00Z",
test/monitor/ops-incidents.test.ts:20:    created_at: Date.now(),
test/monitor/ops-incidents.test.ts:74:        created_at: Date.now() - 5 * 60 * 1000,
test/monitor/ops-incidents.test.ts:112:        created_at: Date.now() - 5 * 60 * 1000,
test/monitor/ops-incidents.test.ts:134:        created_at: Date.now() - 31 * 60 * 1000,
test/monitor/ops-incidents.test.ts:259:          created_at: Date.now(),
test/fixtures/deploy-plan/standard-plan.json:40:  "created_at": "2025-03-19T11:00:00Z",
test/deploy-lock.test.ts:292:      db.createDeployLog({
test/health-monitor.test.ts:25:    created_at: partial.created_at ?? '2026-01-01T00:00:00.000Z',
test/volume-minio.test.ts:51:    created_at: partial.created_at ?? '2026-01-01T00:00:00.000Z',
test/db/repos/action-run.repo.test.ts:285:    it('returns all action runs for a project ordered by created_at descending', () => {
test/mcp-service-tools.test.ts:27:    created_at: partial.created_at ?? '2026-01-01T00:00:00.000Z',
test/deploy-plan-db.test.ts:44:      expect(created.created_at).toBeDefined();
test/deploy-plan-db.test.ts:237:    it('lists all plans ordered by created_at desc', () => {
test/deploy-plan-db.test.ts:348:    it('sets created_at and updated_at on creation', () => {
test/deploy-plan-db.test.ts:356:      expect(created.created_at).toBeDefined();
test/deploy-plan-db.test.ts:358:      expect(created.created_at).toBeTruthy();
test/deploy-plan-db.test.ts:362:    it('keeps created_at unchanged on update', () => {
test/deploy-plan-db.test.ts:370:      const originalCreatedAt = created.created_at;
test/deploy-plan-db.test.ts:381:      expect(updated!.created_at).toBe(originalCreatedAt);
test/deploy-plan-types.test.ts:63:      created_at: new Date().toISOString(),
test/deploy-plan-types.test.ts:130:      created_at: new Date().toISOString(),

exec
/bin/bash -lc "sed -n '1,110p' src/db/repos/deploy-log.repo.ts" in /home/lee/OpenLander
 succeeded in 0ms:
import { and, desc, eq, sql } from 'drizzle-orm';

import type { DrizzleClient, SqliteDatabase } from '../drizzle.js';
import { deployLogs } from '../schema.drizzle.js';
import type { DeployLogRow } from '../types.js';

export class DeployLogRepo {
  constructor(
    private readonly db: DrizzleClient,
    private readonly sqlite: SqliteDatabase,
  ) {
    void this.sqlite;
  }

  createDeployLog(log: {
    id: string;
    projectId: string;
    environmentId?: string;
    status: DeployLogRow['status'];
    trigger: DeployLogRow['trigger'];
    triggerDetail?: string;
    commitSha?: string;
    commitMessage?: string;
    buildLog?: string;
    durationMs?: number;
  }): void {
    this.db
      .insert(deployLogs)
      .values({
        id: log.id,
        project_id: log.projectId,
        environment_id: log.environmentId ?? null,
        status: log.status,
        trigger: log.trigger,
        trigger_detail: log.triggerDetail ?? null,
        commit_sha: log.commitSha ?? null,
        commit_message: log.commitMessage ?? null,
        build_log: log.buildLog ?? null,
        duration_ms: log.durationMs ?? null,
      })
      .run();
  }

  getDeployLogs(projectId: string, limit = 20, environmentId?: string): DeployLogRow[] {
    const whereClause = environmentId
      ? and(eq(deployLogs.project_id, projectId), eq(deployLogs.environment_id, environmentId))
      : eq(deployLogs.project_id, projectId);

    return this.db
      .select()
      .from(deployLogs)
      .where(whereClause)
      .orderBy(desc(sql`rowid`))
      .limit(limit)
      .all() as DeployLogRow[];
  }

  getLastDeployLog(projectId: string, environmentId?: string): DeployLogRow | undefined {
    const whereClause = environmentId
      ? and(eq(deployLogs.project_id, projectId), eq(deployLogs.environment_id, environmentId))
      : eq(deployLogs.project_id, projectId);

    return this.db
      .select()
      .from(deployLogs)
      .where(whereClause)
      .orderBy(desc(sql`rowid`))
      .limit(1)
      .get() as DeployLogRow | undefined;
  }

  updateRuntimeLog(deployId: string, runtimeLog: string): void {
    this.db
      .update(deployLogs)
      .set({ runtime_log: runtimeLog })
      .where(eq(deployLogs.id, deployId))
      .run();
  }

  getDeployLog(deployId: string): DeployLogRow | undefined {
    return this.db.select().from(deployLogs).where(eq(deployLogs.id, deployId)).get() as
      | DeployLogRow
      | undefined;
  }
}

exec
/bin/bash -lc "sed -n '600,740p' src/pipeline/deploy-core.ts" in /home/lee/OpenLander
 succeeded in 0ms:
            projectName,
            error: error.message,
            preflightWarnings: error.result.warnings,
            buildDurationMs: 0,
          };
        }
        throw error;
      }
    }

    const envType = 'production' as const;
    let targetEnvironment = this.db
      .getEnvironmentsByProject(projectId)
      .find((env) => env.type === envType);

    if (!targetEnvironment) {
      const project = this.db.getProject(projectId);
      targetEnvironment = this.db.createEnvironment({
        id: `${projectId}-${envType}`,
        projectId,
        type: envType,
        branch: config.branch ?? project?.branch ?? 'main',
      });
    }

    const result = await this.deployEnvironment(projectId, targetEnvironment.id, {
      ...config,
      _projectId: projectId,
      name: projectName,
      trigger,
    });
    if (preflightWarnings && result.preflightWarnings === undefined) {
      return { ...result, preflightWarnings };
    }
    return result;
  }

  async deployEnvironment(
    projectId: string,
    environmentId: string,
    config: Partial<ProjectConfig> = {},
  ): Promise<DeployResult> {
    const startTime = Date.now();
    const project = this.db.getProject(projectId);
    if (!project) {
      return {
        success: false,
        projectId,
        projectName: 'unknown',
        error: `Project not found: ${projectId}`,
        buildDurationMs: Date.now() - startTime,
      };
    }
    const environment = this.db.getEnvironment(environmentId);
    if (!environment || environment.project_id !== projectId) {
      return {
        success: false,
        projectId,
        projectName: project.name,
        error: `Environment not found: ${environmentId}`,
        buildDurationMs: Date.now() - startTime,
      };
    }
    const deployConfig: Partial<ProjectConfig> = { ...config };
    const projectName = deployConfig.name ?? project.name;
    const trigger = deployConfig.trigger ?? 'api';
    const source = deployConfig.source ?? 'git';
    const repoUrl = deployConfig.repoUrl ?? project.repo_url ?? '';
    if (source !== 'image' && !repoUrl) {
      return {
        success: false,
        projectId,
        projectName,
        error: `Missing repo URL for project: ${projectId}`,
        buildDurationMs: Date.now() - startTime,
      };
    }
    const routeName = getRouteName(projectName);
    const orchestrationDeps = this.createOrchestrationDeps();
    if (deployConfig.envVars) {
      this.db.mergeEnvVars(projectId, deployConfig.envVars);
    }
    if (environment.container_id) {
      try {
        const runtimeLog = await this.docker.getLogs(environment.container_id, 500);
        if (runtimeLog) {
          const lastLog = this.db.getLastDeployLog(projectId, environmentId);
          if (lastLog) {
            this.db.updateRuntimeLog(lastLog.id, runtimeLog);
          }
        }
      } catch {
        // Container may already be gone — best-effort capture
      }

      try {
        await this.docker.safeRemoveContainer(environment.container_id);
      } catch {
        // container may already be removed
      }
    }
    await eventBus.emit('deploy:start', { projectId, repoUrl });
    this.db.updateEnvironment(environmentId, {
      status: 'building',
      containerId: null,
      imageTag: null,
      assignedPort: null,
    });
    this.db.updateProject(projectId, {
      status: 'building',
      containerId: null,
      imageTag: null,
      assignedPort: null,
    });
    let buildLog = '';
    let clonePath = '';
    let diffContext: string | undefined;
    let commitSha: string | undefined;
    let commitMessage: string | undefined;
    let imageTag = `openlander/${routeName}:${String(Date.now())}`;
    const previousTag = `openlander/${routeName}:previous`;
    let preservedPreviousTag: string | null = null;
    if (source !== 'image') {
      const currentRunningTag = environment.image_tag ?? project.image_tag;
      if (currentRunningTag && currentRunningTag !== previousTag) {
        try {
          await this.docker.tagImage(currentRunningTag, `openlander/${routeName}`, 'previous');
          await this.markRollbackImage(previousTag);
          preservedPreviousTag = currentRunningTag;
        } catch (err) {
          if (!isDockerNotFoundError(err)) {
            log.warn({ err, currentRunningTag }, 'Failed to preserve previous image for rollback');
          }
        }
      } else if (currentRunningTag === previousTag) {
        preservedPreviousTag = currentRunningTag;
      }
    } else {
      const currentRunningTag = environment.image_tag ?? project.image_tag;
      if (currentRunningTag) {
        this.db.updateProject(projectId, { previousImageTag: currentRunningTag });

exec
/bin/bash -lc "rg --files src/pipeline | rg 'blueGreen|executePlan|rollback'" in /home/lee/OpenLander
 succeeded in 0ms:
src/pipeline/deploy/rollback.ts

codex
핵심 로직은 읽었습니다. 이제 관련 테스트를 실제로 돌려서, 구현 의도와 mock/타이머 동작이 어긋나는 부분이 있는지도 확인하겠습니다.
exec
/bin/bash -lc 'npm test -- test/deploy-lock.test.ts test/db.test.ts test/blue-green-port-conflict.test.ts test/pipeline/deploy/rollback.test.ts test/tools/deploy-lock-mcp-tools.test.ts test/tools/registry-critical-tools.test.ts' in /home/lee/OpenLander
exec
/bin/bash -lc 'npm run typecheck' in /home/lee/OpenLander
 succeeded in 1324ms:

 RUN  v4.1.0 /home/lee/OpenLander


 Test Files  6 passed (6)
      Tests  68 passed (68)
   Start at  18:39:57
   Duration  1.04s (transform 1.07s, setup 117ms, import 2.53s, tests 1.18s, environment 0ms)


 succeeded in 4128ms:
exec
/bin/bash -lc "sed -n '1,110p' src/db/schema.ts" in /home/lee/OpenLander
 succeeded in 0ms:
/**
 * Raw SQL schema for initial table creation (CREATE TABLE IF NOT EXISTS).
 * This matches the Drizzle schema in schema.drizzle.ts and is used only
 * for first-run initialization. Drizzle handles all query operations.
 */
export const SCHEMA = `
CREATE TABLE IF NOT EXISTS projects (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL UNIQUE,
  repo_url TEXT,
  branch TEXT DEFAULT 'main',
  status TEXT DEFAULT 'stopped' CHECK(status IN ('running', 'stopped', 'building', 'error')),
  visibility TEXT DEFAULT 'internal' CHECK(visibility IN ('internal', 'quick-share', 'shared', 'production')),
  assigned_port INTEGER UNIQUE,
  container_id TEXT,
  image_tag TEXT,
  previous_image_tag TEXT,
  public_url TEXT,
  parent_project_id TEXT REFERENCES projects(id) ON DELETE CASCADE,
  dockerfile_path TEXT DEFAULT 'Dockerfile',
  docker_target TEXT DEFAULT NULL,
  build_context TEXT DEFAULT NULL,
   build_method TEXT DEFAULT NULL CHECK(build_method IN ('dockerfile', 'compose')),
   source TEXT NOT NULL DEFAULT 'git' CHECK(source IN ('git', 'image')),
   image_url TEXT DEFAULT NULL,
   image_cmd TEXT DEFAULT NULL,
   container_port INTEGER DEFAULT NULL,
   pending_fix TEXT DEFAULT NULL,
  created_at TEXT DEFAULT CURRENT_TIMESTAMP,
  updated_at TEXT DEFAULT CURRENT_TIMESTAMP,
  deploy_lock_session TEXT,
  deploy_lock_at TEXT,
   access_code TEXT,
   access_code_iv TEXT,
   is_preview INTEGER DEFAULT 0 CHECK(is_preview IN (0, 1)),
    pr_number INTEGER
);

CREATE TABLE IF NOT EXISTS environments (
  id TEXT PRIMARY KEY,
  project_id TEXT NOT NULL REFERENCES projects(id) ON DELETE CASCADE,
  type TEXT NOT NULL CHECK(type IN ('production', 'development')),
  branch TEXT NOT NULL DEFAULT 'main',
  status TEXT DEFAULT 'idle' CHECK(status IN ('running', 'stopped', 'building', 'error', 'idle')),
  assigned_port INTEGER UNIQUE,
  container_id TEXT,
  image_tag TEXT,
  previous_image_tag TEXT,
  public_url TEXT,
  created_at TEXT DEFAULT CURRENT_TIMESTAMP,
  updated_at TEXT DEFAULT CURRENT_TIMESTAMP,
  UNIQUE(project_id, type)
);

CREATE TABLE IF NOT EXISTS env_vars (
  id TEXT PRIMARY KEY,
  project_id TEXT NOT NULL REFERENCES projects(id) ON DELETE CASCADE,
  environment_id TEXT REFERENCES environments(id) ON DELETE CASCADE,
  key TEXT NOT NULL,
  value TEXT NOT NULL,
  created_at TEXT DEFAULT CURRENT_TIMESTAMP,
  UNIQUE(project_id, key)
);

CREATE TABLE IF NOT EXISTS deploy_logs (
  id TEXT PRIMARY KEY,
  project_id TEXT NOT NULL REFERENCES projects(id) ON DELETE CASCADE,
  environment_id TEXT REFERENCES environments(id) ON DELETE CASCADE,
  status TEXT CHECK(status IN ('success', 'failed', 'cancelled')),
  trigger_source TEXT CHECK(trigger_source IN ('chat', 'webhook', 'api')),
  commit_sha TEXT,
  commit_message TEXT,
  build_log TEXT,
  duration_ms INTEGER,
  created_at TEXT DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS timeline_events (
  id TEXT PRIMARY KEY,
  project_id TEXT NOT NULL REFERENCES projects(id) ON DELETE CASCADE,
  deploy_id TEXT,
  type TEXT NOT NULL,
  message TEXT NOT NULL,
  detail TEXT,
  severity TEXT,
  percent INTEGER,
  tool_name TEXT,
  action_buttons TEXT,
  created_at TEXT DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS domain_mappings (
  id TEXT PRIMARY KEY,
  project_id TEXT NOT NULL REFERENCES projects(id) ON DELETE CASCADE,
  domain TEXT NOT NULL UNIQUE,
  cloudflare_zone_id TEXT,
  cloudflare_dns_record_id TEXT,
  status TEXT DEFAULT 'active' CHECK(status IN ('active', 'pending', 'error')),
  created_at TEXT DEFAULT CURRENT_TIMESTAMP
);


CREATE TABLE IF NOT EXISTS oauth_tokens (
  id TEXT PRIMARY KEY,
  provider TEXT NOT NULL UNIQUE,
  access_token TEXT NOT NULL,
  refresh_token TEXT,
  expires_at TEXT,
  token_type TEXT DEFAULT 'Bearer',
  auth_method TEXT DEFAULT 'manual',

codex
리뷰 코멘트에 정확한 위치를 달기 위해 관련 줄 번호를 뽑고 있습니다. 지금까지는 심각한 SQLi/타입 에러는 보이지 않았고, 주로 동시성 경계와 테스트 커버리지의 빈틈이 핵심입니다.
exec
/bin/bash -lc "nl -ba src/tools/defs/deploy.ts | sed -n '345,535p'" in /home/lee/OpenLander
 succeeded in 0ms:
   345	          return {
   346	            active: isActive ? 1 : 0,
   347	            jobs: status ? [formatJob(status)] : [],
   348	          };
   349	        };
   350
   351	        const buildLockedQueuedResult = (
   352	          currentLockInfo: { session: string; lockedAt: string },
   353	          timedOut = false,
   354	        ) => ({
   355	          active: 1,
   356	          locked: true,
   357	          lock_session: currentLockInfo.session,
   358	          jobs: [
   359	            formatJob({
   360	              projectId: project.id,
   361	              projectName: project.name,
   362	              phase: 'queued',
   363	              startedAt: new Date(currentLockInfo.lockedAt),
   364	            }),
   365	          ],
   366	          ...(timedOut ? { timeout: true } : {}),
   367	        });
   368
   369	        let status = appCtx.jobManager.getStatus(project.id);
   370	        const lockInfo = appCtx.db.getDeployLockInfo(project.id);
   371
   372	        if (!wait) {
   373	          const result = buildProjectResult(status) as Record<string, unknown>;
   374	          if (lockInfo) {
   375	            result['locked'] = true;
   376	            result['lock_session'] = lockInfo.session;
   377	          }
   378	          if (!status && lockInfo) {
   379	            result['active'] = 1;
   380	            result['jobs'] = [
   381	              formatJob({
   382	                projectId: project.id,
   383	                projectName: project.name,
   384	                phase: 'queued',
   385	                startedAt: new Date(lockInfo.lockedAt),
   386	              }),
   387	            ];
   388	          }
   389	          return result;
   390	        }
   391
   392	        if (status && (status.phase === 'done' || status.phase === 'failed')) {
   393	          return buildProjectResult(status);
   394	        }
   395
   396	        if (lockInfo && !status) {
   397	          return buildLockedQueuedResult(lockInfo);
   398	        }
   399
   400	        return await new Promise((resolve) => {
   401	          let settled = false;
   402
   403	          const cleanup = (): void => {
   404	            clearTimeout(timer);
   405	            clearInterval(dbPollInterval);
   406	            unsubSuccess();
   407	            unsubFailed();
   408	          };
   409
   410	          const resolveFromDeployLog = (lastLogCreatedAt: string): void => {
   411	            if (settled) return;
   412	            settled = true;
   413	            cleanup();
   414
   415	            const dbProject = appCtx.db.getProjectByName(projectName);
   416	            resolve({
   417	              active: 0,
   418	              jobs: [
   419	                formatJob({
   420	                  projectId: project.id,
   421	                  projectName: project.name,
   422	                  phase: 'done',
   423	                  startedAt: new Date(lastLogCreatedAt),
   424	                  completedAt: new Date(lastLogCreatedAt),
   425	                }),
   426	              ],
   427	              ...(dbProject ? { health: dbProject.status } : {}),
   428	            });
   429	          };
   430
   431	          const matchesProject = (payload: {
   432	            projectId: string;
   433	            parentProjectId?: string;
   434	          }): boolean => payload.projectId === project.id || payload.parentProjectId === project.id;
   435
   436	          const resolveWithCurrent = (timedOut: boolean): void => {
   437	            if (settled) return;
   438	            settled = true;
   439	            cleanup();
   440
   441	            const current = appCtx.jobManager.getStatus(project.id);
   442	            if (current) {
   443	              const lastLog = appCtx.db.getLastDeployLog(project.id);
   444	              const logIsNewer =
   445	                lastLog && new Date(lastLog.created_at).getTime() > current.startedAt.getTime();
   446	              if (
   447	                logIsNewer &&
   448	                lastLog.status === 'success' &&
   449	                (current.phase === 'failed' || current.phase === 'done')
   450	              ) {
   451	                resolve({
   452	                  active: 0,
   453	                  jobs: [
   454	                    formatJob({
   455	                      projectId: project.id,
   456	                      projectName: project.name,
   457	                      phase: 'done',
   458	                      startedAt: new Date(lastLog.created_at),
   459	                      completedAt: new Date(lastLog.created_at),
   460	                    }),
   461	                  ],
   462	                  ...(appCtx.db.getProjectByName(projectName)
   463	                    ? { health: appCtx.db.getProjectByName(projectName)?.status }
   464	                    : {}),
   465	                  ...(timedOut ? { timeout: true } : {}),
   466	                });
   467	                return;
   468	              }
   469
   470	              const payload = buildProjectResult(current) as Record<string, unknown>;
   471	              if (timedOut) payload['timeout'] = true;
   472	              resolve(payload);
   473	              return;
   474	            }
   475
   476	            const currentLockInfo = appCtx.db.getDeployLockInfo(project.id);
   477	            if (currentLockInfo) {
   478	              resolve(buildLockedQueuedResult(currentLockInfo, timedOut));
   479	              return;
   480	            }
   481
   482	            const dbProject = appCtx.db.getProjectByName(projectName);
   483	            resolve({
   484	              project: projectName,
   485	              status: dbProject?.status ?? 'unknown',
   486	              phase: 'none',
   487	              ...(timedOut ? { timeout: true } : {}),
   488	            });
   489	          };
   490
   491	          const unsubSuccess = eventBus.on('deploy:success', (payload) => {
   492	            if (matchesProject(payload)) resolveWithCurrent(false);
   493	          });
   494
   495	          const unsubFailed = eventBus.on('deploy:failed', (payload) => {
   496	            if (matchesProject(payload)) resolveWithCurrent(false);
   497	          });
   498
   499	          const timer = setTimeout(
   500	            () => {
   501	              resolveWithCurrent(true);
   502	            },
   503	            Math.max(1, timeoutSec) * 1000,
   504	          );
   505
   506	          const dbPollInterval = setInterval(() => {
   507	            if (settled) {
   508	              clearInterval(dbPollInterval);
   509	              return;
   510	            }
   511
   512	            const lastLog = appCtx.db.getLastDeployLog(project.id);
   513	            if (!lastLog || lastLog.status !== 'success') {
   514	              return;
   515	            }
   516
   517	            const currentJob = appCtx.jobManager.getStatus(project.id);
   518	            const logIsNewer =
   519	              !currentJob ||
   520	              new Date(lastLog.created_at).getTime() > currentJob.startedAt.getTime();
   521
   522	            if (logIsNewer) {
   523	              resolveFromDeployLog(lastLog.created_at);
   524	            }
   525	          }, 5000);
   526
   527	          status = appCtx.jobManager.getStatus(project.id);
   528	          if (status && (status.phase === 'done' || status.phase === 'failed')) {
   529	            resolveWithCurrent(false);
   530	          }
   531	        });
   532	      }
   533
   534	      const buildAllResult = () => {
   535	        const allJobs = appCtx.jobManager.getStatuses();

exec
/bin/bash -lc "nl -ba src/db/repos/project.repo.ts | sed -n '295,355p'" in /home/lee/OpenLander
 succeeded in 0ms:
   295	      .get();
   296	    return (row?.cnt ?? 0) > 0;
   297	  }
   298
   299	  acquireDeployLock(projectId: string, sessionId: string): boolean {
   300	    this.cleanExpiredDeployLocks();
   301	    this.db
   302	      .update(projects)
   303	      .set({
   304	        deploy_lock_session: sessionId,
   305	        deploy_lock_at: sql`CURRENT_TIMESTAMP`,
   306	        updated_at: sql`CURRENT_TIMESTAMP`,
   307	      })
   308	      .where(
   309	        and(
   310	          eq(projects.id, projectId),
   311	          or(isNull(projects.deploy_lock_session), eq(projects.deploy_lock_session, sessionId)),
   312	        ),
   313	      )
   314	      .run();
   315	    const row = this.sqlite.prepare('SELECT changes() as changes').get() as {
   316	      changes: number;
   317	    } | null;
   318	    return (row?.changes ?? 0) > 0;
   319	  }
   320
   321	  releaseDeployLock(projectId: string, sessionId?: string): boolean {
   322	    if (sessionId !== undefined) {
   323	      this.db
   324	        .update(projects)
   325	        .set({
   326	          deploy_lock_session: null,
   327	          deploy_lock_at: null,
   328	          updated_at: sql`CURRENT_TIMESTAMP`,
   329	        })
   330	        .where(and(eq(projects.id, projectId), eq(projects.deploy_lock_session, sessionId)))
   331	        .run();
   332
   333	      const row = this.sqlite.prepare('SELECT changes() as changes').get() as {
   334	        changes: number;
   335	      } | null;
   336
   337	      if ((row?.changes ?? 0) === 0) {
   338	        log.warn(
   339	          { projectId, sessionId },
   340	          '[DeployLock] releaseDeployLock session mismatch or lock missing',
   341	        );
   342	        return false;
   343	      }
   344
   345	      return true;
   346	    }
   347
   348	    this.db
   349	      .update(projects)
   350	      .set({ deploy_lock_session: null, deploy_lock_at: null, updated_at: sql`CURRENT_TIMESTAMP` })
   351	      .where(eq(projects.id, projectId))
   352	      .run();
   353
   354	    return true;
   355	  }

exec
/bin/bash -lc "nl -ba src/db/migration.ts | sed -n '88,112p'" in /home/lee/OpenLander
 succeeded in 0ms:
    88	    image_tag TEXT,
    89	    previous_image_tag TEXT,
    90	    public_url TEXT,
    91	    container_port INTEGER,
    92	    created_at TEXT DEFAULT CURRENT_TIMESTAMP,
    93	    updated_at TEXT DEFAULT CURRENT_TIMESTAMP,
    94	    UNIQUE(project_id, type)
    95	  )`);
    96	  sqlite.exec('CREATE INDEX IF NOT EXISTS idx_environments_project ON environments(project_id)');
    97
    98	  const environmentColumns = sqlite.prepare("PRAGMA table_info('environments')").all() as Array<{
    99	    name: string;
   100	  }>;
   101	  const environmentColNames = new Set(environmentColumns.map((c) => c.name));
   102
   103	  if (!environmentColNames.has('container_port')) {
   104	    sqlite.exec('ALTER TABLE environments ADD COLUMN container_port INTEGER');
   105	    // Backfill container_port from projects table for existing environments
   106	    sqlite.exec(
   107	      'UPDATE environments SET container_port = (SELECT container_port FROM projects WHERE id = environments.project_id) WHERE container_port IS NULL',
   108	    );
   109	  }
   110
   111	  const envVarColumns = sqlite.prepare("PRAGMA table_info('env_vars')").all() as Array<{
   112	    name: string;

exec
/bin/bash -lc "nl -ba src/pipeline/deploy/rollback.ts | sed -n '99,137p'" in /home/lee/OpenLander
 succeeded in 0ms:
    99	    try {
   100	      await this.cleanupRunningContainer(target.target);
   101
   102	      const { port, containerName } = await this.resolveContainerRuntime(target.target);
   103	      const containerPort = (await this.docker.getImageExposedPort(rollbackImageTag)) ?? port;
   104
   105	      const envType: OpenLanderEnv = 'production';
   106	      const containerId = await this.docker.runContainer({
   107	        imageTag: rollbackImageTag,
   108	        name: containerName,
   109	        port,
   110	        containerPort,
   111	        envVars: this.db.getEnvVars(projectId, productionEnvironment?.id),
   112	        traefikLabels: buildTraefikLabels(project.name, containerPort, undefined, envType),
   113	        network: getPolicy(envType).networkName,
   114	      });
   115
   116	      this.db.updateProject(projectId, {
   117	        status: 'running',
   118	        assignedPort: port,
   119	        containerPort,
   120	        containerId,
   121	        imageTag: rollbackImageTag,
   122	        previousImageTag: currentImageTag,
   123	      });
   124
   125	      if (productionEnvironment) {
   126	        this.db.updateEnvironment(productionEnvironment.id, {
   127	          status: 'running',
   128	          containerId,
   129	          imageTag: rollbackImageTag,
   130	          previousImageTag: currentImageTag,
   131	          assignedPort: port,
   132	          containerPort,
   133	        });
   134	      }
   135
   136	      await eventBus.emit('deploy:rollback', {
   137	        projectId,

exec
/bin/bash -lc "nl -ba src/pipeline/deploy-core.ts | sed -n '1510,1585p'" in /home/lee/OpenLander
 succeeded in 0ms:
  1510	      });
  1511
  1512	      this.jobManager?.updatePhase(projectId, 'starting');
  1513	      newPort = await allocatePort(this.db, this.docker, {}, 'production');
  1514	      const containerPort = (await this.docker.getImageExposedPort(imageTag)) ?? newPort;
  1515	      const envVars = resolveEnvVars({ projectId, environmentId }, { env: this.env });
  1516	      const secretFiles = this.env.getSecretFilesForDeploy(projectId);
  1517	      const networkName = getPolicy('production').networkName;
  1518
  1519	      greenContainerId = await this.docker.runContainer({
  1520	        imageTag,
  1521	        name: projectContainerName(`${projectName}-green`),
  1522	        port: newPort,
  1523	        containerPort,
  1524	        envVars,
  1525	        traefikLabels: { 'traefik.enable': 'false' },
  1526	        network: networkName,
  1527	        secretFiles,
  1528	      });
  1529	      shouldCleanupGreen = true;
  1530
  1531	      await eventBus.emit('deploy:run', {
  1532	        projectId,
  1533	        containerId: greenContainerId,
  1534	        port: newPort,
  1535	        url: getProjectUrl(projectName),
  1536	      });
  1537
  1538	      buildLog += `[health] Checking http://localhost:${String(newPort)}${healthCheckPath}\n`;
  1539	      const healthy = await this.healthCheck(
  1540	        newPort,
  1541	        healthCheckPath,
  1542	        healthCheckRetries,
  1543	        healthCheckIntervalMs,
  1544	      );
  1545
  1546	      if (!healthy) {
  1547	        throw new Error(
  1548	          `Health check failed for ${projectName} on port ${String(newPort)} path ${healthCheckPath}`,
  1549	        );
  1550	      }
  1551	      buildLog += '[health] Passed\n';
  1552
  1553	      // Promote green container: stop old blue, rename green to canonical name.
  1554	      // This avoids creating a new container (which caused port conflicts)
  1555	      // and achieves zero-downtime promotion.
  1556	      await this.docker.stopContainer(blueContainerId);
  1557	      await this.docker.safeRemoveContainer(blueContainerId);
  1558
  1559	      // Add Traefik labels to the green container so it receives traffic
  1560	      const canonicalName = projectContainerName(projectName);
  1561	      const greenContainer = this.docker.getClient().getContainer(greenContainerId);
  1562	      await greenContainer.rename({ name: canonicalName });
  1563	      shouldCleanupGreen = false;
  1564
  1565	      this.db.updateProject(projectId, {
  1566	        status: 'running',
  1567	        containerId: greenContainerId,
  1568	        assignedPort: newPort,
  1569	        containerPort,
  1570	        imageTag,
  1571	        previousImageTag: project.image_tag,
  1572	      });
  1573	      if (prodEnv) {
  1574	        this.db.updateEnvironment(prodEnv.id, {
  1575	          status: 'running',
  1576	          containerId: greenContainerId,
  1577	          assignedPort: newPort,
  1578	          containerPort,
  1579	          imageTag,
  1580	          previousImageTag: prodEnv.image_tag,
  1581	        });
  1582	      }
  1583
  1584	      const durationMs = Date.now() - startTime;
  1585	      const projectUrl = getProjectUrl(projectName);

exec
/bin/bash -lc "nl -ba test/db.test.ts | sed -n '433,472p'" in /home/lee/OpenLander
 succeeded in 0ms:
   433	      ).not.toThrow();
   434	    });
   435	  });
   436
   437	  describe('Environments container_port', () => {
   438	    beforeEach(() => {
   439	      db.createProject({ id: 'p1', name: 'my-app', repoUrl: 'https://github.com/test/a' });
   440	    });
   441
   442	    it('environments table has container_port column after migration', () => {
   443	      const dbPath = join(tmpDir, 'test.db');
   444	      const sqlite = createLegacySqlite(dbPath);
   445
   446	      const columns = (
   447	        sqlite.prepare('PRAGMA table_info(environments)').all() as Array<{
   448	          name: string;
   449	        }>
   450	      ).map((c) => c.name);
   451
   452	      sqlite.close();
   453	      expect(columns).toContain('container_port');
   454	    });
   455
   456	    it('updateEnvironment persists containerPort', () => {
   457	      const envs = db.getEnvironmentsByProject('p1');
   458	      const env = envs.find((e) => e.type === 'production');
   459	      expect(env).toBeDefined();
   460
   461	      db.updateEnvironment(env!.id, { containerPort: 8080 });
   462
   463	      const updated = db.getEnvironment(env!.id);
   464	      expect(updated).toBeDefined();
   465	      expect(updated!.container_port).toBe(8080);
   466	    });
   467	  });
   468	});

exec
/bin/bash -lc "nl -ba test/deploy-lock.test.ts | sed -n '279,370p'" in /home/lee/OpenLander
 succeeded in 0ms:
   279	      expect(result.error).toContain('Project not found');
   280	    });
   281	  });
   282
   283	  describe('get_deploy_status wait=true hardening', () => {
   284	    it('wait=true resolves from deploy_logs when job not in JobManager', async () => {
   285	      vi.useFakeTimers();
   286
   287	      db.createProject({
   288	        id: 'p1',
   289	        name: 'status-app',
   290	        repoUrl: 'https://github.com/test/status-app',
   291	      });
   292	      db.createDeployLog({
   293	        id: 'log-1',
   294	        projectId: 'p1',
   295	        status: 'success',
   296	        trigger: 'api',
   297	      });
   298
   299	      const ctx = createStatusToolContext(db, new JobManager());
   300	      const tool = getDeployStatusTool();
   301
   302	      const resultPromise = tool.execute(
   303	        { project_name: 'status-app', wait: true, timeout: 6 },
   304	        { appCtx: ctx, target: 'agent' },
   305	      ) as Promise<Record<string, unknown>>;
   306
   307	      await vi.advanceTimersByTimeAsync(5000);
   308	      const result = await resultPromise;
   309
   310	      expect(result).toMatchObject({
   311	        active: 0,
   312	        jobs: [expect.objectContaining({ name: 'status-app', phase: 'done' })],
   313	      });
   314	      expect(result).not.toHaveProperty('timeout');
   315	    });
   316
   317	    it('wait=true shows locked/queued when lock held but no active job', async () => {
   318	      db.createProject({
   319	        id: 'p1',
   320	        name: 'locked-status-app',
   321	        repoUrl: 'https://github.com/test/locked-status-app',
   322	      });
   323	      db.acquireDeployLock('p1', 'test-session');
   324
   325	      const ctx = createStatusToolContext(db, new JobManager());
   326	      const tool = getDeployStatusTool();
   327
   328	      const result = (await tool.execute(
   329	        { project_name: 'locked-status-app', wait: true, timeout: 30 },
   330	        { appCtx: ctx, target: 'agent' },
   331	      )) as Record<string, unknown>;
   332
   333	      expect(result).toMatchObject({
   334	        active: 1,
   335	        locked: true,
   336	        lock_session: 'test-session',
   337	        jobs: [expect.objectContaining({ name: 'locked-status-app', phase: 'queued' })],
   338	      });
   339	    });
   340
   341	    it('wait=true transitions through queued to done when deploy event fires', async () => {
   342	      db.createProject({
   343	        id: 'p1',
   344	        name: 'evented-status-app',
   345	        repoUrl: 'https://github.com/test/evented-status-app',
   346	      });
   347	      db.acquireDeployLock('p1', 'test-session');
   348
   349	      const jobManager = new JobManager();
   350	      const ctx = createStatusToolContext(db, jobManager);
   351	      const tool = getDeployStatusTool();
   352
   353	      const queuedResult = (await tool.execute(
   354	        { project_name: 'evented-status-app', wait: true, timeout: 30 },
   355	        { appCtx: ctx, target: 'agent' },
   356	      )) as Record<string, unknown>;
   357
   358	      expect(queuedResult).toMatchObject({
   359	        active: 1,
   360	        locked: true,
   361	        lock_session: 'test-session',
   362	        jobs: [expect.objectContaining({ name: 'evented-status-app', phase: 'queued' })],
   363	      });
   364
   365	      jobManager.trackJob('p1', 'evented-status-app');
   366	      jobManager.updatePhase('p1', 'done');
   367	      db.releaseDeployLock('p1');
   368	      await eventBus.emit('deploy:success', {
   369	        projectId: 'p1',
   370	        url: 'https://example.test',

exec
/bin/bash -lc "nl -ba test/deploy-lock.test.ts | sed -n '370,410p'" in /home/lee/OpenLander
 succeeded in 0ms:
   370	        url: 'https://example.test',
   371	        totalDurationMs: 1000,
   372	      });
   373
   374	      const doneResult = (await tool.execute(
   375	        { project_name: 'evented-status-app', wait: true, timeout: 30 },
   376	        { appCtx: ctx, target: 'agent' },
   377	      )) as Record<string, unknown>;
   378
   379	      expect(doneResult).toMatchObject({
   380	        active: 0,
   381	        jobs: [expect.objectContaining({ name: 'evented-status-app', phase: 'done' })],
   382	      });
   383	    });
   384	  });
   385	});

exec
/bin/bash -lc "nl -ba src/web/api/routes.ts | sed -n '186,202p'" in /home/lee/OpenLander
 succeeded in 0ms:
   186	    'alert:resolved',
   187	  ];
   188
   189	  // Auto-release deploy locks on completion/failure
   190	  eventBus.on('deploy:success', (p) => {
   191	    ctx.db.releaseDeployLock(p.projectId);
   192	  });
   193	  eventBus.on('deploy:failed', (p) => {
   194	    ctx.db.releaseDeployLock(p.projectId);
   195	  });
   196	  eventBus.on('compose:up', (p) => {
   197	    ctx.db.releaseDeployLock(p.projectId);
   198	  });
   199	  eventBus.on('compose:failed', (p) => {
   200	    ctx.db.releaseDeployLock(p.projectId);
   201	  });
   202

exec
/bin/bash -lc "rg -n \"cleanExpiredDeployLocks\\(\" -n src/db/repos/project.repo.ts src/db/index.ts && nl -ba src/db/repos/project.repo.ts | sed -n '356,390p'" in /home/lee/OpenLander
 succeeded in 0ms:
src/db/index.ts:141:  cleanExpiredDeployLocks(timeoutMinutes = 10) { return this.projectRepo.cleanExpiredDeployLocks(timeoutMinutes); }
src/db/repos/project.repo.ts:300:    this.cleanExpiredDeployLocks();
src/db/repos/project.repo.ts:363:  cleanExpiredDeployLocks(timeoutMinutes = 30): number {
   356
   357	  getDeployLockInfo(projectId: string): { session: string; lockedAt: string } | null {
   358	    const project = this.getProject(projectId);
   359	    if (!project?.deploy_lock_session || !project.deploy_lock_at) return null;
   360	    return { session: project.deploy_lock_session, lockedAt: project.deploy_lock_at };
   361	  }
   362
   363	  cleanExpiredDeployLocks(timeoutMinutes = 30): number {
   364	    this.db
   365	      .update(projects)
   366	      .set({ deploy_lock_session: null, deploy_lock_at: null })
   367	      .where(
   368	        sql`${projects.deploy_lock_session} IS NOT NULL AND ${projects.deploy_lock_at} < datetime('now', '-' || ${timeoutMinutes} || ' minutes')`,
   369	      )
   370	      .run();
   371	    const row = this.sqlite.prepare('SELECT changes() as changes').get() as {
   372	      changes: number;
   373	    } | null;
   374	    return row?.changes ?? 0;
   375	  }
   376	}

codex
1. 높음: `wait=true`가 “이번 배포”가 아니라 “가장 최근 성공 로그”만 보고 `done`으로 오판할 수 있습니다. [src/tools/defs/deploy.ts:512](/home/lee/OpenLander/src/tools/defs/deploy.ts#L512) 의 5초 poll은 `lastLog.status === 'success'` 이고 현재 `JobManager`에 job이 없으면 바로 완료로 해석합니다. 그런데 호출 시점 이전의 오래된 성공 로그와 현재 idle 상태를 구분할 기준이 없습니다. 그래서 idle 프로젝트에 `get_deploy_status(wait=true)`를 호출해도 5초 뒤 `done`이 반환될 수 있고, 락 획득 전/후 레이스로 새 배포 시작 직전의 공백 구간에서도 이전 성공 로그를 주워 false positive가 납니다. 최소한 호출 시작 시각, lock session, 혹은 관찰 대상 job 시작 시각을 기준으로 “이 호출 이후 생성된 로그만” 봐야 합니다.

2. 중간: `environments.container_port` backfill은 “컬럼이 없는 DB”에서만 실행되어, `66ceceb`를 한 번 거친 DB는 `d31f708` 이후에도 영구히 `NULL`이 남을 수 있습니다. [src/db/migration.ts:103](/home/lee/OpenLander/src/db/migration.ts#L103) 은 `container_port` 컬럼이 없을 때만 `ALTER TABLE`과 `UPDATE ... SET container_port = (SELECT ...)`를 같이 수행합니다. 즉 `66ceceb` 시점에 이미 컬럼만 추가된 인스턴스는, 이후 `d31f708`로 올려도 조건문에 걸리지 않아 backfill이 재실행되지 않습니다. 데이터 무결성 측면에서 backfill은 컬럼 존재 여부와 분리해 idempotent하게 항상 실행하거나, 최소한 `WHERE container_port IS NULL` 조건으로 별도 실행해야 안전합니다.

3. 낮음: 새 테스트 하나가 실제 동작을 검증하지 못합니다. [test/deploy-lock.test.ts:341](/home/lee/OpenLander/test/deploy-lock.test.ts#L341) 의 “queued to done” 테스트는 첫 호출이 [src/tools/defs/deploy.ts:396](/home/lee/OpenLander/src/tools/defs/deploy.ts#L396) 에서 즉시 `queued`로 반환된 뒤, 상태를 바꾸고 두 번째 호출로 `done`을 확인합니다. 즉 이벤트 구독 경로([src/tools/defs/deploy.ts:491](/home/lee/OpenLander/src/tools/defs/deploy.ts#L491))나 5초 DB poll 경로를 전혀 타지 않아서, 설명과 달리 “하나의 wait 호출이 queued에서 done으로 전이되는지”는 검증하지 못합니다. 이 테스트는 회귀 방지력이 약합니다.

세션 스코프 lock 자체는 이번 변경 범위 안에서는 “영구 잠금” 시나리오를 재현하지 못했습니다. `acquire`/`release`가 같은 `sessionId`를 쓰고, mismatch 시에도 lock을 지우지 않을 뿐이며, 만료 청소도 있습니다. 다만 변경 밖의 코드인 [src/web/api/routes.ts:190](/home/lee/OpenLander/src/web/api/routes.ts#L190) 부근은 여전히 unconditional release라서, 영구 잠금 대신 “오래된 완료 이벤트가 새 세션 lock을 너무 일찍 풀어버리는” 반대 방향의 리스크는 남아 있습니다.

확인 결과 관련 테스트 6개와 `typecheck`는 모두 통과했습니다. 변경 파일들에서는 SQL injection 징후는 보이지 않았고, 쿼리는 Drizzle 조건식이나 상수 SQL만 사용합니다.
tokens used
192,013

```

## Concise summary

Provider completed successfully. Review the raw output for details.

## Action items

- Review the response and extract decisions you want to apply.
- Capture follow-up implementation tasks if needed.
